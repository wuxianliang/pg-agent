# V8 剩余范围里程碑计划（G14–G19）· 2026-09-16

> 定位：一次性补全 `docs/designs/v8-dev.md`（871 行）中既有 17 gate 尚未覆盖的剩余冻结面——并发竞态与探针（G14）、failure-drain（G15）、audit 指纹与三类拒绝路径（G16）、compact 三命令（G17）、driver 切换正向 + fork 完整面（G18）、收尾杂项 + v10 次级依赖（G19）。现状基线 = 既有 17 gate **2965 PASS**（G1–G13：G1 191 / G2 86 / G3 130 / G4 266 / G5 250 / G6 333 / G7a 255 / G7b 133 / G7c 214 / G8a 93 / G8b 222 / G9a 51 / G9b 72 / G10 144 / G11 207 / G12 165 / G13 153，见矩阵表头）。实现依据：规格原文**唯一权威**、`docs/analysis/v8-impl-digest/` 摘要为入口——凡 digest 与原文冲突以原文为准，发现冲突即停下对照原文改判并记偏差台账。

> 命名铁律：与既有 G1–G13 完全同构——`uv run python v8/<dir>/test_<name>.py`、库名 `agent_v8_<stage>`、`SETUP` 累计加载 `v8/load.py` 全序 SQL。本计划不发明合同外要求；按文件所有权与依赖切分，**不把跨 gate 的大改塞进一个 gate**（G12 gate 的 165 PASS 在本计划中保持逐字冻结）。注：六份抽取各自用过内部 gate 号（如 compact 抽取标"G14"、closeout 抽取标"G14/G15"）——本计划统一重编号为 G14–G19。

## Gate 总表

| Gate | 目录 / 测试 | 数据库 | 验收内容（合同条款级，MUST 逐条） | 外部阻塞 |
|---|---|---|---|---|
| G14 | `v8/concurrency/test_concurrency.py`（新目录 `v8/concurrency/`，与其它 stage 同构：独立 `setup_db.py`（DB=`agent_v8_concurrency`、STAGE=`concurrency`）+ `test_concurrency.py` + `__init__.py`；不新增 SQL 文件——复用 gates 加载集） | `agent_v8_concurrency` | **并发竞态与探针补强**（矩阵 #5/#8/#15 余量 + #2 第二余项「判定源独立性注入的 recovery 臂」；不新增业务合同、不新增 SQL 文件；**MUST NOT 复用/改写 `v8/gates/setup_db.py`**——G12 的 `test_gates.py` 以 `from v8.gates.setup_db import DB` 冻结引用 `agent_v8_gates`，改写该模块会静默重定向 G12 到另一库名、破坏「G12 逐字冻结」）。D1 Conformance 8 (vi) 下线×seal 竞态——两条 seal 路径（初始 decision seal `v_prepare_step` / tools seal `v_seal_batch`）× 两条提交序，含"初始 seal 绑定未提交时下线并发（generation 行锁互斥）"两序：P1『下线先提交』= 真实 `v_revoke_generation` 取 generation FOR UPDATE（函数返回但不提交、锁保持）→ seal 线程阻塞于 generation FOR SHARE（tools 半则阻塞于 T1 预锁的 session 行锁）→ T1 提交 status='failed' → T2 解除，断言 seal 稳定 `rejected_mismatch`+保留 `GENERATION_REVOKED`、`counts(session)==(0,0,0,0)`（不创建 step/LLM slot/batch/tool effect/首 attempt/seal receipt）、无新增 dispatch；P2『seal 先提交』= seal 持 session FOR UPDATE + generation FOR SHARE（创建已完成未提交）→ 真实下线线程直达 generation FOR UPDATE 阻塞于 T1 的 FOR SHARE → T1 提交 → 锁内重扫发现新绑定≠预锁集 → 内部整体回滚重试 → drain，断言 seal accepted（batch/成员原子创建）、其未 dispatch effect 被重扫描 drain 为 `cancelled_before_dispatch`＋audit 记 `GENERATION_REVOKED`、下线 `attempts>=2`、`SELECT count(*) FROM effect_attempts WHERE effect_id=<新 effect>` 不含 `dispatch_started` 行。D2 Conformance 8 (viii) 下线×retry allocation 竞态——普通 `retry_effect` 与 recovery `recovery_takeover` 各两条提交序 + 两段最终收束 + unknown sibling 前置阻断独立子用例：A『下线先提交』= holder 持 generation FOR UPDATE（或真实下线线程持 session+generation），`retry_effect` 线程阻塞于 generation FOR SHARE 第 4 步 → 提交 failed → 解除，断言零分配（0 新 attempt、`attempt_no` 不递增）、普通入口 receipt `rejected_mismatch`+`GENERATION_REVOKED`；同事务最终聚合按两段唯一裁定 (α) 三合取命中 → step `failed_terminal`/`GENERATION_REVOKED`＋session `failed`/`GENERATION_REVOKED`（不永久停留 `failed_retryable`）、(β) 未命中 → cohort 维持 `failed_retryable`、step 维持等待态（不提前终态化）；B『allocation 先提交』= T1 `retry_effect` 持 session FOR UPDATE＋generation FOR SHARE＋新 attempt 已建未提交 → T2 真实下线线程阻塞于 session 行锁 → T1 提交 → 解除 → drain，断言分配有效、新 attempt 随后 `cancelled_before_dispatch`、MUST NOT 新 dispatch、冻结身份回滚（attempt 行留存）；recovery 入口对照 = 命令整体 `accepted`＋子结果 `allocation_denied: GENERATION_REVOKED`、接管与旧 attempt 结算不回滚；unknown sibling 阻断子用例 = 同批含 `unknown_outcome` sibling → 不命中规则 5、不达授权/generation 门、无 `allocation_denied:GENERATION_REVOKED` 子结果、cohort 维持 `failed_retryable`、step 维持 `blocked_unknown_effect`。**判定源独立性注入 recovery 臂（矩阵 #2 第二余项）**：对 `recovery_takeover` 与普通 `complete_effect` 两入口分别注入 parent 快照偏离（`effect_requests` 派生快照与当前最大 attempt 权威行不一致）与 envelope 四源读取（session/effect/attempt/fence）逐源偏离，断言唯一证据分类函数**只按注入后的持久化控制态**独立判定——不采信 parent 快照、不采信任何单一声明源、偏离源不进冻结快照域；偏离注入后合法 completion 仍按既有 fence 序 `STALE_JOB_FENCE`/`ATTEMPT_SUPERSEDED` 稳定拒、零控制态，两入口三层结果与 code 一致。D3 Conformance 15 finish×尾部 chunk 竞争——streaming LLM effect（`execution_mode='streaming'`、`decision_only` 关 turn），chunks 0..N-1 已 append、completion 带 `stream_complete=true`+`final_chunk_index=N`（final 先到、缺尾 chunk N）；序 A（chunk 先提交）尾部合法 chunk append → `finish_session` → 断言等值按完整流执行通过（无 conflict fact）、canonical end 唯一、session `completed`；序 B（finish 先提交）`finish_session` 提交（session terminal）→ 尾部合法 chunk append（terminal 分支白名单、观测类）→ 断言 chunk accepted、session 保持 `completed`、effect 终态/turn end/槽位内容不变（MUST NOT 回退 effect 或重开 turn）；等值生命周期拆分——正确尾部→无 conflict fact、矛盾尾部→`CANONICALIZER_CONFLICT` 事实＋portable 验收器判失败且业务终态不变；显式交错形态（holder 持 session 行 FOR UPDATE → chunk append 线程阻塞于 `sessions FOR UPDATE` → 提交 → 按 terminal 分支放行；反向同断言）。D4 Conformance 15 槽位显式锁序位探针（第八位主锁序第七位 turn_end_slot：attempt→slot）——holder 持目标 `turn_end_slots` 行 FOR UPDATE；T2 repair 线程经 session→step→effect→attempt 后阻塞于 `v_turn_end_slot_update` 槽位 FOR UPDATE；T3 以 NOWAIT/短 `statement_timeout` 对 `effect_attempts` 目标行 FOR UPDATE 必败（证明 attempt 先于 slot）；查 `pg_locks` 断言 T2 的 attempt 锁 granted、turn_end_slots 锁 waiting；holder 提交 → T2 解除 → 唯一 canonical `turn/end` 收敛、反转提交序 trace 仍唯一；并产出"规格要求取得集 vs 实测取得集"差异报告（规格 L356/L395 将 `complete_effect`/`request_cancel`/`FORCE_JOB_TAKEOVER` 列为槽位锁取得者，实现全部 `IF NOT EXISTS`+INSERT 建槽、从不取槽位行锁——逐路径输出 `pg_locks` 是否含 granted 的 turn_end_slots 行锁之布尔，存在未取得路径则显式断言"差异已记入台账/计划"，避免静默通过）；compact 末位次序（第八位）由 G17 锁序探针（矩阵 #6）补齐，本 gate 只断言 attempt→turn_end_slot 两相邻位序、harness 须可扩展。D5 Conformance 5 EffectResult 声明 authority vectors (ii)(iii)（实现已在、仅缺断言，落 `v8/effect/test_effect.py::test_outcome_mismatch`）：(ii) 声明 `failed_retryable` × 证据无终局回执（无 `provider_receipt` 且无 `no_side_effect_proof`→唯一分类函数输出 unknown）→ completion accepted、effect MUST 收束 `unknown_outcome`（待 repair）、MUST NOT 落已知失败、同事务写 `effect_audit.reason='RESULT_OUTCOME_MISMATCH'` 恰一行；(iii) 声明 `cancelled_after_dispatch` × 证据 provider 证实成功（`known_success`+`provider_receipt`）→ completion accepted、effect MUST `succeeded`、MUST NOT 落取消终态、同事务 mismatch audit 恰一行；(iv) 声明与派生一致 → mismatch audit 行数 0（既有已覆盖，对照）。**`v8/effect/test_effect.py` 共享写入注意**：本文件另有 `DECISION_PLAN_INVALID` 纯 receipt 拒绝断言块（`test_effect.py` 约 L620–629，断言拒绝后 `effect_requests.status=='dispatch_started'` 零控制态）——该块随 G15-D4 的行为改造归 **G15 所有权**（见 G15 行与「文件所有权」段），G14 仅在其自身 D5 的声明向量处编辑本文件；故 G14 与 G15 **共改同一物理文件、MUST 串行**（Wave1 并行声称据此撤销）。D6 回归与记账：矩阵 #2/#5/#8/#15 四行状态与缺口去向更新；台账按 D4 探针结论追加 A 系条目（若裁定记偏差）或 B 节划除项；本计划默认新 gate（G12 165 PASS 逐字不变）。 | 否（全为 DB 层锁等待交错与声明向量，无 fake-tool 执行器依赖；规格"fake 工具调用次数不增加"统一翻译为 DB 可观测等价"无新增 `effect_attempts` 行 / 无新 `dispatch_started`"） |
| G15 | `v8/drain/test_drain.py`（新 stage） | `agent_v8_drain` | **failure-drain**（`v_fail_session` / INFRA 闭包函数现完全缺失）。D1 `fail_session` 第 (3) 类 INFRA 收束闭包：session 在同一控制事务立即 terminal failed / failure_code ∈ {INFRA_ASSEMBLY_FAILED, INFRA_PROTOCOL_VIOLATION}（lease 撤销、`session_fence` 单调+1、`active_step_id` 清空、lease 三列 NULL）；两 code 各一 fixture；传入非闭合集 code 稳定拒。D2 INFRA 三分支 drain（与 WORKSPACE_LOST 同款、仅 code 替换）：(i) unknown_outcome effect → step 保持 `blocked_unknown_effect`/`UNKNOWN_AFTER_DISPATCH`；(ii) in-flight pending 且非 sticky cancel → step `waiting_effect`（sticky → `cancel_requested`）、outcome_code NULL、标 drain pending；(iii) 皆无 → step `failed_terminal`/对应 INFRA code；重复 drain 幂等（无第二语义事件、`session_fence` 不再递增、仅刷新 `drain_step_id`）。D3 INFRA 消费 pre-dispatch 双表原子同步共享子操作：事务内每个 ready effect 与其当前 max `attempt_no` 行同事务转 `cancelled_before_dispatch`、parent 派生执行快照同步、查两表无单边中间态、写 `ABORTED_BEFORE_DISPATCH` audit、不产生 completion 语义事件/receipt；收束后到达的 worker completion 因 attempt 已终态被既有 envelope/mismatch 规则拒、仅写 receipt+effect_audit。D4 `DECISION_PLAN_INVALID` 拒绝改为同事务 INFRA 收束（`v8_effect.sql` 四个拒绝点由 `v8_reject_command` 纯 receipt 改造）：四类非法标记组合（空 plan×decision_only=false、空 plan×decision_only=true×final_tools=true、非空 plan×decision_only=true、非空 plan×decision_only=false×final_tools=false）各一 fixture ＋ plan canonical 缺失/非 JSON/tools 不一致三形态：receipt outcome='rejected_mismatch'/code='DECISION_PLAN_INVALID'，同事务 effect=`failed_terminal`、step=`failed_terminal`/`INFRA_PROTOCOL_VIOLATION`、session=`failed`/`INFRA_PROTOCOL_VIOLATION`、effect.`retry_stop_reason` 按 §3.2.2 有序分类派生（max_attempts=1 → first_attempt_failure）；同 command_id 重试幂等、零新增控制态。**G4 断言同步更新（本 gate 交付项）**：`v8/effect/test_effect.py` 的 `DECISION_PLAN_INVALID` 断言块（现约 L617–629，断言拒绝后 `effect_requests.status=='dispatch_started'`、零 mismatch audit 行）MUST 改写为同事务 INFRA 收束后的新契约——effect=`failed_terminal`、step/session=`failed`/`INFRA_PROTOCOL_VIOLATION`、`effect_audit` 恰留收束行；该断言块随本 gate 所有权自 G14 迁入 G15（G14/G15 共改 `v8/effect/test_effect.py` ⇒ Wave1 MUST 串行）。D5 父层 code 优先级：先经 WORKSPACE_LOST/INFRA_ASSEMBLY_FAILED fail-closed 持久化 `session.failure_code`，再提交非法标记 decision completion → effect `failed_terminal`/`INFRA_PROTOCOL_VIOLATION` 留 audit、`session.failure_code` 保持既有 cause、step 按既有 drain 三分支收束、session 全程不离开 failed。D6 两排除形态：(a) (iii)/(iv) pending observation（`stream_complete=false` 非终局）→ 走 observation 分流、五款状态门裁定、MUST NOT 判 `DECISION_PLAN_INVALID`、零控制态修改、不写收束；(b) quiescing 下终局 `complete_effect` → 返回 `DRIVER_QUIESCING`、不结算、不进语义层、不触发收束。D7 A30：`v_retry_stop_reason_cas` CAS 不一致由 RAISE EXCEPTION 事务 abort 改为受控 `INFRA_PROTOCOL_VIOLATION` 收束（列保原值、两侧值留存 audit、同 command_id 重试幂等）。**G7a 断言同步更新（本 gate 交付项）**：`v8/retry/test_retry.py`（现约 L987–999，以 `except psycopg2.Error` 断言 `CAS mismatch -> INFRA_PROTOCOL_VIOLATION`、无返回值契约、且该文件目前不在任何 gate 所有权清单内）MUST 改写为返回契约——函数返回受控结果（不再 RAISE、不再 abort 事务），断言：返回值 = `INFRA_PROTOCOL_VIOLATION` 收束 outcome/code ＋ `effect_audit` 收束行恰一条（两侧值留存）＋ `effect_requests.retry_stop_reason` 列保持原值 ＋ 同 command_id 重试幂等（无第二语义事件）；`v8/retry/v8_retry.sql` L185–188 的函数体注释同步改述。该文件所有权自本 gate 起显式纳入（先前「无人所有」即本问题的静默回归来源）。D8 `ISOLATION_UNSUPPORTED` 入口 INFRA 半边：isolation > read committed 事务内调用 INFRA 收束 → 返回 `ISOLATION_UNSUPPORTED`、零控制态。D9 terminal 子协议矩阵 (b) 行对 INFRA 放行 + A98 回归：INFRA 收束后未决 unknown effect 的 repair 正常采信收束（recovery claim 仅收束用途可取得）；in-flight pending 的后续 completion 照常结算并重复 drain 判定；全程 session 保持 failed/INFRA code、MUST NOT 创建新 step/effect/attempt；completed/cancelled 与非 drain 失败仍稳定拒 `SESSION_TERMINAL`。D10 `drain_step_id` 写入/清空合同：存在非终态 drain step 时写 `sessions.drain_step_id`、`active_step_id` 保持清空；drain 全部完成同事务清空 `drain_step_id`；重复 drain 只刷新指针。D11 pre-dispatch 三触发源边界与单一化交接：断言 WORKSPACE_LOST drain 与 INFRA 收束均调用同一共享 pre-dispatch 子操作（同一函数身份）；request_cancel/§4 generation drain 既有调用点回归不变；两份实现（grant 窄版 `v_predispatch_cancel_sync` × plugin 六参版 `v_pre_dispatch_cancel_sync`，A68/A73）的单一化**本计划显式归 G17（compact-driver 收口，见 G17 D14）**——G12 已交付但**未做**单一化（A73 明记两定义暂并存），故 MUST NOT 再指向已关闭的 G12；单一化三调用点为 WORKSPACE_LOST drain（消费 grant 窄版）、`v_request_cancel` 与 §4 generation drain（消费 plugin 六参版），统一到富实现（六参缺省版）并删除窄版；本 gate 以行为等价断言替代、不断言两份同源。D12 fail_session 来源枚举负向 + `INFRA_ASSEMBLY_FAILED` 触发源标注：(a) 纯 eligible `failed_retryable` 批次经聚合规则 5 → step `failed_retryable`/session `ready`、MUST NOT 进入 fail_session；(b) 仅规则 4 terminal failure 批次 → 进入 fail_session 第 (1) 类、code 由 `retry_stop_reason` 集合唯一派生；(c) `INFRA_ASSEMBLY_FAILED` 以直接调用闭包覆盖（assemble 真实 caller 标注为 G19）。D13 文档与 gate 表更新：`v8/README.md` gate 表新增 drain 行 + 从已知边界删除 DECISION_PLAN_INVALID 条目（现 L63）；台账 B 节 L119 划消、**A40 的「DECISION_PLAN_INVALID 仍为纯 receipt 拒绝，INFRA 收束随 drain 里程碑」子句划改**（本 gate D4 消解该偏差）。 | 否（跨里程碑边界：§4 generation drain→G17/G18；INFRA_ASSEMBLY_FAILED 真实 assemble 触发→G19） |
| G16 | `v8/audit/test_audit.py`（新 stage） | `agent_v8_audit` | **audit 指纹**（矩阵 #1/#10/#15 共同去向；台账 A11 补全、B 节 L121 消除）。D1 `rejection_fingerprint@v1`（Py）：输入=命令 payload 原始字节全集、SHA-256→64 字节小写 hex、结构性分离（MUST NOT 重序列化、无字节剔除）；含组合字符/转义/成员顺序不同的语义等价两份 payload 返回**不同** fingerprint（证明无重序列化）；硬编码 golden。D2 `transport_rejection_key@v1`（Py）：输入=接入层原始报文/帧字节、不需 payload 边界、MUST NOT 引用 canonical hash 或 rejection fingerprint；同帧不同 payload 解释 → 同键；golden。D3 `malformed_binding_fingerprint@v2`（Py）：字段编码 `[8B 大端名长][名原始字节][tag ASCII]0x00[8B 大端值长][值原始字节]`；两段编码序（固定十字段 → unknown_fields 段按解码名 UTF-8 字节序分组×组内出现序）；digest=SHA-256；版本标签不入 hash 输入；十字段 occurrence_no 0–9、缺字段合成占位 `type_tag=NULL`/`value_raw` 零长、含 NUL 字段名不产生定界歧义、plain 拼写显式 null == 合成占位而 escaped 拼写互异、≥3 组 golden。D4 `received_result_fingerprint@v2`+`raw_invalid_digest@v1`（result 侧）：严格两径（完整 ABI 解析→DFS occurrence grammar 六条、容器行先于子行、重复字段按出现序；任何层级失败→单一 `RAW_INVALID` occurrence，固定名 `__raw_invalid__`/tag RAW/value=digest）；`raw_invalid_class` TRUNCATED/CORRUPT_BYTES/UNBOUNDED_KEY 有序首中即停；两路径保存原始字节非重序列化产物。D5 三类拒绝路径判定序 (a)(b)(c)+三键 kind 唯一归属：(a) ID 不可归属→`transport_rejection_key@v1`、不占 command_id、独立 ingress 存储；(b) transport 畸形（有效 ID）→`transport_rejection_key@v1` 并占 command_id（首占 `rejected_mismatch` / 已有 binding→`IDEMPOTENCY_CONFLICT`）；(c) transport 合法不可规范化→`rejection_fingerprint@v1` 经分流占 command_id；(1)(2) 步三键共用查重/首占、(3)(4) 仅 canonical 路径；`(b)` 先于 `(c)` 顺序断言；三键混用被拒；accepted 命名空间仅含 `canonical_request_hash`。D6 `effect_audit` 全列补齐（经新 `v8/audit/v8_audit.sql` ALTER、不动 G2 冻结 DDL：A8/A55/A90 先例）：补 `command_id`、`result_hash`、`result_parse_path`(三值)、`raw_invalid_class`/`binding_invalid_class`(各三值)、`received_session_id/step_id/effect_id/attempt_no`、`expected_driver`/`received_driver`、`expected_driver_epoch`/`received_driver_epoch`、`expected_dispatch_session_fence`/`received_...`、`expected_job_fence`/`received_job_fence`、`expected_request_hash`/`received_request_hash`、`expected_idempotency_key_hash`/`received_idempotency_key_hash`、`received_binding_raw bytea`、`received_result_raw bytea`；保留 10 列 `UNIQUE NULLS NOT DISTINCT`（含 internal 三列）；逐列存在性 + 三值 CHECK 负向 + 去重键恰 10 列 + 内部三列全有全无联合 CHECK 负向。D7 occurrences 两子表（DDL+不可变触发器+DB 级等价关系）：`effect_audit_binding_occurrences`（type_tag 五值禁 RAW）与 `effect_audit_result_occurrences`（六值含 RAW），`PK(audit_id, occurrence_no)`、`occurrence_no>=0`、行不可变；等价触发器强制 result 侧三形态与 binding 侧分流（`invalid_binding`⇔class 非 NULL ∧ 行数 0 ∧ raw 64 字节摘要；`canonical/malformed`⇔class NULL ∧ 行数 ≥10）；occurrence_no 升序重放可生成字节级相同 fingerprint。D8 接收时一次性抽取+脱敏（统一递归脱敏域 N06）+三载体同源：idempotency key 原值替换为 SHA-256 hash（occurrence value_raw=64 字节 hex、type_tag TEXT）；嵌套/数组内同名成员递归替换；三载体（received_* 标量、received_binding_raw、occurrences 子表）grep 原值零命中；显式 null/非字符串→`invalid_binding` UNBOUNDED_KEY、零 occurrence、raw 存 64 字节摘要。D9 effect_audit 写入路径接线（`complete_effect`/`reconcile` 结构层拒绝）：expected 侧读 attempt 执行快照/effect `current_job_fence`/sessions cancellation_epoch；received 侧保存脱敏后 binding/result；不可归属形态 expected 四列统一 NULL、唯一身份来源=audit_context_session_id；每类结构层拒绝（stale/mismatch/repair）写一行 audit 且零控制态修改、幂等一行/同 binding 异 result 两行。D10 独立 ingress/malformed receipt 存储（路径 (a)）：键对 kind=`transport_rejection_key`、绑定授权调用上下文、不建 command_bindings/不占 command_id；同报文重发按该键幂等；RLS 跨租户不可查。D11 指纹 golden vectors（Py 硬编码 + SQL==Python 字节级）：三键+`received_result_fingerprint@v2`+`raw_invalid_digest@v1` 各 ≥3 组独立手算期望（含 escaped/嵌套/未知字段/重复字段/含 NUL 字段名）；版本标签不进入 hash 输入；golden 以硬编码常量落 `test_canonical.py` 与 `test_audit.py`（A75 先例）。D12 v10 §6.4 复用合同注记：三键算法为 v10 O 域 typed key 的精确复用源（只改作用域），函数签名/输入域/输出域（64 字节小写 hex）文档化，v10 可直接 import/调用。D13 gate 落点与全量回归：既有 17 gate 零回归（断言计数若变按 A48/A71 记台账）。**D14 `internal_op_audits` 表创建（本 gate 交付，消解 G17-D8 归属矛盾）**：G16 加载序先于 G17，故该表 DDL MUST 由 G16 落于 `v8/audit/v8_audit.sql`——逐列（parent_session_id/parent_command_id/internal_op_ordinal `CHECK(>=0)`/internal_op_kind 五值闭合/source_operation/target_identity/event_key/parent_receipt_ref）+ **两条 UNIQUE 约束**（`UNIQUE(parent_session_id,parent_command_id,internal_op_ordinal)` 与 `UNIQUE(parent_session_id,event_key)`）+ ordinal/kind 的 CHECK 负向＋写入后不可变触发器；G17 仅消费（写 `compact_terminal_abort` 行），不得重复创建。跨里程碑裁定（「依赖与顺序」段）据此以 G16 为唯一创建者。 | 半阻塞（v8 无真实 transport/ingress 层，路径 (a)/(b) 的"原始报文/帧字节"无 host；按 G13 先例交付 DB 侧、以 raw frame bytes 为入参的可注入 ingress 拒绝函数（host 无关合同面），真实 transport 接线明确划为范围外——不阻塞 DB 侧交付） |
| G17 | `v8/compact/test_compact.py`（新 stage） | `agent_v8_compact` | **compact 三命令**（控制平面整体缺席；矩阵 #6 余量即验收目标、Conformance 6）。D1 `compactions` 控制态表 DDL（s33 §1.1 冻结逐列）：12 列、`UNIQUE(session_id,compaction_id)`、部分唯一索引 `UNIQUE(session_id) WHERE status='locked'`、status 三值、状态条件绑定 CHECK（finalized ⇔ result_identity NOT NULL ∧ abort_identity NULL 等三态）、`base_seq`/`through_seq` 创建后不可变、终态行 MUST NOT 删除；经新 stage 文件落地、G2 冻结 DDL 不动。D2 `compact_lock`（idle→locked 唯一进入）：统一命令 envelope + compact capability 有效 grant；创建 locked 行冻结 `base_seq`/`through_seq`（=锁内最大已分配 seq）；同事务写 `compaction/start` 审计事件（session_events、event_class=audit）；存在任一非终态 LLM effect → 稳定拒 `COMPACT_BUSY_EFFECTS`（零控制态）；receipt 稳定结果=compaction identity；同 command_id 幂等。D3 `compact_finalize`（locked→finalized 唯一路径）：fenced 提交（owner+fence CAS）；同事务写 `compaction/end` 审计事件（payload 与 result_identity 列值同事务写入）；两阶段生成序 semantic payload → digest → event_key；MUST NOT 产第二次结果事件/第二行；仅改 payload 自引用字段值 → `compaction_result_digest@v1` 重算不变。D4 `compact_abort`（locked→aborted 唯一路径）：fenced CAS、释放 lock 回 idle、MUST NOT 产结果事件；写 `internal_op_audits` 的 `compact_terminal_abort` 行（parent_command_id=自身 command_id）、abort_identity=该行 event_key；重放按三元组 (parent_session_id,parent_command_id,internal_op_ordinal) 定位返回。D5 七步定位序（冻结、MUST NOT 合并/跳过/互换）：(1) 授权调用上下文与 session 归属校验（最高优先、`GRANT_DENIED`、receipt 命名空间外、独立授权拒绝审计行、同上下文重发幂等合并）；(2) 请求键计算（canonical/transport 畸形/payload 不可规范化三分流）；(3) 原 receipt 查重命中即原样返回；(4) binding 冲突→`IDEMPOTENCY_CONFLICT`（不覆盖首次 binding）；(5) 统一前置分流（(a) 拒绝键路径不进业务段、(b) canonical hash 不符→`REQUEST_HASH_MISMATCH`）；(6) 业务判定与执行/终态返回；(7) receipt/binding 写入（首占语义）。D6 终态 replay 判定序五步（仅作用于未命中 receipt 的新请求，与 receipt 幂等前置分支显式拆分）：授权/归属 → lease 要求（terminal session 终态历史行重放为只读 replay 允许无协调 lease；非 terminal MUST 持有效协调 lease）→ compact owner fence stale（`STALE_COMPACT_FENCE` 优先于参数冲突）→ 参数一致性（base_seq/through_seq/payload）→ 完全一致只读返回原 identity、控制态零修改；任一不一致→`IDEMPOTENCY_CONFLICT`。D7 终态事务同受控 abort（内部子操作，非独立命令）：session 进入任一 terminal 状态的事务 MUST 同事务 abort 该 session 仍处 locked 的 compact lock（fenced abort 推进 owner_fence、lock 落 aborted 并同事务写 abort_identity、控制态回 idle、MUST NOT 产结果事件、不另立 command_id/receipt）；此后旧 owner 的 finalize/abort MUST stale-reject `STALE_COMPACT_FENCE`；三入口（request_cancel 无活跃工作分支 / `v_finish_session` / WORKSPACE_LOST fail-closed）各自终态化均触发。D8 `internal_op_audits` 表消费+统一内部子操作审计合同（`compact_terminal_abort` 类接入；**表 DDL 由 G16-D14 创建、本 gate 只消费写入、MUST NOT 重复创建**——G16 加载序先于 G17）：表列与两 UNIQUE/CHECK 冻结面与 G16-D14 逐字一致（parent_session_id/parent_command_id/internal_op_ordinal CHECK>=0/internal_op_kind 五值闭合/source_operation/target_identity/event_key/parent_receipt_ref）；event_key 五元组派生、ordinal 五类全序 × 同类目标字节序自 0 单调、写入后不可变、固定调用顺序 `compact_terminal_abort` 居末；跨 session 同 command_id 三元组隔离；同事务多类子操作按五类全序落盘、乱序物理插入下重放序唯一。D9 八位主锁序第 8 位（compact 锁行，末位）接入：compact 三命令先锁 session 行再锁 compact 行；终态事务在既有锁集（含 drain 触及 turn_end_slot 行）之后叠加 compact 行锁、不产生前位逆序；MUST NOT 与 turn_end_slot 槽位位互换；显式锁序位探针（矩阵 #6 余）。D10 compact × LLM effect 并发冲突矩阵（闭合，先提交者赢）：(1) compact_lock × 非终态 LLM effect → `COMPACT_BUSY_EFFECTS`；(2) 创建/seal LLM effect × active compact lock → `COMPACT_IN_PROGRESS`（接入两条 seal 路径 `v_prepare_step`/`v_seal_batch` + 派发门 `v_dispatch_effect`）；**加载序守卫（本 gate 强制）**：上述三函数位于 `v8_effect.sql`/`v8_tools.sql`（加载序第 effect/tools 位），而 `compactions` 表与 compact SQL 在加载序**末尾追加**（见「SQL 加载序」段：compact 无中插）——每个低阶 stage 库（effect=9/tools=10/loop=9/retry=13/repair=14/cancel=15/gates=15/compat=18）**均无 `compactions` 表**；故三函数内每处 compact 归属检查 MUST 以 A51 先例 `to_regclass('compactions')` 守卫（缺表即跳过 compact 检查、不 `RAISE` `relation "compactions" does not exist`）或等价 `to_regclass` 门控早返回，使全部低阶 stage 库照常加载、gate 不因未定义关系变红；本计划不采「把 compact SQL 前移到 effect/tools 之前」的加载序重排替代方案；两侧经同一 session 行锁串行化、线性化点=提交序；fenced finalize/abort 的 lease 到期后新 owner 经 recovery lease CAS 接管（单调推进 fence），旧 owner MUST stale-reject `STALE_COMPACT_FENCE`；三状态门在 seal 两路径与 dispatch 门各至少一向量。D11 五个具名字节级算法（compact_finalize 事务内）+golden：(a) `compaction_result_digest@v1`=SHA-256(排除 {result_identity,event_key,abort_identity} 视图 payload canonical bytes)；(b) canonical record 编码（七字段长度定界）；(c) digest 输入层（normalize 截断至 seq<=through_seq 的 semantic 类输出，含 S03 边界）；(d) `logical_cutoff_digest`（六键完整全序 record 顺序拼接 SHA-256）；(e) `replacement_set_digest`（record 整体字节序 multiset 编码 SHA-256）；(f) `compact_result_identity@v1`=SHA-256("v8:compact-result@v1" ‖ NUL ‖ len8‖session_id ‖ raw32(logical_cutoff_digest) ‖ raw32(replacement_set_digest))；跨运行时字节级 golden（完整全序三形态、provisional end 投影三例、截断边界三位置）、identity 排除断言 (O02)、record 类型保真负向 (O03)、observational 隔离 (N03)、SQL==Python 交叉核对。D12 compact semantic 结果表示接入 normalize（projection 层）：自 finalize 记录提取 {logical_cutoff_digest, compact_result_identity@v1, replacement_set_digest} 三字段参与 semantic 比较；MUST NOT 自事件流重算/猜测/改写；compact 无独立置换输出 artifact（前缀模型）。D13 Python 客户端+gate+门表登记：`v8/compact/client.py`（沿 v8/effect/client.py 冻结命令约定）、`setup_db.py`（`agent_v8_compact`+`seed_stage_grants` 含 compact capability）、`test_compact.py`、README 门表登记；G1–G13 全部门零回归（本里程碑原地改 effect/tools/cancel 文件）。**D14 pre-dispatch 双实现单一化收口（A68/A73 消解，本 gate 交付）**：compact-driver 收口——将 grant 窄版 `v_predispatch_cancel_sync`（WORKSPACE_LOST drain 消费）与 plugin 六参版 `v_pre_dispatch_cancel_sync`（`v_request_cancel` 与 §4 generation drain 消费）统一到富实现（六参缺省版），删除窄版、三调用点全部改指富实现；行为等价断言（单一化前后三触发源三层结果/code 逐字一致）＋断言库内仅剩一份 pre-dispatch 定义；G12 既有断言零回归。 | 否（跨里程碑：拒绝键路径 step 2/5a 依赖 G16 三键指纹——Conformance 6 (v)(ii) 子向量随 G16；`internal_op_audits` 表+五类全序 ordinal 为 G15/G16/G17 共用件，本计划裁定归 G16 创建、G17 消费） |
| G18 | `v8/reconcile/test_reconcile.py`（新 stage） | `agent_v8_reconcile` | **driver 切换正向 + fork 完整面**（矩阵 #11 正向半边 + fork 完整面；台账 A37 余腿、A83 消解）。D1 新增 `session_switch_intents` 表（切换 intent 唯一持久化）：唯一 switch_identity + target_driver + create-time CAS 快照；每 session 至多一条；finish_switch 同事务清空；sessions 15 列闭合清单不增列。D2 `v_reconcile` 统一命令入口（受权、使用当前 recovery lease、expected driver/epoch/mode/fence CAS），分派 begin_switch/finish_switch；envelope 不符→`DRIVER_EPOCH_STALE`（零控制态）、缺有效 recovery lease 拒、同 command_id 重放幂等、未知 mode/action 稳定拒。D3 begin_switch 四款安全点 guard（`SWITCH_DEFERRED`）：(i) 无未密封 tools plan；(ii) 无 stage=decision in-flight effect；(iii) 无任何未 dispatch 的 `ready` effect（含已密封未派发）；(iv) 无非终态且可能经 completion/repair 产 tools plan 的 decision step（含 `blocked_unknown_effect` 的 stage=decision step）；任一不满足→稳定 `SWITCH_DEFERRED` 且不改 mode/fence/switch intent、不撤 lease；逐款清除条件后重发→成功进入 quiescing。D4 begin_switch 成功路径同事务副作用：写 switch_identity+target_driver、`session_fence+1`、撤当前协调 lease；MUST NOT 自动撤销仍有效 job lease；driver_mode → quiescing。D5 quiescing 拒绝与例外闭合集：create_step/prepare_step/create_effect/seal_batch/retry_effect/dispatch_effect/普通 claim/语义输入 append/timer/wait/sleep/finish/fail 与**终局** complete_effect → `DRIVER_QUIESCING`（guard 在业务 mutation 前锁 session）；观测例外照常接受：assistant/chunk、session/heartbeat（含旧 epoch，记录不匹配不拒）、attempt/heartbeat、stream_progress（走五款状态门）。D6 A37 余腿：quiescing 下 recovery takeover 的 known_failure 处置由结构拒 `DRIVER_QUIESCING` 改为受控边收束（复用 `shared_cancel_closure` / `failed_retryable→failed_terminal`）；三层结果与 code 与 (α) completion 入口全等；attempt 计数不增；无新 ready attempt。D7 finish_switch CAS 屏障+一次性生效：无旧 epoch 非终态 effect、无 unresolved unknown、无有效旧 job lease（或已推进至必被 stale-reject）、`active_step_id` 空或指终态 step、无未密封计划；随后写 target_driver、`driver_epoch+1`、`session_fence+1`、清 lease/switch intent、保留 sticky cancel/failure 与既有 state、MUST NOT 无条件置 ready；含 terminal session 矩阵 (d) 变体（只改 mode/driver/driver_epoch/ownership/session_fence）。D8 切换完成后旧 epoch 写入 stale-reject：所有 claim/append/completion 携带 (session_id,driver,driver_epoch,fence)，`driver_epoch+1` 后旧 epoch 写入稳定拒；历史 receipt 读取不受影响。D9 fork 元数据持久化：child session 记录 parent_session_id/parent_through_seq/fork_depth/manifest version；inherited_event_count 仅作缓存；跨 session source 持久化 {session_id, seq}。D10 fork 多 driver/epoch 继承矩阵+切换联动：child 恒 `driver_epoch=1`/`session_fence=1`/`driver_mode=active`，不继承 lease/fence/switch intent/cancellation latch/compact lock/job；child driver = p_driver 或 parent.driver；父处 quiescing 或 terminal 时 fork 仍允许、MUST NOT 读改父 switch intent、父控制态零变化。D11 Conformance 11 (a)(b)(c) gate（真实命令非孪生）：(a) fork 三断言；(b) `SWITCH_DEFERRED` 四款 guard + quiescing 收束 + finish_switch 屏障 + `driver_epoch+1`；(c) unsupported → 稳定 `UNSUPPORTED`、mode/fence/switch intent 不变、幂等重放；按 §5.2 两维矩阵产出 fixture/subcase × 数据库层/真实 I/O 层分列报告。D12 compat 侧 capability 路由收敛（A83 消解）：supported driver 的 `v_compat_begin_switch` 路由进共享 reconcile 实现、删除 stage-local `SWITCH_PROTOCOL_NOT_IMPLEMENTED`；unsupported 路径保持原样；不得存在第二套 switch guard 实现。D13 §4 generation drain 接口：reconcile 事务遵守八位主锁序；裁定并断言 reconcile 的 `ISOLATION_UNSUPPORTED` 适用性；下线 session + 既存 switch intent 的 finish_switch 可达（矩阵 (d)）；与下线事务全序预锁无锁序环。D14 新 stage `v8/reconcile/` 与 gate 落点 + 全量 17 既有 gate 零回归。 | 数据库层不阻塞；真实 I/O 层子用例（Conformance 11(b) in-flight 真实 I/O 层、需 `dispatch_interception=sync_before_io`）被 pinned DSH host 阻塞——§5.2 两维矩阵：数据库层照常、真实 I/O 层 blocked 不得记绿。规格约束：sessions 15 列闭合清单冻结 → switch intent 必须落伴随表而非 sessions 新列 |
| G19 | `v8/assemble/test_assemble.py` + `v8/closeout/test_closeout.py`（新 stage ×2） | `agent_v8_assemble` / `agent_v8_closeout` | **收尾杂项 + v10 次级依赖**（seam 清单/两阶段 manifest 先做；矩阵 #1/#2/#3/#9/#10/#12/#13/#14/#15）。**assemble 段（v10 次级依赖，前）**：D1 seam 六处同事务有效 grant 检查点（recall/fold/env_read/env_write/tool_resolve/authorize_effect，复用 G10 `v_grant_lock_judge`）——失败 `GRANT_DENIED`、授权前置、零副作用、不进 `dispatch_started`；每 seam ≥1 负向（无 grant/已撤销/slice 已撤销）。D2 `tool_resolve` 同事务授权 + 可见工具集 == manifest 快照冻结集；快照外工具不出现；≥1 负向。D3 `authorize_effect` 按具体参数再授权（`v_gate_params` target/path/bytes）；参数越界→`GRANT_DENIED`；≥1 负向+1 正向。D4 seam 并发：撤销 × seam 检查点两提交序（授权线性化点机制 (a) seam 半边）、锁序与 G12 八位主锁序一致（无死锁序回归）。D5 两阶段 manifest 快照：fold/recall/catalog/grant/policy 五源绑定同一 manifest hash、inject 固定 `assembly_cutoff_seq`；cutoff 之前本轮可见、之后留待下一次 assemble（Conformance 7）；dispatch 前撤销使 dispatch 返 `GRANT_DENIED`（快照不豁免，本段只补快照生成侧）。**`INFRA_ASSEMBLY_FAILED` 真实触发点（本 gate 交付，落 D5 的失败路径）**：manifest assembly 五源任一绑定失败（hash 不一致/缺源/cutoff 快照不完整）→ MUST 同事务调用 G15 的 `fail_session` 第 (3) 类 INFRA 闭包（`INFRA_ASSEMBLY_FAILED`），session `failed`/`INFRA_ASSEMBLY_FAILED` 与 G15 D1/D2/D3 契约逐字一致——消解 G15-D12(c)「assemble 真实 caller 标注为 G19」的悬空（本处为唯一真实 assemble 触发点）。D6 manifest hash golden（SQL==Python ≥3 组手算、换序敏感+UTF-8/转义）。**closeout 段（v8 收尾杂项，后）**：D7 NOTIFY/队列唤醒层——`transition_wait(fence,predicate,observed_version)` 同事务复检 + NOTIFY 仅作 hint + DB 扫描恢复；wait 与 completion 并发不丢唤醒；NOTIFY 丢失/重复/乱序三种扰动后最终状态一致；禁 NOTIFY 后仍收敛。D8 attempt/heartbeat 双拆分（O01 受保护观测写入路径 + 生命周期/授权门）：四款校验（attempt 归属/未终态/当前 job lease owner fence 匹配（quiescing 与 failure-drain 旧 epoch 例外、audit 记 epoch）/superseded 或 lease 失效固定 `rejected_stale`）；session 生命周期门（terminal × attempt 未终态照常、attempt 已终态拒）；公开 append 越权→`EVENT_TYPE_RESTRICTED`；零控制态；heartbeat 的 failure-drain 例外 MUST NOT 外溢到 stream_progress。D9 `FORCE_JOB_TAKEOVER` 受控内部操作**五 outcome**（判定序 隔离级→目标→stale→前置→执行）：(1) 成功（CAS 推进 `current_job_fence`、撤旧 job lease、旧 attempt 取代标记、条件分配；receipt/audit 记 operator identity + force reason ≤4096B；**成功路径锁集 = §3.2.2 第 1 步完整八位预锁集**——session→grant/slice→generation→step→effect→attempt，触及槽位时 `turn_end_slot` 位后置，与 G14 槽位/锁序探针共享同一锁序实现）；(2) 目标不存在/已终态→`rejected_mismatch`；(3) expected fence/lease owner 不符→`rejected_stale`；(4) 前置不满足可普通接管→`rejected_mismatch`+`FORCE_NOT_REQUIRED`；**(5) 非 READ COMMITTED 隔离级别入口 → `ISOLATION_UNSUPPORTED`、零锁零控制态，并加 READ COMMITTED 正向对照**（Conformance 8 (x) 明写「recovery 接管与 FORCE_JOB_TAKEOVER 事务同款入口拒绝（各至少一用例）」——A97 三入口闭合不含 FORCE_JOB_TAKEOVER，本项补齐该第四入口；REPEATABLE READ/SERIALIZABLE 稳定拒、READ COMMITTED 照常执行）；五类均零控制态（除成功）；同 command_id 幂等；普通 recovery 携带 force 语义被授权校验层拒。D10 `reconcile` 入口接线（结果接收子操作，与 complete_effect 共用两层验证+流事实四步序）+`OBSERVATION_WRONG_ENTRY` 固定拒绝：遇字段矩阵 (iii)/(iv)→MUST NOT 终局结算、固定拒 `OBSERVATION_WRONG_ENTRY`；quiescing 下 reconcile 结果接收不套 `DRIVER_QUIESCING`（唯二合法终局结算入口）；非 (iii)/(iv) 终局形态照常结算。D11 槽位 create-when-absent（偏差 A41 消解）：cancel 收束/repair closer/已知终结 end 三条路径在槽位行不存在时同事务按 `turn_end_key@v1` 派生创建（不接受调用者指定）；初始行 `slot_status='provisional'`/`head_event_key`=provisional end event_key/`resolution_identity_canonical=NULL`/`version=1`；并发双写经 `UNIQUE(session_id,turn_id)` 收敛；repair 处槽位缺失不再防御性拒 `REPAIR_TARGET_INVALID`；受保护槽位更新函数仍为唯一写入路径。D12 跨 attempt 残留清理：被取代 attempt 的 chunk 不参与文本合并（仅 observational 留存）；合并/收齐只取终局/当前 attempt；stream_completions 跨 attempt 清理；≥1 用例（cohort 重试分配后旧 attempt 残留 chunk 不影响新 attempt 收齐/等值）。D13 真实 lease/超时窗口 sweep：退役事务局部 GUC `v8.stream_window_exhausted`、以真实 lease/超时判定窗口耗尽；窗口内 (iii)/(iv) pending 观测照常接受、耗尽按证据分类；`STREAM_INCOMPLETE` 仅在真实窗口耗尽记录（≥1 正向+1 负向）；控制态零修改。**D14 矩阵 #12 凭据缺失 MUST（DB/宿主无关，本 gate 回归）**：绑定真实 provider 的 capability 在凭据 env（`DEEPSEEK_API_KEY`/`OPENAI_API_KEY`/`OPENAI_API_URI` 等）缺失时 MUST 进入 capability 来源 blocked 清单、**永不触发真实调用**（默认 loop 与既有 gate 零真实调用、退出码仍 0）；回归 A87 的 G13 门控合同（缺省永不构造 `DeepSeekLLM`、缺失即 skip 计 blocked）；#12 第二余项「真实 loop 形态（pinned host）」归「外部阻塞」（见下）。**D15 矩阵 #13 capability API 边界 + Conformance 13 运行期半边**：DB 可断言半边——凭证（provider key/token）MUST NOT 进入 `session_events` payload / jobs payload / 日志载体（逐载体 grep 原值零命中，为 G10 已交付 DB 层半边的运行期复述）；运行期端到端接线余半（真实 loop 的 jobs/日志面）归「外部阻塞」（pinned host）、不得记绿。 | 数据库层不阻塞（G19 全部 DB 层判据纯本地可测）；P0C 真实 I/O 层子用例、跨运行时真实 loop 形态（矩阵 #12 第二余项、#13 运行期半边）blocked（pinned DSH host）——**仅真实 I/O 层 blocked，数据库层不阻塞**。跨里程碑：D10 reconcile 入口落地前 v10 §7.1 接入门证据链不完整（unknown/repair/reconcile 闭环）——assemble+closeout 共同构成 v10 接入前置 |

注：外部阻塞列 = 该 gate 是否被 pinned DSH host / 真实 I/O 阻塞。除 G16（半阻塞：仅真实 transport 接线范围外）、G18（真实 I/O 层子用例 blocked）与 G19（真实 I/O 层 blocked）外，G14/G15/G17 均无外部阻塞；**G19 数据库层不阻塞、仅真实 I/O 层 blocked**（其 D1–D15 的 DB 层判据纯本地可测）。抽取 #6 主张 v10 次级依赖（assemble 段）先行；本计划按编排分组置于 G19，因它与 G14–G18 无写集重叠（新目录 + load.py/README/矩阵/台账四点），后置安全；若 v10 接入在关键路径上，可将其前移。

## 范围对照（六份抽取 → gate 映射）

| 抽取（内部定位） | 归入 | 矩阵缺口 | 台账相关 |
|---|---|---|---|
| 并发竞态与探针补强（DB 层锁等待交错） | G14 | #5 (vectors ii/iii) / #8 (vi)(viii) / #15 (finish×尾 chunk、槽位探针) / **#2（判定源独立性注入 recovery 臂，D2 补测）** | A71（若计数重立） |
| failure-drain（fail_session 第 (3) 类 + DECISION_PLAN_INVALID 收束） | G15 | #5 / #13（drain 运行期半边） | A30 / A37 / A39 / **A40（D4 消解「纯 receipt 拒绝」子句）** / A98 |
| audit 指纹（三键 + 三类拒绝路径 + effect_audit 全列 + occurrences） | G16 | #1 (a)(b)(c)/#10 指纹 golden/#15 接收侧审计 | A11（补全）/A33/A47/B 节 L121 |
| compact 三命令（控制平面） | G17 | #6 / #8（下线事务受控 abort 协同） | A68/A73（收口，**本 gate D14 收口**） |
| driver 切换正向 + fork 完整面 | G18 | #11（正向半边）/ #8（§4 接口） | A37（余腿消解）/A83（消解）/A97 |
| 收尾杂项 + v10 次级依赖（seam 清单/两阶段 manifest） | G19 | #1/#2/#3/#9/#10/**#12（D14 凭据缺失回归）**/#13（D15 运行期半边）/#14/#15 | A41（消解）/A72（消解）/**A87（#12 凭据缺失门控复述）**/B 节 L116–L120 |

「不发明合同外要求」：本表所有条目均可回溯到抽取的 `spec_ref`（v8-dev.md 行号）与矩阵 #1–#16 缺口列；G19 另引用 `docs/designs/v10-dev.md` §3.2/§3.3/§6.4/§7.1 作为 v10 次级依赖的冻结输入。

运行命令（仓库根，全部 `uv run python …`，退出码 0 = 通过）：

```bash
uv run python v8/concurrency/test_concurrency.py  # G14（并发竞态 + 槽位探针 + 声明向量）
uv run python v8/drain/test_drain.py         # G15（fail_session 第 (3) 类 INFRA 收束）
uv run python v8/audit/test_audit.py         # G16（三键指纹 + 三类拒绝路径 + effect_audit 全列）
uv run python v8/compact/test_compact.py     # G17（compact_lock/finalize/abort + 并发矩阵）
uv run python v8/reconcile/test_reconcile.py # G18（begin_switch/finish_switch 正向 + fork 完整面）
uv run python v8/assemble/test_assemble.py   # G19a（seam 检查点 + 两阶段 manifest 快照）
uv run python v8/closeout/test_closeout.py   # G19b（唤醒层 + heartbeat + FORCE_JOB_TAKEOVER + 收尾）
```

既有 gate 回归清单 = `v8/README.md` 运行块 + stage 表。**实况核对（本计划更正）**：运行块现况为 **16 条** `uv run python` 行（G1–G13 但**不含 G9b**），`test_observation.py`（G9b，72 PASS）仅见于 README 散文（L60），运行块与 stage 表均缺——按原「运行块 17 条」实施会漏跑 G9b。**故 G16 与 G19 的 README 收尾工件 MUST 向运行块补入 `uv run python v8/stream/test_observation.py  # G9b` 并补 G9b stage 表行（DB `agent_v8_stream`）**；补入后运行块 = 17 条，Closeout 全量 = 既有 17（含 G9b）+ G14–G19 六 gate = 23 条（G19 拆 G19a/G19b 时为 24 条运行命令）。

## 依赖与顺序

- **串行铁律**：`v8/load.py`、`v8/effect/v8_effect.sql`、`v8/effect/test_effect.py`（G14 D5 声明向量 + G15 G4 断言同步）、`v8/grant/v8_grant.sql`、`v8/events/v8_append.sql`、`v8/canonical/keys.py`、`v8/events/canonicalizer.py`、**`v8/retry/v8_takeover.sql`（G18+G19）**、**`v8/cancel/v8_closure.sql`（G18+G19）**、**`v8/cancel/v8_cancel.sql`（G17+G19）**、**`v8/README.md`（每 gate 收尾）**、**Conformance 矩阵与偏差台账两份 docs（每 gate 收尾）** 被多 gate 共同修改——凡共改同一 SQL/Py/docs 文件的 gate MUST 串行，禁并行分支对同一物理文件双写；`load.py`/README/矩阵/台账按「一行一主 + 分支内就地调和」处理。
- **Wave1（**G14 → G15 串行，不再声称可并行**——二者共改 `v8/effect/test_effect.py`、`v8/load.py`、`v8/README.md`、Conformance 矩阵与偏差台账五处，写集**相交**）**：G14 = 新 `v8/concurrency/`（`test_concurrency.py`/`setup_db.py`/`__init__.py`）+ `v8/effect/test_effect.py`（D5 声明向量）+ `load.py` 一个 `STAGE_THROUGH` 键（`"concurrency"` = 同期 gates 值，G14 先行时 = 13）；G15 = 新 `v8/drain/` + `v8/grant`/`v8/effect`/`v8/retry` SQL + `v8/effect/test_effect.py`（G4 断言同步）/`v8/retry/test_retry.py`（G7a 断言同步）+ `load.py` 插入与下游重编号。G15 插入点（drain，cancel 后）在 G14 复用键之后，但**共享文件的存在使并行不成立**——MUST 先 G14 后 G15。
- **Wave2（单人串行）**：G16 → G17 → G18 → G19，各自在 main 上实施（`effect`/`grant`/`events`/`keys.py`/`canonicalizer.py`/`load.py` 共改 ⇒ 无安全并发）。G17 依赖 G16（三键指纹 step 2/5a、`internal_op_audits` 共用件）；G18 依赖 G16/G17（reconcile 结果接收子操作与 compact 锁序探针协同）；G19 依赖全部门（closeout 核对）。
- **备选并行**（若第二 worker 可用）：G16 ∥ G18 写集共 `load.py` **+ `v8/README.md` + Conformance 矩阵 + 偏差台账**（四件收尾工件强制每 gate 更新 README/矩阵/台账，三者均不在任何 gate 所有权清单内 ⇒ 为共享文件，**并非「仅共 load.py」**）；四点（`load.py`/README/矩阵/台账）按「一行一主 + 分支内就地调和」处理，可并行（G16 = audit 新目录 + keys.py + events/append + effect；G18 = reconcile 新目录 + grant + compat + retry/takeover + cancel/closure + schema；二者不共改任何生产 SQL 文件）。
- **跨里程碑裁定**：`internal_op_audits` 表 + 五类全序 ordinal 为 G15/G16/G17 共用件——G15 只写 `effect_audit`（`internal_op_kind='infra_closure'`，不依赖该表）、G16/G17 共用；本计划裁定归 **G16（audit）创建、G17 消费**（`abort_identity`/`compact_terminal_abort` 行），加载序 G16 < G17（抽取冲突：G17 抽取把它列为 compact 自建——以本计划裁定为准并记台账）。
- **SQL 加载序（相对序，各 gate 分支内就地重编号、Closeout 统一核对）**：schema → keys → **audit（G16，events 前三键判定消费）** → grant → stream → events → **assemble（G19，grant 后 plugin/effect 前）** → plugin → effect → tools → closure → retry → takeover → repair → cancel → **drain（G15，cancel 后）** → **reconcile（G18，drain 后、compat 前）** → compat → **compact（G17，末尾追加、无中插）**。**最终态常量（Closeout 核对）**：新增 audit=3 / assemble=7 / drain=16 / reconcile=17 / compact=19；`gates` **15**、`concurrency` **15**、compat **14→18**。**被 audit（第 3 位）与 assemble（events 后）两处插入推移的文件位次**（两处均在 cancel 前，故 cancel 起 +2）：grant 3→4、stream 4→5、events 5→6、plugin 6→8、effect 7→9、tools 8→10、closure 9→11、retry 10→12、takeover 11→13、repair 12→14、cancel 13→15（drain→16、reconcile→17、compat 14→18、compact=19）。**对应 `STAGE_THROUGH` 键终值**：grant 6 / events 6 / stream 13 / effect 9 / tools 10 / retry 13 / loop 9 / repair 14 / cancel 15 / plugin 15 / **gates 15 / concurrency 15** / compat 18（新增 audit 3 / assemble 7 / drain 16 / reconcile 17 / compact 19）。**MUST NOT 保留 `gates` 13 / `concurrency` 13**——若 gates/concurrency 停在 13，其加载集截断在 cancel 之前（旧 13 = 现 takeover），丢掉 repair+cancel，直接令逐字冻结的 G12（165 PASS）红，正是本段自警的「stale 计数截断加载集直接红」。重编号 MUST 在同一分支提交内就地完成（A55 先例）。
- **文件所有权（只许该 gate 改；未列既有文件一律不动）**：
  - G14：新 `v8/concurrency/`（`test_concurrency.py`/`setup_db.py`/`__init__.py`；DB=`agent_v8_concurrency`、STAGE=`concurrency`，与其它 stage 同构）、`v8/effect/test_effect.py`（**仅 D5 声明向量块**——`DECISION_PLAN_INVALID` 拒绝块归 G15）、`v8/load.py`（`"concurrency"` 一键）、`v8/README.md`、Conformance 矩阵、偏差台账（收尾工件）；**不改任何生产 SQL**；**MUST NOT 触碰/改写 `v8/gates/setup_db.py` 或 `v8/gates/test_gates.py`**（G12 逐字冻结、`test_gates.py` 冻结 import `v8.gates.setup_db`）。
  - G15：新 `v8/drain/`（`v8_drain.sql`/`setup_db.py`/`test_drain.py`/`__init__.py`）；`v8/grant/v8_grant.sql`（`v_workspace_fail_closed` 重构为参数化共享核心）、`v8/effect/v8_effect.sql`（四拒绝点）、`v8/retry/v8_retry.sql`（`v_retry_stop_reason_cas`）、**`v8/effect/test_effect.py`（`DECISION_PLAN_INVALID` 拒绝块 G4 断言同步，自 G14 迁入所有权）**、**`v8/retry/test_retry.py`（`v_retry_stop_reason_cas` G7a 断言同步——该文件此前不在任何 gate 所有权清单内）**、`v8/README.md`、Conformance 矩阵、偏差台账、`v8/load.py`（插入 + 重编号）；**回归只读** `v8/cancel`/`v8/plugin`/`v8/repair`/`v8/schema`。
  - G16：新 `v8/audit/`（`v8_audit.sql`/`test_audit.py`/`setup_db.py`/`__init__.py`；**`v8_audit.sql` 内含 `internal_op_audits` DDL，见 D14**）；`v8/canonical/keys.py`、`v8/canonical/test_canonical.py`、`v8/events/v8_append.sql`（receipt/binding 判定扩三键）、`v8/effect/v8_effect.sql`（全列 audit 写入）、`v8/load.py`、`v8/README.md`、Conformance 矩阵、偏差台账；**只读** `v8/stream`/`v8/grant`。
  - G17：新 `v8/compact/`（`v8_compact.sql`/`client.py`/`setup_db.py`/`test_compact.py`）；`v8/effect/v8_effect.sql`、`v8/tools/v8_tools.sql`、`v8/cancel/v8_cancel.sql`、`v8/grant/v8_grant.sql`、`v8/events/v8_append.sql`、`v8/events/canonicalizer.py`、`v8/canonical/keys.py`、`v8/load.py`、`v8/README.md`、Conformance 矩阵、偏差台账。**约束**：`v8_compact.sql` MUST NOT 创建 `internal_op_audits`（归 G16-D14，本 gate 只消费）；D14 单一化须删除 `v8/grant/v8_grant.sql` 的窄版 `v_predispatch_cancel_sync` 并改 WORKSPACE_LOST drain 调用点指向富实现。
  - G18：新 `v8/reconcile/`（`v8_reconcile.sql`/`setup_db.py`/`test_reconcile.py`/`fixtures.py`）；reconcile stage 新 SQL 文件 `v8/reconcile/v8_reconcile.sql` 内（`session_switch_intents`；**MUST NOT 改 `v8/schema/v8_schema.sql`**——G2 冻结 DDL，照 G16/G17 建表先例）、`v8/grant/v8_grant.sql`（`v_fork_session` 扩展）、`v8/compat/v8_compat.sql`+`v8/compat/test_compat.py`（A83 收敛）、`v8/retry/v8_takeover.sql`（A37 余腿）、`v8/cancel/v8_closure.sql`（被复用）、`v8/load.py`、`v8/gates/test_gates.py`（G12 回归断言同步）、`v8/README.md`。
  - G19：新 `v8/assemble/`、`v8/closeout/`；`v8/loop/runtime.py`、`v8/stream/v8_stream.sql`、`v8/retry/v8_takeover.sql`、`v8/repair/v8_repair.sql`、`v8/cancel/v8_cancel.sql`+`v8/cancel/v8_closure.sql`、`v8/events/v8_append.sql`、`v8/stream/observation.py`、`v8/events/canonicalizer.py`、`v8/load.py`、`v8/README.md`。

## 收尾工件与提交规则（每 gate）

四件收尾工件（AGENTS.md 硬性前置）：(1) `v8/load.py` 注册（新 SQL 的 `SQL_LOAD_ORDER` 条目 + `STAGE_THROUGH` 重编号，终值见「SQL 加载序」段）；(2) Conformance 矩阵更新（表头 PASS 计数 + 缺口行 **`#1/#2/#3/#5/#6/#8/#9/#10/#11/#12/#13/#14/#15/#16`**——#12 此前遗漏于此清单）；(3) 偏差台账更新（A 节新条目或消解注记 + B 节划线——相关：**A30/A37/A39/A40/A41/A68/A72/A83/A87/A97/A98**）；(4) `v8/README.md`（stage 表行 + 运行命令 + 已知边界句 L56–63；**G16/G19 MUST 向运行块补入 `uv run python v8/stream/test_observation.py  # G9b` 及 G9b stage 表行，见「依赖与顺序」末段实况核对**）。

提交规则：一 gate 一提交；提交前本 gate + 直接受影响既有 gate 实跑全绿（Closeout 全量 23 条）；`git add` 显式路径逐项（MUST NOT `-A`/`.`）；消息格式仿 git log 先例 `<版本>: <祈使句摘要>（GXX …）`（如 `v8: add failure-drain INFRA closure and DECISION_PLAN_INVALID settlement (G15 drain stage)`）；尾行 `Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>`；提交后 `git push origin main`（禁 force/force-with-lease；被拒先 fetch 看差异 + `pull --rebase`，无法安全合并停下问用户）。顺序不可颠倒：测试全绿 → 工件更新 → add → commit → push。

## 外部阻塞与需裁定项

**外部阻塞（P0C 真实 I/O / pinned DSH Node host，用户决策）**——解锁链 = §5.2 前置验证本体 → 全部真实 I/O 层子用例 → P0C 最小 turn 双运行时同 observable trace → pinned 五件套实证 → compat-only T0–T4 参加面实测 → P0C 终签（链见 `v8/compat/pinned_host_manifest.json`，各项 null+note）。本计划触及面：

- G16（半阻塞）：v8 无真实 transport/ingress host，「原始报文/帧字节」无落点——路径 (a)/(b) 以 DB 侧、raw bytes 入参的可注入 ingress 拒绝函数落地（G13 先例），真实接线划为范围外。
- G18（真实 I/O 层子用例 blocked）：Conformance 11(b) in-flight 收束的**真实 I/O 层**子用例（需 `dispatch_interception=sync_before_io`）——§5.2 两维矩阵数据库层照常、真实 I/O 层记 blocked，**不得记绿**。
- G19（真实 I/O 层 blocked）：跨运行时真实 loop 形态的 assemble manifest 复现、P0C 终签。
- **这些不阻塞各 gate 的数据库层半边**（G14/G15/G16/G17/G18/G19 的 DB 层判据照常验收；**G19 数据库层不阻塞、仅真实 I/O 层 blocked**）——凡 blocking 项严格限制在「真实 I/O 层」维度，不得越界到数据库层判据。

**需用户/计划裁定（非外部，逐条裁定后方可冻结）**：

- ① G14-D4 槽位锁字面合规——(a) 在 complete_effect/request_cancel/FORCE_JOB_TAKEOVER 按位序补取槽位 FOR UPDATE，还是 (b) 记 A 系偏差（session 行锁串行化同会话全部命令 + `UNIQUE(session_id,turn_id)` 收敛、锁位冗余）；探针须先产出实测取得集再裁定。
- ② G14 落点——本计划默认 Option B 新 `test_concurrency.py` 保 G12 165 PASS 冻结；备选追加进 `test_gates.py` + A71 计数重立。
- ③ 规格「fake 工具调用次数」到 DB 层的翻译——本计划统一翻译为「无新增 `effect_attempts` 行 / 无新 `dispatch_started`」，不引入 runtime 依赖（与 DB 层 gate 定位一致）。
- ④ G14-D2 的 (ix-a)「无死锁环」无条件断言是否需真实并发交错——默认 G11 GUC seam + G14 竞态向量间接覆盖。
- ⑤ G18 reconcile 是否受 §4 `ISOLATION_UNSUPPORTED` 约束——倾向不适用（非「锁后重扫描协议」），须显式裁定并 gate 固化。
- ⑥ G18 fork_depth 基值、manifest version 语义、fork driver 准入、child `active_catalog_generation` 继承。
- ⑦ G19 `FORCE_JOB_TAKEOVER` operator 授权基础——默认复用 G10 operator 通道、不新增 subject_kind。
- ⑧ G19 真实窗口 sweep 扫描器归属——DB 定时扫描 vs runtime scan。
- ⑨ G16 `effect_audit` 全列落地方式——默认经新 audit SQL ALTER（A8/A55/A90 先例），不扰动 G2 冻结 DDL。
- ⑩ G16 `transport_rejection_key` 的帧/报文粒度——待 ingress seam 定义对齐后冻结。
- ⑪ `internal_op_audits` 归属（见「依赖与顺序」跨里程碑裁定）——本计划裁定归 G16 创建、G17 消费，若评审有异议提请复裁。

## 准出判据

1. G14–G19 六个 gate 实跑退出码 0（各自 `uv run python v8/<dir>/test_<name>.py`，G19 拆 G19a/G19b 时两条）；
2. 既有 17 gate 全部实跑退出码 0；断言计数变化（G12 若追加用例、迁移所致）MUST 记台账并按 A48/A71 重立基线——"2965 总数不降"非硬判据（与迁移指令互斥）；
3. 四件收尾工件随每 gate 更新且 Closeout 核对通过（矩阵表头计数、台账划线与消解注记、README 边界句、`load.py` 重编号）；
4. 每 gate 一提交、已 push origin main。

## 明确不做 / 风险

- **明确不做**（他里程碑/外部）：P0C 真实 I/O 半边（pinned host，见上）；compact/fork 的运行时端到端（本计划交付 DB 合同面）；v10 §3.3 Plan/Bind 七步（属 v10 P0——G19 只交付 v8 §2.1/§3.3 seam 清单 + 两阶段 manifest 快照作为其前置）；§3.3 assemble 面七步本体（G19 只交付 seam 检查点 + 快照，七步运行时归 v10）。
- **范围切分纪律**：G14 独立成 gate（不塞进 G12）以保 G12 165 PASS 冻结；G19 若 closeout 实测体量超预算，按其自身建议拆 G19a（assemble，v10 次级依赖）/ G19b（唤醒层 + heartbeat ∥ 接管 + reconcile 入口 + 槽位 + 流式收尾）——两段合同互不重叠、可独立验收。
- **风险 1（共改热点的合并序）**：Wave2 四 gate 共改 `effect`/`grant`/`events`/`keys.py`/`canonicalizer.py`/`load.py`——单人串行 + 全量回归兜底；`load.py` 重编号、README 边界句、矩阵缺口行、台账段落为共享热点，按「一行一主 + 分支内就地重编号 + 合并提交内调和」处理（`v8-p1-grant-generation-p0c-plan` 共享热点段先例）。
- **风险 2（锁序耦合）**：compact 第 8 位、槽位第 7 位、grant/slice 位要求 G14/G17/G18 共享同一锁序实现，任一私改即死锁回归（G12 已冻结八位主锁序）。
- **风险 3（v10 冻结面）**：G16 三键算法一旦落地即被 v10 §6.4 引用为冻结面，算法变更须经版本号提升（§1.3「仅经版本号提升改变」）。
- **风险 4（seam 下沉判定）**：G19 seam 检查点须把「授权检查」下沉到 DB（v10 §3.3「非 Bind 外调」），否则「同事务」不可判定——决定 G19 assemble 段是「新增 SQL 函数 + runtime 调用点替换」而非纯 SQL。
