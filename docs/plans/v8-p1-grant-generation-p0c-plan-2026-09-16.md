# V8 P1 授权与世代域 + P0C compat 计划（G10–G13）· 2026-09-16

> 基线：既有 13 gate **2312 PASS**（G1–G9b，含 G9b `stream/test_observation.py`；合同 2026-09-14 T56 终审冻结）。本计划补全规格剩余三大冻结面：§2.1/§2.2 完整 grant/slice 与 workspace_handles 模型（G10）、§4 插件世代域与下线协议（G11）、seal/dispatch 双实时授权门与 cohort 恒过替换（G12）、§5/P0C dsh-compat 的 host 无关合同面（G13）。实现依据：`docs/designs/v8-dev.md` **原文唯一权威**、`docs/analysis/v8-impl-digest/` 摘要为入口——凡 digest 与原文冲突以原文为准，实现中发现冲突即停下对照原文改判并记偏差台账。

## Gate 总表

| Gate | 目录 / 测试 | 数据库 | 验收内容（合同条款级，MUST 逐条） |
|---|---|---|---|
| G10 | `v8/grant/test_grant.py` | `agent_v8_grant` | §2.1/§2.2 完整模型：G9a stub DDL 自 `v8_stream.sql` §2–§4 摘除迁入 `v8_grant.sql` 并取代为完整模型；有效 grant 判定完整化（constraints 六维度参数级求值各 ≥1 负向：path 前缀外/命令白名单外/目标外/TTL 超/max_bytes 超/max_rows 超 + 合取项 3 三向量：目标资源不属于 slice.spec → GRANT_DENIED；subject 经 grant/driver 链完整解析；slice-membership 按目标资源判定）；受控撤销函数（slice 撤销同事务级联失效其下全部 grant 或等效检查 + p_reason + 同事务 audit 行；撤销后重建 = 新 grant_id；无审计路径不存在/被拒；spec/constraints 创建后 MUST NOT 就地修改）；授权线性化点 = 机制 (a) FOR UPDATE 固定锁序 (workspace_id,slice_id,grant_id) 升序、嵌于 session→grant/slice→generation→step→effect→attempt：撤销先提交 → 其后授权检查 MUST 见 revoked 返 GRANT_DENIED，授权/dispatch 先提交 → 撤销 MUST NOT 追溯取消已 dispatch_started attempt（收束只经 fence/lease 与 completion/repair），两事务交错按锁等待构造提交序断言；RLS（workspace_id 行级隔离 + worker role 无业务表直接 DML 仅 capability function + SECURITY DEFINER 逐函数审计 + 禁 SET ROLE/BYPASSRLS + RLS MUST NOT 替代 slice-membership：同租户异 slice 资源仍 GRANT_DENIED）；operator 通道（grant/slice 签发撤销走受控内部操作 + 原因 + 审计；plugin_specs.required_services 仅目录声明注记；FORCE_JOB_TAKEOVER 未实现——无回归可保持，合同另行排期、见「明确不做」）；workspace_handles 全链（10 字段 + UNIQUE(session_id,run_id)、五状态闭合迁移表（未列出迁移 MUST NOT 执行、lost/discarded 终态无出边）、受控 mutation 三段协议 CAS、fenced publish（stale 被 owner_fence CAS 拒）、yield 前 checkpoint、materialize(checkpoint_digest) 四项拒绝、WORKSPACE_LOST fail-closed 与确定性 failure-drain 三分支（复用 pre-dispatch 双表原子同步子操作——自 `v_request_cancel` 内联块抽出前移至 `v8_grant.sql`、三条取消路径共享单实现，G11 改 cancel 侧调用、G12 回归覆盖；drain 及重复 drain 入口非 READ COMMITTED 稳定拒 ISOLATION_UNSUPPORTED；LOST 后迟到 completion/repair 重跑同一 drain 判定的收敛断言与终态 session recovery claim/repair 例外（偏差 A39 收口）需 effect/repair stage 函数——显式划入 G12 行，见彼处）、fork 只继承 delegable=true 不可变 slice 的 grant 副本（live handle 不继承）+ 最小 fork 命令本体（切点 guard：切点内未解决 unknown → FORK_CUTOFF_UNSTABLE；子会话创建 + 事件前缀继承——含完整 repair closer 的前缀允许创建且子 trace 无 provisional 表示、父已 repair 但切点在前仍拒；Conformance 11 (a) 三断言的实现在 `v8_grant.sql`、验收随 G13 gate——其加载序含 repair/closer 表）、capability API 边界（raw path/DB connection 不出 API、凭证不进 events/jobs/日志）——Conformance 13）；stream registry（stream_id 可归属 effect 绑定 provider/adapter，替换 A60 近似：外来 provider 复用 → CHUNK_ATTRIBUTION_INVALID）；append 双 grant 合取完整化（subject_id-only 近似换完整 subject 解析，三合取缺任一 → CHUNK_ATTRIBUTION_INVALID 零落盘——绑定态全强制；未绑定 effect 且四调用方腿全缺省的 legacy 路径 G10 阶段保留偏差 A57 注记降级以兼容既有 gate、G12 摘除降级并消解 A57）+ session/heartbeat 条目 event_append 授权门（无有效 grant（含已撤销）或跨租户 → GRANT_DENIED：授权前置拒绝——receipt 命名空间外、独立授权拒绝审计行、同上下文重发幂等合并；Conformance 1 (vi) 数据库层半边，attempt/heartbeat 双拆分半边随其实现里程碑）；交付可复用「锁定+判定」子操作函数（G12 全部门消费同一锁序实现） |
| G11 | `v8/plugin/test_plugin.py` | `agent_v8_plugin` | §4 世代域：表族 DDL（plugin_specs/plugin_implementations/generation_members/generations——(identity,contract_version)→specs 绑定 FK 不满足者进不了目录与 generation、唯一键 (identity,contract_version,locus,driver,plugin_version)、全局不可变行 UPDATE/DELETE 触发器稳定拒绝、同键同 digest refresh 复用不可变行仅加成员行、同键异 digest 再发布稳定拒绝、内容变更 MUST 伴 plugin_version 递增、GC 无引用随成员行删且共享实现行 MUST NOT 删）；依赖解析稳定拓扑排序（priority DESC, identity ASC, plugin_version ASC，同键以 driver/implementation_id UTF-8 字节序决出 MUST NOT 依赖 locale；dotted-numeric (major,minor,patch)）+ 依赖环/重复 provide/版本不匹配三类整代拒绝 building→failed；generation digest 覆盖成员全集（SQL==Python 字节级，≥3 组 golden 手算向量）；发布 building→{active,failed}（构建任一环节失败 active 指针不变、已绑定 step/job 不受影响、原子切换无中间态）；steps 增 catalog_generation + 实现绑定列（ALTER 于 v8_plugin.sql + DEFAULT 种子代，历史回填 = 种子代不可区分）；readiness（apply(ctx,config) 进程内幂等纯登记：键 (generation_id,identity,plugin_version,handler_name)、同 digest no-op/异 digest 冲突、无外部 IO；目录 active ≠ handler ready 负向；运行期 handler 故障 MUST NOT 改写 generation 状态）；下线七条（active→failed）：共享 generation 检查子操作交付（供 G12 串联）、active 指针 operator 显式处置 MUST NOT 自动漂移（置空期间 assemble 稳定拒 NO_ACTIVE_GENERATION 零控制态保持 claimed、显式 yield 回 ready）、未派发 drain（ready effect + attempt 同事务 cancelled_before_dispatch/ABORTED_BEFORE_DISPATCH + audit GENERATION_REVOKED，复用/抽取 G8a pre-dispatch 同步子操作，重扫可重复至无残留、迟到 completion 拒）、in-flight 持快照允许完成、step 收束优先既有聚合规则（仅三合取才 failed_terminal/GENERATION_REVOKED、sticky cancel 优先不改写）、readiness 故障区分、下线事务 session 全序预锁 + 锁内重扫 + 发现新关联整体重试（MUST NOT 倒序补锁）+ 非 READ COMMITTED 入口拒 ISOLATION_UNSUPPORTED；retired 不受限负向；Conformance 8 (i)–(v)/(ix)/(x)——(x) 本 gate 限下线事务入口非 READ COMMITTED 拒（recovery 接管入口归 G12、WORKSPACE_LOST drain 入口归 G10、FORCE_JOB_TAKEOVER 未实现记偏差注记）——(vi)/(viii) 竞态随 G12 接线后覆盖；assemble NO_ACTIVE_GENERATION 接线（runtime assemble 段） |
| G12 | `v8/gates/test_gates.py` | `agent_v8_gates` | 双实时授权门 + cohort 恒过替换 + 既有 gate 回归（对既有文件原地修改）：第一道门 = seal 授权阶段真实现于两条 seal 路径（v_prepare_step 初始 decision seal 与 v_seal_batch tools seal——创建任何 step 成员/LLM slot/batch/首个 attempt 之前按具体参数完整实时授权，grant/slice 行锁先于 generation 行锁取得；任一成员失败 → 整个 seal 零副作用稳定拒绝 GRANT_DENIED，仅规定拒绝 receipt[+audit]；assemble 后 seal 前撤销 MUST 拒；同 command_id 重放幂等返回原拒绝；授权恢复后新 command_id 重发方可 seal）；seal 路径 generation 检查紧随授权阶段（串联 G11 子操作共享读；failed → 零副作用拒 GENERATION_REVOKED；retired 照常；无绑定视为非 failed 测试固话）；第二道门 = dispatch 派发门（ready→dispatch_started 状态 UPDATE 前实时重验，manifest 快照不豁免；拒绝只读零控制态 effect 保持 ready；generation 只读不加锁，drain 所有权在下线事务）；两门独立性三序列（MUST NOT 互替、任一通过不豁免另一门；dispatch 通过后撤销不追溯取消已派发 attempt、completion 照常结算）；cohort 授权检查替换 v8_retry.sql 恒过 TODO（步骤 (3)：任一成员 grant/slice 撤销 → 整批不分配、cohort 维持 failed_retryable；普通入口 receipt GRANT_DENIED 且 session 保持 claimed/lease 不隐式释放/fence 不递增、初始 ready 保持 ready；recovery 入口 receipt 整体 accepted + result_canonical 子结果 allocation_denied:GRANT_DENIED、不写 rejected_mismatch、接管与旧 attempt 结算 MUST NOT 回滚；混合入口预锁覆盖断言：recovery 预锁 grant/slice 全集 MUST 覆盖非本次接管成员——GA 并发撤销 → 整批不分配、B 接管照常提交；授权通过对照 superseded_by_attempt_no 同事务写入；授权拒绝不阻塞规则 3/4 收束、grant 有效性 MUST NOT 入 retry_eligible 谓词）；步骤 (4) generation 门 + 拒绝分配后最终聚合（三合取 → 受控边 failed_retryable→failed_terminal 保留原 code + audit RETRY_STOPPED_BY_CLOSURE + retry_stop_reason；未命中维持 failed_retryable；recovery 入口 allocation_denied:GENERATION_REVOKED）；takeover 预锁集补全八位（grant/slice 全集显式含 eligible sibling + generation 共享读先于任何 step/effect/attempt 锁）；seal 冻结 effect→grant 绑定（effect_requests.grant_id，后续门按冻结 grant_id 复验）；各 stage setup seed 可通过 grant 保证正向零回归；GRANT_DENIED/GENERATION_REVOKED 落 outcome=rejected_mismatch 保留具体 code、receipt/audit 同事务提交 MUST NOT 经 rollback 丢失；摘除 `v8_append.sql` 偏差 A57 legacy 降级（未绑定 effect 且四调用方腿全缺省不再放行 → CHUNK_ATTRIBUTION_INVALID；既有 chunk fixture 直插 grants 行 grant-seeded 化，见文件所有权）；WORKSPACE_LOST drain 后迟到 completion/repair 到达重跑同一 drain 判定幂等收敛（Conformance 13 数据库层，drain 实现归 G10、此处库层含 effect/repair 函数可自测）+ 终态 session 的 recovery claim/repair 例外（偏差 A39 收口）+ recovery 接管入口非 READ COMMITTED 稳定拒 ISOLATION_UNSUPPORTED（Conformance 8 (x) 三入口补全：下线事务 G11、drain G10、接管此处）；Conformance 14 数据库层向量族 (i)–(viii) + Conformance 8 (vi)(viii) 竞态 + 全部既有 13 gate 零回归；effect/tools/retry/takeover 四处 scope guard 头注记退役 |
| G13 | `v8/compat/test_compat.py` | `agent_v8_compat` | P0C dsh-compat host 无关面：compat_unmapped_audit 统一命令（envelope 携 adapter identity/fixture digest/未知 DSH 类型 canonical identity/payload hash、统一 receipt/binding；同 command_id 同 payload 幂等恰一条不追加、换 payload 稳定 IDEMPOTENCY_CONFLICT；零控制态修改；MUST NOT 写语义结果类事件；公开 append facade 追加 compat/unmapped 稳定拒 EVENT_TYPE_RESTRICTED）；adapter manifest 落库（dispatch_interception ∈ {sync_before_io, after_io_only, none}（未知 = none）× driver_switch_capability ∈ {supported, unsupported}，创建后不可变；compat-only 声明绑定显式插件/fixture 清单、丢弃不支持事件仍宣称 portable 被拒）；P0C passed/failed/blocked 报告器（fixture/subcase × 测试边界（数据库层/真实 I/O 层分列）× 两维组合，覆盖 Conformance 全部 16 条——已实现子句记 passed/failed、未实现子句（hook 超时、compact、reconcile 入口等）照各矩阵行缺口列记 🟡 并列缺口去向、不冒充全覆盖；blocked 并集规则逐字：sync+supported 全参加 / +unsupported 仅 11 正向 blocked 换强制负向 / after_io_only|none+supported dispatch 维各行 blocked / 两维皆缺取并集；非矩阵来源 blocked、条目缺失、以 Native 绿冒充 portable、真实 I/O 层降级记绿、数据库层通过替代真实 I/O 层、负向因 blocked 免除——六类一律不通过）；§5.1 十六行映射数据库层用例（语义结果类经公开 facade 直写拒、白名单照常、stream_progress 仅经 complete_effect 非终局路径、load() 合成走共享 repair、fiber/isolate/pending plan-mode 不落盘、X03 归属矩阵逐行——X03 用直插 grants 行（stub 兼容面）实现；走 seal/dispatch 路径的用例一律携带 setup seed 的固定命名 grants（Wave1 恒过下可跑、G12 门变真后凭 seed 通过——见 setup_db.py 所有权约定））；真实 provider 认证 DB 协议层（DeepSeek：协议形态/幂等/uncertain window 各 ≥1 绿——凭据环境变量门控：缺失时 skip 并计入 blocked 清单、退出码仍 0、真实调用 MUST 在数据库事务外；偏差注记说明与 AGENTS.md「FakeLLM/FakeTool、不调真实 provider」约定的关系（缺省永不触发真实调用，显式凭据属 capability 来源 blocked）；fake suite 仍权威）；Conformance 11 无依赖子用例（fork 切点 FORK_CUTOFF_UNSTABLE 三断言 + unsupported driver UNSUPPORTED 强制负向，MUST NOT 因 blocked 免除）；pinned host 外部 manifest 工件（五件套 + 外部清单各项 null+note，存在未解析项 MUST NOT 宣称 compat contract 已过）；GUC seam（仿 G9b v8.stream_window_exhausted 先例）仅驱动矩阵逻辑测试 |

注：G10–G13 对应任务抽取 A1/A3/A2/A4；与偏差台账偏差号 A1–A61 是两个命名空间，引用偏差写全称（如「偏差 A56」）。

运行命令（仓库根，全部 `uv run python …`，退出码 0 = 通过）：

```bash
uv run python v8/grant/test_grant.py    # G10（grant/slice 完整模型 + workspace_handles）
uv run python v8/plugin/test_plugin.py  # G11（世代域 + 发布 + 下线七条）
uv run python v8/gates/test_gates.py    # G12（双实时门 + cohort 替换 + 全量回归）
uv run python v8/compat/test_compat.py  # G13（P0C host 无关合同面）
```

既有 13 gate 回归清单 = `v8/README.md` 运行块 12 条 + G9b `v8/stream/test_observation.py`。

## 依赖与并行波次

- **Wave1（三线并行）**：G10 ∥ G11 ∥ G13，git worktree 隔离、各自分支（`v8-g10-grant` / `v8-g11-plugin` / `v8-g13-compat`）各自提交。合并序 = SQL 加载序（G10 → G11 → G13），每次合并回 main 后复跑该 gate 与直接受影响既有 gate 再 push。G12 的全部前置（grant 锁定+判定子操作、generation 检查子操作）在本波交付。
- **Wave2（单人串行）**：G12 在 Wave1 全部合并后的 main 上实施——改动横跨 effect/tools/retry/takeover 与全部 setup seed，不适合并行。
- **Closeout**：17 条命令全量回归（4 新 + 13 既有）+ 收尾工件核对（矩阵表头 PASS 计数、台账 G7b 133/127 计数不一致按实跑统一）+ digest [LATER] 标注统一更新（s2/s31b/s33，归 Closeout 避免 Wave1 共享文件冲突）+ push。

**文件所有权（只许该 gate 改；未列既有文件一律不动）**：

- G10：新建 `v8/grant/`（`__init__.py` / `v8_grant.sql` / `setup_db.py` / `test_grant.py`，operator 受控函数需要时加 `client.py`；pre-dispatch 双表原子同步子操作自 `v8_cancel.sql` 前移入此）；既有仅 `v8/stream/v8_stream.sql`（§2–§4 stub 摘除 + 头部声明改写）、`v8/stream/test_stream.py`（`test_grant_stub` 整块迁入 G10、留薄烟测，迁移时抽出 grant-seeded chunk fixture 共享 helper 供 G12 复用）、`v8/events/v8_append.sql`（grant 查找升级完整 subject 解析 + session/heartbeat 授权门；偏差 A57 降级本 gate 保留、G12 摘除）；`v8/load.py` 第 3 位插入 `grant` 条目 + 插入点之后目标受影响的既有 stage 计数就地 +1（branch-local 期望值见共享热点段）；`v8/README.md` 仅 G10 表行/命令行/L52（该物理行 Wave1 由 G10 独占改写 grant 边界句，G11 的 generation 措辞随其合并提交手工并入）；矩阵 `#9`/`#14` grant 部分（`#9` 缺口列 seam 项保留不划）+ `#2` 行 grant 位（`#2` 整物理行 Wave1 由 G10 独占，见共享热点段）；台账 A62 起本 gate 块 + B 节 L82 仅划已交付四项（授权线性化点/RLS/operator 通道/constraints 参数级求值）、seam 清单段改记新偏差（→ assemble 里程碑）+ 偏差 A56/A60/A61 消解注记 + G9a 断言计数重立注记（`test_grant_stub` 迁移后 `test_stream.py` 新计数表，A48 先例）。
- G11：新建 `v8/plugin/`（`__init__.py` / `v8_plugin.sql` / `setup_db.py` / `client.py` / `test_plugin.py`）；既有仅 `v8/cancel/v8_cancel.sql`（`v_request_cancel` 的 pre-dispatch 同步内联块改为调用前移至 `v8_grant.sql` 的共享子操作、最小 diff——三取消路径同款语义单实现，G12 回归覆盖）、`v8/loop/runtime.py` 仅 assemble 段（L544–575 邻域）与 run_turn claim/yield 段；`v8/load.py` 第 6 位插入 `plugin` 条目 + 插入点之后目标受影响的既有 stage 计数就地 +1（branch-local 期望值见共享热点段）；`v8/README.md` 仅 G11 表行/命令行/L54 generation 相关分句（L52 归 G10 独占，G11 的 generation 措辞随其合并提交手工并入 L52）；矩阵 `#8` + `#2` generation 部分（`#2` 整物理行 Wave1 由 G10 独占，generation 表述随 G11 合并提交手工并入同一行）；台账续块。**不改** `v8_effect/v8_tools/v8_retry/v8_takeover`（世代检查以共享子操作交付，接线归 G12）。
- G13：新建 `v8/compat/`（`__init__.py` / `v8_compat.sql` / `setup_db.py`（seed 固定命名 grants 行——Wave1 恒过下无害，G12 门变真后走 seal/dispatch 路径的用例凭此通过；G13 合并于 G10/G11 之后，grant 模型已在加载序内）/ `client.py` / `test_compat.py` + 报告器模块）；既有仅 `v8/loop/runtime.py` 的 FakeLLM 邻域（L135–231/L750–759 增真实 provider adapter、不动主链）；`v8/load.py` 仅末位追加 `compat` 条目（无中位插入，既有 stage 计数全不动）；`v8/README.md` 仅 G13 表行/命令行/L5 与 P0C 边界句；矩阵 `#16`/`#1`/`#12`；台账续块。**不改** `v8/events/v8_append.sql`（白名单已闭合，compat/unmapped 越权负向在 test_compat.py 断言）。
- G12：`v8/effect/v8_effect.sql`、`v8/tools/v8_tools.sql`、`v8/retry/v8_retry.sql`、`v8/retry/v8_takeover.sql`（门插入/恒过替换/预锁补全/scope guard 退役）；`v8/events/v8_append.sql`（偏差 A57 legacy 降级摘除——未绑定 effect 且四调用方腿全缺省不再放行）；`v8/events/test_events.py` 与 `v8/stream/test_observation.py`（及 `v8/stream/test_stream.py` 留守块若仍依赖该降级）的 chunk fixture 块改 grant-seeded（直插 grants 行绑 effect，或用 G10 迁移时抽出的共享 helper——A57 摘除的配套，缺此两处则 G3/G9b 回归无法修复）；新建 `v8/gates/`（`__init__.py` / `setup_db.py` / `test_gates.py`）；各 stage `setup_db.py` seed 可通过 grant（effect/tools/cancel/repair/retry/stream/loop/plugin/compat 九处——compat 为 G13 固定命名 grants 的补种兜底，G13 在 Wave1 已自行 seed、G12 负责门变真后的全量对账补种）；`v8/load.py` 仅 `gates` 条目；`v8/README.md` G12 行 + L46 加载序描述 + 已知边界全量改写；矩阵 `#2`/`#9`/`#14` 终态 + 表头计数；台账 B 节 L73 划线。
- 共享热点（`load.py` 计数、README L46/L52、矩阵表头行与 `#2` 行、台账编号段）：`load.py` 的 SQL_LOAD_ORDER 插入与 STAGE_THROUGH 重编号 MUST 在同一分支提交内就地完成（A55 先例本就如此）——各分支对本分支插入点之后目标受影响的既有 stage 计数**就地 +1**（仅改数值、不重排条目顺序、不动他 gate 文件），否则该分支被强制复跑的既有 gate 会按 stale 计数截断加载集直接红；合并提交只负责跨分支数值调和与注释/总述改写。branch-local 期望值：G10 分支 events 5/stream 10/effect 6/tools 7/retry 10/loop 6/repair 11/cancel 12，G11 分支 stream 10/effect 6/tools 7/retry 10/loop 6/repair 11/cancel 12（events 4 在插入点前不动），G13 分支仅末位追加、既有计数全不动。「只追加自有条目」仅适用于追加式条目（矩阵行、台账块、README 表行）；同一物理行的共享编辑（矩阵 `#2` 行、README L52）按**一行一主**处理——Wave1 由 G10 独占改写，G11 的 generation 措辞随其合并提交手工并入（禁并行分支对同一物理行双写，防合并时静默丢句）。

## SQL 加载序变更

新 `SQL_LOAD_ORDER`（14 文件，A55 重编号先例）：schema → keys → **`grant/v8_grant.sql`（新第 3 位，G9a stub 取代者：stub DDL 自 `v8_stream.sql` §2–§4 摘除迁入、由完整 §2.1 模型取代；仍 MUST 先于 events——公开 append 路径消费 grant 谓词；pre-dispatch 双表原子同步子操作自 `v8_cancel.sql` 前移入此——三条取消路径共享单实现，仅依赖 schema 表族，函数体晚绑定、无调用序风险）** → `stream/v8_stream.sql`（仅剩流列 + G9b 观测）→ events → **`plugin/v8_plugin.sql`（第 6 位，先于 effect：steps 增列/种子代/世代域与 generation 检查子操作被 effect 及以后阶段消费）** → effect → tools → closure → retry → takeover → repair → cancel → **`compat/v8_compat.sql`（末位，仅依赖 schema/keys/events/grant）**。

`STAGE_THROUGH` 全量重编号（按 14 文件加载序逐位核对）：schema 2 / grant 5（gate 覆盖 append 消费者）/ events 5 / stream 10 / effect 7 / tools 8 / **retry 11**（含 v8_retry+v8_takeover）/ loop 7 / **repair 12**（含 v8_repair）/ cancel 13 / plugin 13 / compat 14；G12 再增 `gates: 13`。retry/repair MUST 按 11/12：照 10/11 写会使 `files_through` 截断在 takeover/repair 之前、G7b/G7c 首个函数调用即 function-not-exist（违反 AGENTS.md「不破坏既有 gate」与判据 2）。stream 语义差异注记：旧值 9 加载集含 takeover、终值 10 不含（`test_stream.py` 无 takeover 依赖，10 成立，勿照旧值语义类推）。stub 摘除后 stream 两 gate（`test_stream.py` / `test_observation.py`）MUST 复跑零回归（表仍在加载序内，搬文件对 gate 透明）。**G12 无新 SQL**——对 `v8_effect/v8_tools/v8_retry/v8_takeover` 既有文件的原地修改，不是新加载项。

## 收尾工件与提交规则（每 gate）

四件收尾工件（AGENTS.md 硬性前置）：(1) `v8/load.py` 注册；(2) Conformance 矩阵更新（表头 PASS 计数 + 行 `#1/#2/#8/#9/#12/#14/#16`）；(3) 偏差台账更新（A 节 A62 起新块、B 节 L73/L82 划线——L82 限已交付四项、seam 清单新记偏差、G9a 断言计数重立（G10）；A56/A60/A61 消解注记（G10）；**A57 消解注记随 G12 摘除 legacy 降级落账**——消解清单不得只列 A56/A60/A61）；(4) `v8/README.md` 更新（stage 表行 + 运行命令 + L5 范围 + 已知边界）。

提交规则：一 gate 一提交；提交前同 stage 全部 gate 实跑全绿（Wave1 分支内 = 本 gate + 直接受影响既有 gate；Wave2/Closeout = 全量 17 条）；`git add` 显式路径逐项（MUST NOT `-A`/`.`）；消息格式仿 git log 先例 `<版本>: <祈使句摘要>（GXX …）`（如 `v8: add full grant/slice model, linearization and workspace handles (G10 grant stage)`），尾行 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`；提交后 `git push origin main`（禁 force/force-with-lease；Wave1 分支先按合并序并回 main 再 push，被拒先 fetch 看差异 + `pull --rebase`，无法安全合并停下问用户）。顺序不可颠倒：测试全绿 → 工件更新 → add → commit → push，任一步失败即停。

## P0C 环境边界（G13）

pinned DSH Node host 未确认：仓库无 package.json/node_modules/vendored SDK；本机 Node v24.13.1 可用——缺的是上游 DSH 包身份/版本/来源，不是 Node 运行时。裁定：G13 以确定性 GUC seam（先例 G9b `v8.stream_window_exhausted`）+ manifest 表行实现合同可测部分；**测试注入 ≠ capability 实证**——dispatch_interception 真实取值只能来自 §5.2 对 pinned 包的前置验证三行表，禁止以 GUC 伪造 sync_before_io 通过真实 I/O 层行。显式 blocked 清单（解锁条件 = 用户/上游确认 pinned DSH 包 → §5.2 前置验证出结果 → manifest 从 null+note 转实证）：① §5.2 前置验证本体；② 全部真实 I/O 层子用例（Conformance 3/5/6/11/13/14/15 真实 loop 形态）；③ P0C 主目标「最小 turn 双运行时同 observable trace」；④ pinned 五件套实证值；⑤ compat-only T0–T4 分级与降级运行时参加面实测；⑥ P0C 报告终签（报告器先出 host 无关部分，真实 I/O 层行挂待实证）；⑦ 真实 provider DB 协议层子用例（凭据环境变量缺失时 skip 计入 blocked、退出码不变 0——capability 来源，见 G13 行）。

## 准出判据

1. G10–G13 四个新 gate 实跑退出码 0；
2. 既有 13 gate 全部实跑退出码 0；断言计数变化（test 迁移/恒过替换所致——G10 迁 `test_grant_stub` 后 G9a、G12 替换恒过后各 gate）MUST 在偏差台账注记并逐 gate 重立基线（新计数表，A48 先例）——「2312 总数不降」不再是硬判据（与迁移指令互斥、不可机械满足）；
3. 四件收尾工件随每 gate 更新且 Closeout 核对通过（矩阵表头计数与 G7b 计数统一、台账划线与消解注记、README 边界句、load.py 重编号）；
4. 每 gate 一提交、已 push origin main。

## 风险与开放问题（五份抽取汇总去重）

**计划内已裁定**（随对应 gate 记偏差 A62 起）：
- 线性化机制选 (a) FOR UPDATE 固定锁序（规格二选一；全库既有行锁串行化 + G7b 八位预锁先例；不做规格未允许的 (a)/(b) 混合）；G10 交付可复用「锁定+判定」子操作，G12 全部门复用同一实现。
- slice 撤销级联 = 锁内等效检查（不落 UPDATE 级联，语义由锁序闭合）。
- constraints 六维度求值 schema 自定（path 前缀 / command 白名单精确 / target 集合 / TTL 与 [not_before,expires_at) 关系 / max_bytes/max_rows 单调用作用域）——A1 最大实现自裁空间，随 G10 评审确认。
- subject 链解析规则自定；`v_grant_valid` 保持三参签名（偏差 A56 冻结面不破坏）+ 新增带 caller 身份与参数的完整判定入口；append 查找不再按 subject_id 裸匹配。
- workspace_handles 全量入 G10（§2.2 属其合同面）；RLS 角色名/policy 谓词自定（测试以 SET LOCAL ROLE 行使）；operator 通道 = 受控函数 + p_reason + audit，不新增 receipt 命令。
- effect 多 grant 选择 = seal 冻结单条（确定性择一），dispatch/cohort 按冻结 grant_id 复验。
- 测试适配 = 各 setup seed 可通过 grant（不做「无 grant 降级放行」——那正是要消灭的恒过形态；append 侧既有偏差 A57 降级为兼容性例外：G10 阶段保留、G12 摘除并消解，seal/dispatch 门自 G12 起无任何降级）。
- steps 增列走 v8_plugin.sql ALTER + DEFAULT 种子代（A8 先例，不动 G2 冻结 DDL）；种子代 bootstrap 直插（发布流程由 G11 gate 独立验收）。
- manifest hash 绑定留 assemble 里程碑，G12 只落「两道实时门 + 快照不豁免」语义；Conformance 14 真实 I/O 形态随 G13 blocked。
- dispatch 门 handler readiness/compact 检查与 dispatch 侧授权前置拒绝（receipt 之前的调用方门）不入 G12（compact 三命令/driver 切换 LATER，注记保留）；session/heartbeat 的 event_append 授权门不在此列——归 G10 交付（见 G10 行），attempt/heartbeat 双拆分半边随其实现里程碑并已入「明确不做」。
- A2/A3 检查点所有权：G11 交付共享 generation 检查子操作与下线事务，G12 接线 seal/dispatch/cohort（两 gate 验收互指）；GRANT_DENIED 后 coordinator 再触发编排不改 runtime，验收以测试直接再调用。

**需用户决策**：
- pinned DSH Node host 身份（哪个包/版本/从何处取得）——解锁 G13 blocked 清单的前置。
- driver 切换正向机制（begin_switch/quiescing/finish_switch）排期：Conformance 11 正向子用例既不能记 blocked（非 capability 来源）也不能缺失，须裁定提升进前置或与 P0C 终签同批。
- G10 constraints 求值 schema 若评审对默认语义有异议，提请用户裁定后再冻结。

**风险**：Wave1 三线对 `load.py`/README/矩阵/台账同点编辑——文件所有权分区 + 合并序 + 分支内就地重编号与合并提交内调和/一行一主（见共享热点段）缓解；G12 改动面横跨四 SQL 文件与全部 setup——Wave2 单人串行 + 全量回归兜底；锁序耦合（grant/slice 位嵌于 session 与 generation 之间）要求三个 gate 共享同一锁序实现，任一私改即死锁回归。

## 明确不做（另行排期）

§3.3 assemble 面七步与 manifest hash 绑定、seam 清单各同事务 grant 检查点（recall/fold/env_read/env_write/tool_resolve/authorize_effect → assemble 里程碑；Conformance 14 首句的 dispatch 半边照测、seam 半边随 assemble，台账 L82 seam 段改记偏差不划线）、compact 三命令、driver epoch 切换正向机制（G13 仅交付 UNSUPPORTED 强制负向）、attempt/heartbeat 授权双拆分半边（attempt 未实现，随其里程碑）、FORCE_JOB_TAKEOVER 合同（未实现——`v8_retry/v8_takeover.sql` §2.3 头注 [LATER]、台账 B 节 L76 余量与矩阵 `#2` 缺口均如实记载；不存在「G7b 既有四 outcome」可保持，其依赖的 grant/§4 随本计划就绪、另行排期，矩阵 `#2` 缺口该条保留不划线）、fork 命令完整面（多 driver/epoch 继承矩阵与切换联动——最小 fork 本体：切点 guard + 子会话创建 + 前缀继承随 G10 交付，Conformance 11 (a) 三断言随 G13 验收，不再整体 deferred）、§4 runtime 完整面（ctx 四方法/hook 元数据/两阶段工具授权/认领并发限制）、compat host 本体与全部真实 I/O 层（见 P0C blocked 清单）、audit 指纹三键与 occurrences 子表。
