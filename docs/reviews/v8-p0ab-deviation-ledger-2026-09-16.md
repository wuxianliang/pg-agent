# V8 P0A核心+P0B 偏差台账 · 2026-09-16

实现各 stage 自报 deviations + 对抗审查裁决的 consolidated 台账（来源：G1–G5 交付报告、对抗审查 20 findings 及修复；G6/G7a/G7b/G7c/G8a/G8b 追加）。规格原文 = `docs/designs/v8-dev.md`。**后续 stage 消除某条偏差时更新此表**。

## A. 裁定保留（已验收的偏差）

| # | 层 | 偏差 | 理由 / 后续 |
|---|---|---|---|
| A1 | G1 | `TIME_FIELD_NAMES` 为 P0B 占位注册表 `{created_at,updated_at,occurred_at,started_at,finished_at,expires_at}`；注册字段非字符串值透传不校验 | 冻结 schema 落地时替换为真实时间类型字段名 |
| A2 | G1 | `normalize_rfc3339` 拒闰秒（`:60`）与无偏移形态；接受小写 t/z 归一大写 | 闰秒为 datetime 表示域限制；裁决不修 |
| A3 | G1 | canonical 管线中 NFC 校验先于结构校验执行（规格步骤序 (1) schema → (2) NFC），仅影响双重非法输入的错误码优先级 | 裁定不修；版本提升时统一 |
| A4 | G1 | 安全整数范围内 `{"$int": n}` 的转义歧义：escaper 对范围内值不转义（作普通数据），canonicalize 收到未转义形态则拒 | 严格读法歧义；裁定不修，golden 已锁行为 |
| A5 | G1 | 错误码划分为自定：恰一键 `$int` 值非法 → `TAGGED_INT_INVALID`；其余未转义 `$` 前缀键 → `UNESCAPED_DOLLAR_KEY` | 规格要求两类拒绝但未冻结 code 名 |
| A6 | G1 | 非整数 IEEE-754 数按 RFC 8785 ES6 序列化（修复 P1 #0 引入）；整数超 ±(2^53-1) 的浮点词法形态拒 `UNSAFE_NUMBER` | 冻结分区「超界整数 MUST tagged」按值域适用 |
| A7 | G2 | 类型选择：identity 列 uuid、fence/epoch/序号 bigint、哈希/码 text、`resolution_identity_canonical` bytea | 规格只冻结列名 |
| A8 | G2 | steps 增列 `decision_only`/`final_tools`/`plan_canonical`（stage 增列模式）；`sealed_batch_no DEFAULT 0` | 规格读清单 B 需要标记列；"at least contains" |
| A9 | G2 | sessions 初态由 BEFORE INSERT 触发器强制（state=ready/fence=1/epoch=1/next_seq=1） | 任务内选定方案 |
| A10 | G2 | `turn_end_slots` 四列保护触发器带 GUC 通道 `set_config('v8.slot_protected_write','on',…)`；`turn_end_key` 无条件不可变 | G7c 的受保护槽位更新函数为正式使用方 |
| A11 | G2 | `effect_audit` 仅基础列（result_parse_path/expected_*/received_* 全列 deferred）；已含五值 internal_op_kind CHECK 与 L4-R04 | completion-验证 stage 补全 |
| A12 | G2 | `superseded_by_attempt_no` write-once（NULL→值一次）；「仅经 retry_cohort_allocation 写入」路径限制留函数层 | G7a 落地 |
| A13 | G4 | `claim/recovery_claim/yield` 返回 jsonb 而非 receipt（lease CAS 是非 receipt 受控操作） | s31a §2.3 依据 |
| A14 | G4 | `v_completion_event_key@db1` 内部派生（绑定四元组 session_id/event_type/(effect_id,attempt_no)/payload_hash，非跨语言断言对象） | s31b §3.7(b) 只冻结绑定域不冻结字节公式 |
| A15 | G4 | turn/end 已知终结的 `resolution_identity_canonical` = `{"interrupted":false}` canonical 字节 | 结构化 resolution 对象随 G7c repair 落地 |
| A16 | G5 | 重投去重简化：turn 已关而 session 未完时，相同文本的重投视为同一 delivery | P0B 简化；不同文本开新 turn |
| A17 | G5 | yield 覆盖以两变体（claim→yield 于 seal 前；step-6 协调 claim 处 yield）替代「seal 后 yield」 | seal 提交即撤协调 lease，严格 seal 后 yield 不可达 |
| A18 | G5 | provider_error/timeout 故障统一走 unknown 收束（无绑定终局回执即 unknown） | P0B 无 retry/deadline 机制 |
| A19 | G4 | steps 选 last step 用 `created_at DESC, step_id DESC`（事务起始时间并列歧义） | P0B 单 decision step 不可达；裁定不修 |
| A20 | G3 | stage-local code 名：`DRIVER_EPOCH_STALE`/`EXPECTED_SEQ_MISMATCH`（rejected_stale）；`ENVELOPE_INVALID`/`EMPTY_BATCH`/`ENVELOPE_FIELD_MISSING`/`ATTRIBUTION_MATRIX_VIOLATION`/`SEMANTIC_ORDINAL_MISSING|FORBIDDEN|OUT_OF_RANGE`（envelope 结构拒绝） | 规格冻结 outcome 类但未冻结这些 code 名 |
| A21 | G6 | `v_seal_batch` 要求 state=claimed + 协调 lease（规格 §2.6.2 CAS 清单列 driver/epoch/fence、active mode、无 sticky cancel，未逐字点名 session 状态前置；按 prepare_step 同款协调写约定 + s32a rule 6「coordinator 可 claim 执行 tools seal」与「聚合离开 claimed 撤 lease 递增 fence」推导） | seal 提交即撤 lease 并 fence+1（与初始 decision seal 同款） |
| A22 | G6 | tools seal receipt 重放定位：按 receipt JSON 内 `batch_id` 反查 `command_receipts`（`receipt_key_kind` 闭合集不允许新增 seal-receipt 键型）；新 command_id 的引用 receipt 增 `replay`/`replay_of_command_id` 标记字段 | decision_result_identity 四元组无专列，由 effect/attempt/session_events 组合承载（G4 现状延续），seal 核验消费该组合 |
| A23 | G6 | `tool/call` 事件键 `v8:seal-event-key@db1` 内部派生（绑定 `(session_id, event_type, batch_id, dispatch_ordinal, payload_hash)`；规格冻结 occurrence identity 为 `(batch_id, dispatch_ordinal)`，附加绑定 payload hash 为实现自选加严，与 `v_nonstream_event_key` 同模式） | s31b §3.7(b) 只冻结绑定域不冻结字节公式（同 A14） |
| A24 | G6 | complete_effect 的 tools 分支按批次 `kind='tools'` 判定（`effect_kind='tool'` 为 seal 冻结值，判定源用批次类型而非 effect_kind 列） | effect 与批次归属在 seal 中一一对应；避免依赖 effect_kind 值域 |
| A25 | G6 | 反序完成时 `tool/result` 的 seq 与 `internal_semantic_ordinal` 分配随完成序 | 规格只冻结 semantic 序（`dispatch_ordinal`）与 observable trace 等价（Conformance 7 以 normalize 比对满足）；两种完成序的 seq 均连续无洞 |
| A26 | G6 | stage-local code 名（G6 新增）：`SEAL_MISMATCH`/`STEP_NOT_SEALABLE`/`SEALED_BATCH_NO_STALE`（rejected_stale）/`DECISION_IDENTITY_MISMATCH`/`PLAN_HASH_MISMATCH`/`DECISION_MARK_INVALID`/`EMPTY_TOOLS_PLAN`/`TOOL_RESULT_MISMATCH` | 同 A20：规格冻结 outcome 类但未冻结这些 code 名 |
| A27 | G7a | 「对应证据已在控制态持久化」载体 = 新表 `effect_evidence`（(effect_id, attempt_no, evidence_class) 三元组、append-only；settlement 事务写入 failure_receipt + no_side_effect_proof 两行，`v_retry_eligible` 读回）；「已完成外部 IO/无副作用」证据经 completion 信封由结算事务同事务持久化（completion 路径即持久化路径） | 任务书允许「effect_audit/evidence 行」；effect_audit 全列 DDL 为 LATER（A11），专用表更直接承载谓词读取 |
| A28 | G7a | `v_retry_eligible` 的证据腿对两个 retry_class 均要求双要件（回执 + 无副作用证明）均已持久化 | known_failure 分类本身即双要件（判定表行 3），到达 failed_retryable 的 effect 构造上必有两件；最严格读法，无假阴性 |
| A29 | G7a | stage-local code 名（G7a 新增）：`EFFECT_NOT_RETRYABLE`/`STEP_NOT_RETRYABLE`/`SESSION_NOT_RETRYABLE`/`SIBLING_UNKNOWN`/`SIBLING_PENDING`（rejected_mismatch）、`ATTEMPT_SUPERSEDED`（rejected_stale + audit） | 同 A20/A26；SIBLING_* 经漂移注入（direct-SQL sibling）可达，公开路径上由 effect/step 状态前置先行拒绝（聚合状态机保证二者同义） |
| A30 | G7a | `retry_stop_reason` CAS 不一致（R-01 违例）以事务 abort 处理（INFRA_PROTOCOL_VIOLATION 异常），非 fail_session 第 (3) 类受控收束 | fail_session INFRA 收束属 failure-drain 里程碑（B 节既有偏差同款）；列保持原值语义由 abort 保证 |
| A31 | G7a | 失败结算不产生语义事件（无 assistant/message、无 turn/end）；failed session 的 turn 无 end 事件 | 规格失败/取消路径的 turn/end 派生归 G8 取消族与 G7c repair closer；receipt + effect_evidence + audit 留痕 |
| A32 | G7a | `v_aggregate_step`/`v_apply_step_aggregation` 为失败域（规则 1/2/4/5 + 规则 6 资格检测）共享判定/落盘函数；complete_effect 的 success/unknown 分支保留原内联聚合（decision 臂未改——decision 批单 effect 构造上不可达失败 sibling；tools 臂以 EXISTS 守卫委托 4/5） | 判定函数在失败域与虚拟/最终聚合两入口唯一；成功臂行为等价不动（G4/G5/G6 gate 冻结）；完全统一随后续里程碑 |
| A33 | G7a | 规则 4 残留 sibling 收束的 audit 行为普通列（internal_op_kind/parent_command_id/internal_op_ordinal 三列 NULL，reason=`RETRY_STOPPED_BY_CLOSURE`） | A11 既有：effect_audit 内部子操作三列合同 deferred；reason 值规格冻结已用 |
| A34 | G7b | `v_recovery_takeover` 为 receipt 命令（非 lease CAS）：接管是受控恢复写，走统一 adjudicator（幂等重放/`IDEMPOTENCY_CONFLICT`/declared-hash 门）；不要求持有 session 协调/恢复 lease——per-attempt job lease guard 即接管资格权威（session lease 过期不隐含 job lease 失效，反之亦不阻塞） | A13 的 lease CAS 类不含接管；六步流程第 1 步「recovery lease」以 session 行锁 + 命令级 receipt 落地 |
| A35 | G7b | job lease 载体 = effect 行 `lease_owner`/`lease_until`（NULL/缺失/已过期 = 可接管）；「明确撤销」以接管事务内清空两列表示（receipt 记录撤销前值）；P0B dispatch 不设置 job lease，活 lease 仅显式授予（测试注入）产生 | 规格未冻结 job lease 列位置（清单 A 列出 effect 级控制字段含 lease_owner/lease_until）；接管第 2 步 guard 读该两列 |
| A36 | G7b | 接管信封携带 per-effect evidence map（effect_id → 证据对象）；`known_success`/`known_cancellation` 证据在接管处不驱动终局结算（成功收束需完整 EffectResult payload）→ 按保守 unknown_outcome 收束 + receipt 注记 `*_deferred_to_repair`，升级经 G7c repair | 三入口分类一致性（Conformance 2(5)）不受影响（分类仍经唯一 `v_classify_evidence`）；规格「照常结算」的成功/取消臂以 repair 里程碑补全 |
| A37 | G7b | 执行许可 gate 的 sticky cancel/quiescing 处置：G7b 阶段两条件命中且存在 known_failure 处置目标 → 整命令稳定拒绝（`CANCEL_STICKY`/`DRIVER_QUIESCING`，零控制态，变更前置）。**G8b 已消解 sticky 腿**：接管事务内按规格走共享取消收束（settlement 照常 + 规则 3 sticky 出口 1/2 + 共享 turn/end 派生，δ 触发源）；quiescing 腿仍为结构拒绝（受控边收束属 driver 切换里程碑） | 本 stage 无公开路径可产生两条件（request_cancel/finish_switch 未实现）；gate 结构（判定位置在 3(c)、先于一切变更）已按规格落地；G8b 起 sticky 分支经真实收束（A48–A54） |
| A38 | G7b | 接管第 2 步 fence 推进（F1）与 cohort 分配 fence（F2）各取一次序列：F1 无条件推进保证无分配分支的旧 completion stale 语义；分配支按 G7a 冻结子操作另取 F2——两值同事务严格单调（F0→F1→F2），新 attempt `dispatch_job_fence` = 接管事务终态 `current_job_fence` | 规格「新 dispatch_job_fence = 第 2 步推进后的新值」以链式单调读法满足；避免改动 G7a 已冻结的 `v_retry_cohort_allocation` |
| A39 | G7c | `repair` 为 receipt 命令但不要求持有 session 协调/恢复 lease（A34 同款裁定）：规格「repair 必须取得 recovery claim」以统一 adjudicator + session 行锁 + driver/epoch envelope 校验落地；terminal-session repair（INFRA/WORKSPACE_LOST drain 后「recovery claim 可取得、仅用于收束」）在本 stage 不可达——failure-drain 路径尚不存在 | 接管（A34）先例；drain 里程碑落地终态例外入口时补 terminal-session repair |
| A40 | G7c | stage-local code 名与分类（G7c 新增）：目标侧拒绝（effect 未持久化/不属本 session/非 unknown_outcome/无 attempt 行、链上 (v)(3)(6) 校验失败——跨 turn/普通事件/过期链头/孤立 key、resolution 对象与派生结算不一致、closer payload 元数据不一致）统一 `REPAIR_TARGET_INVALID`；信封 attempt 非当前执行 attempt（含已被取代）归证据绑定失败族 `REPAIR_EVIDENCE_REQUIRED`（规格「旧 attempt 证据冒充」逐字）；resolution/closer 形态校验沿用 completion 既有名（`TOOL_RESULT_MISMATCH`/`RESULT_PAYLOAD_INVALID`/`DECISION_PLAN_INVALID`，DECISION_PLAN_INVALID 仍为纯 receipt 拒绝，INFRA 收束随 drain 里程碑） | 同 A20/A26/A29：规格冻结 outcome 类与 `REPAIR_TARGET_INVALID`/`REPAIR_EVIDENCE_REQUIRED` 两个闭合 code，未逐项冻结目标侧细分 code | **〔G15 已消解 D4 子句：见 A111/A106——`DECISION_PLAN_INVALID` 已改为同事务 INFRA 收束〕**
| A41 | G7c | 槽位 create-when-absent 模式未实现：规格允许「repair closer 在槽位行不存在时同事务按同一派生创建」，本 stage 全部 unknown 收束路径（completion unknown 闭包、接管 unknown 分支）恒同事务创建 provisional 槽位，repair 处槽位缺失属不可达形态，防御性拒 `REPAIR_TARGET_INVALID`（零副作用） | create-when-absent 随 cancel/reconcile 里程碑（可产生无 unknown 的槽位需求） |
| A42 | G7c | 规则 3 取消收束在 G7c 阶段仅实现 repair 触发的最小臂（无 sticky latch 的 provider/request 取消族）：残留 `failed_retryable` 经受控边 `failed_retryable -> failed_terminal`（audit `RETRY_STOPPED_BY_CLOSURE` + reason CAS）后按派生表取消行落 step/session `cancelled`；取消 effect code 的聚合读取载体 = closers 行 resolution canonical 字节（effect_requests 无 code 列、DDL 未扩）。**G8b 已消解**：最小臂替换为共享实现（`v_aggregate_step` 规则 3 + `v_apply_step_aggregation` + `v_shared_cancel_closure`；code 读取收敛为 `v_effect_cancel_code`，见 A51） | 完整规则 3（sticky 臂、四触发源、五出口）由 G8b 交付；本 stage 无公开路径产生 sticky latch |
| A43 | G7c | (v)(2) 幂等查重的排序实现：`v_repair` 在目标守卫前对 `turn_end_closers` 做 digest 只读查找以排序幂等返回（同 resolution 重发时 effect 已终态，若先行目标守卫会误拒 `REPAIR_TARGET_INVALID`）；完整两级比对（digest+canonical+payload+派生键+predecessor）仍由唯一受保护槽位更新函数在槽位行锁内权威重执行——前置读取不构成函数外第二链校验路径（supersedes 链上校验唯一在函数内） | 规格 (v)(2)「重发携带旧 supersedes 亦幂等返回」与 §2.7 目标守卫的次序由此确定 |
| A44 | G7c | canonicalizer「被取代中间 closer 消除」按 per-effect 谱系实现：同一 effect 的后到 closer 取代其先到 closer（本 stage repair 对每 effect 至多一条 closer，该消除为防御性路径 + fixture 断言）；turn 级 supersedes 链上**他 effect** 的中间 closer 保留其 per-effect 语义角色（assistant/message、tool/result）——槽位链是槽位级谱系，不消除兄弟 effect 的结果表示；原始 unknown marker（provisional end）恒由 reducer 持有、resolved 后不再出现于输出 | 规格 §1.2 行 66「取代对应 provisional unknown 表示」的 per-effect 读法；多 effect turn 的唯一一致语义 |
| A45 | G8a | `v_cancel_event_key@db1` 内部派生事件键（绑定 `(session_id, event_type, turn_end_key, payload_hash)`）：cancel 生成的 `turn/end` 无已结算 effect 可绑（取消不是 completion），以 canonical 槽位身份 `turn_end_key@v1` + payload hash 派生命名键；每 `(session, turn)` 至多一条 cancel 生成的已知 end，键在会话事件键空间内稳定无碰撞 | 规格未冻结 cancel 生成事件的字节公式（同 A14/A23：s31b §3.7(b) 只冻结绑定域）；已知 end 的生成身份 `(command_id, turn_end_key)` 以本键落地 |
| A46 | G8a | 规则 3 sticky 收束臂在 `v_request_cancel` 内联实现（无 pending/unknown 且 sticky latch 已设 → step/session `cancelled`，`outcome_code` 按派生表 cancel-wins 二分 `CANCELLED_BY_REQUEST`/`FAILED_TERMINAL_CANCELLED`）；`v_aggregate_step` 的规则 3 占位与共享 `shared_cancel_closure` 五出口仍 [LATER]（G8b）。存在 in-flight `dispatch_started` sibling 时按规则 2 sticky 落 step `cancel_requested`/session `waiting_effect`（防御性，本 stage 无公开路径产生——dispatch 先于 cancel 的三段竞态第 1 段） | 任务书 G8a 仅限 request_cancel 命令；内联避免改动 G7a 冻结的共享聚合函数（不动 v8_retry.sql） |
| A47 | G8a | 取消路径 audit 行为普通列（`internal_op_kind`/`parent_command_id`/`internal_op_ordinal` 三列 NULL，reason=`ABORTED_BEFORE_DISPATCH`），同 A33；已终态 attempt 的迟到 completion 经既有 `ATTEMPT_ALREADY_SETTLED` 臂拒绝并**新增一行** audit（reason=`ATTEMPT_ALREADY_SETTLED`，原臂仅写 receipt）——满足 §2.9「零控制态、仅 receipt 与 effect_audit」 | A11/A33 既有：effect_audit 内部子操作三列合同 deferred；新增 audit 为纯加行，对既有 gate 无副作用（复跑全绿） |
| A48 | G8b | `known_cancellation` 结算开启（G4 阶段曾拒 `CANCEL_LATER`）：终局结算经 `v_settle_known_cancellation` 落 `cancelled_after_dispatch` + §4.3 map code，再走共享判定/落盘函数与共享 turn/end 派生。effect-stage 数据库不加载该共享子操作，故 G4 gate 中「拒 CANCEL_LATER」的断言迁移为纯分类断言（判定表两行由 `test_classify_evidence_unit` 覆盖，结算由 G8b gate 覆盖） | 该行为是 G8b 的合同交付项；effect/tools 阶段数据库仍能加载（函数引用按调用时解析、不可达分支不解析），G4 gate 复跑全绿（273 → 266 断言：移除迁移掉的 7 条） |
| A49 | G8b | (α) 出口 3（sticky 下 success）不执行正常成功臂的 decision marks/plan 冻结/turn 关闭聚合（cancel-wins 下这些语义失效）；结果由 `result_hash` + SQL 生成的 `assistant/message`/`tool/result` 语义事件 + `COMPLETED_AFTER_CANCEL` audit 承载（每个已结算 effect 仍保有表示事件，canonicalizer 的 per-effect 信号可导出） | 规格 §2.8 出口 3 只规定「保留 succeeded + 记 audit」；语义事件保留是 §1.2 trace 合同的推论 |
| A50 | G8b | before/after dispatch 二分以 effect 级 status 信号为准（与 canonicalizer 第 3 段 `dispatched` 集合同源，`v_turn_cancel_reason` 与 reducer 一致）：retry 分配后取消的 effect（当前 status 已由同步改写为 `cancelled_before_dispatch`、其退役 attempt 曾在 `dispatch_started`）判 `before_dispatch` | §2.10「曾进入 dispatch_started」在常态（已 dispatch 的 effect 终态仍属 dispatched 集合）成立；该边界源于 A31（失败结算不产语义事件，canonical 输入无承载早前 status 的载体）；attempt 行仍保留 `dispatched_at` 事实，裁决：不引入第二判定源 |
| A51 | G8b | effect 级取消 code 的聚合/派生读取载体重申（A42 延续）：`v_effect_cancel_code` 由持久化事实导出——(a) repair closer resolution code；(b) sticky 收束 audit `RETRY_SUPPRESSED_BY_CANCEL` → `CANCELLED_BY_REQUEST_AFTER_DISPATCH`；(c) 绑定 provider receipt → `CANCELLED_BY_PROVIDER`；(d) 兜底 request 族。`turn_end_closers` 查询以 `to_regclass` 守卫（retry/takeover 阶段库无该表，规则 3 在这些库不可达）。已核对 canonicalizer `_STICKY_CODES`/`_PROVIDER_CODES` 与实现产出的 code 族逐字对齐（`ABORTED_BEFORE_DISPATCH`/`UNKNOWN_AFTER_DISPATCH` 非取消族、不入集合） | `effect_requests` 无 code 列、DDL 未扩（A42 既定）；guard 使共享文件可被全部 ≥ retry 的阶段库加载 |
| A52 | G8b | canonicalizer 两处修正（Conformance 15 强制要求）：(i) `_PENDING_STATUSES` 增补 `planned`/`ready`/`dispatch_started`（§3.2.1 聚合口径的 pending）——在途 sibling 必须抑制已知 end（guard (a)）；(ii) 「仅有 chunk、无 final、LLM effect succeeded」的 partial 合成不再要求 `unsealed_tools_plan=false`（带 tool calls 的 decision 缺 final → partial 留存、turn MUST NOT 终结，guard (b)），`missing_final` end 仍只在关闭 decision 且无未密封 plan 时产生 | 规格 §1.2 两个 reducer 行；G3 gate 复跑全绿（含既有 missing_final 用例） |
| A53 | G8b | unknown 闭包的 provisional `turn/end` 事件行改带 effect 归属列（`effect_id`/`attempt_no`——与既有的创建键 `v_completion_event_key(session,'turn/end',effect,attempt,…)` 绑定一致、此前列留 NULL）；派生事件（`v_derive_cancel_turn_end`）仍无 effect 归属 | 使 canonicalizer 的 per-effect 控制信号可由事件流单独导出（§1.2 输入契约「由表示该 effect 状态的事件承载」）；G4/G7c gate 复跑全绿 |
| A54 | G8b | `v_request_cancel` 活跃 step 收束改走共享判定/落盘函数（G8a 的内联规则 2/3 移除）：latch 在聚合前写入（规则 3 读 `sessions.cancellation_epoch`），applier 以 `p_write_session=false` 运行（session 控制态由命令自身收尾、fence 只递增一次）。`v_apply_step_aggregation` 非 failed 分支补写 `failure_code`（规则 3 的 `CANCELLED_BY_REQUEST`/`CANCELLED_BY_PROVIDER`；规则 1/2/5 传 NULL 无行为变化） | 唯一实现（§2.8/§2.1）；G8a gate 全部断言不变复跑全绿 |
| A55 | G9a | `session_events` 三流列（`stream_id`/`chunk_index`/`observation_ordinal`）经**新 SQL 文件 `stream/v8_stream.sql` 的 ALTER TABLE** 增列，而非并入 `schema/v8_schema.sql`；该文件在 `SQL_LOAD_ORDER` 中排在 `schema/v8_keys.sql` 之后、`events/v8_append.sql` **之前**（`STAGE_THROUGH` 重编号：schema 2 · events 4 · stream 9 · effect 5 · tools 6 · retry 9 · loop 5 · repair 10 · cancel 11）。`stream` stage 取到 retry——gate 覆盖 events/effect/retry 三处消费者 | 任务给定加载序约束（新列/表被 events 段消费 → 必须先行）；schema stage 不含流列（既有 G2 gate 断言不变） |
| A56 | G9a | `v_grant_valid(p_session_id, p_grant_id, p_capability)` 的「subject 匹配调用者」合取按**会话绑定腿**判定（`session` 命中调用 session、`driver`/`plugin_identity` 命中 session.driver、`step` 命中 active_step_id）；经 grant/driver 链的完整 subject 解析与 `constraints` 参数级求值（path/command/target/TTL/max_bytes/max_rows）→ P1。**G10 已消解**：完整模型落于 `v8/grant/`（三参冻结面保留 + 新增 `v_grant_lock_judge`/`v_grant_find_valid` 完整判定入口——subject 按 kind 经调用 session/driver/active_step/声明 caller subject 解析（见 A63）、append 查找不再按 subject_id 裸匹配、`constraints` 六维参数级求值落地（A62））；G9a 断言块随迁移由 G10 gate 复跑覆盖 | 摘要 §2.1 有效 grant 谓词在本 stage 冻结签名（仅三参）；完整模型 P1 |
| ~~A57~~ | G9a | grant 模型 **opt-in**：`v_append_events` 增 4 个 DEFAULT NULL 调用方身份腿（`p_caller_subject/driver/epoch/grant_id`）；目标 effect 未被 grant 绑定（`grant_id IS NULL`）且四腿全缺省时，chunk 六项第 (3)(6) 项按 legacy P0B 路径**注记降级放行**；一旦 effect 被绑定或任一腿被提供，则 (3)+(6) 全部合取强制（缺任一 → `CHUNK_ATTRIBUTION_INVALID` 零落盘）。`v_stream_bound` 的触发条件即此。**G10 阶段保留**（chunk 腿原样 + session/heartbeat 授权门同族降级（A65））；**G12 摘除降级并消解** | 任务要求「既有 gate 不受影响」+ 计划 G9 节「最小 grant stub 或注记降级」；G3 gate 复跑全绿（130 断言不变）；G10 复跑仍全绿 | **〔G12 已消解：见 A99——opt-in 降级摘除，四腿全缺省不再放行〕**
| A58 | G9a | 字段矩阵两个 reject 形态（(0)(ii)/(0)(v)）与计数值域/矛盾 reject 一律复用结构层 code `RESULT_PAYLOAD_INVALID`（不新增 code 名）；pending 两形态（(0)(iii)/(iv)）返回 `outcome='accepted'`、`code=NULL`、receipt `kind='stream_pending'`，并**同事务落一条 `stream_progress` 观测事件 + observation receipt 三元组 `(attempt_no, observation_ordinal, payload digest)`**、控制态零修改（effect/attempt 不变）。`stream_progress` 五款状态门与 observation receipt 完整合同 → G9b | 规格只称「结构层 schema reject」未冻结 code 名；observation receipt 完整合同属 G9b，本 stage 仅落字段矩阵与 pending 落盘骨架 |
| A59 | G9a | `stream_progress` 观测事件的 event_key 用 `v_completion_event_key(session,'stream_progress',effect,attempt, sha256(sha256(payload)‖':'‖ordinal))` 内部派生（把 ordinal 折入 hash 槽使同 attempt 多观测不碰撞）；O01 (α) observation identity 的字节级公式未冻结 | 同 A14/A23 模式（内部派生 identity、非跨语言断言对象）；`observation_ordinal` 按「生成命令事务内 max+1 单调分配、receipt 重试复用」落地 |
| A60 | G9a | chunk 第 (3) 项 stream_id 归属在 stub 下判定为两点：(a) effect 的绑定 grant 必须解析出 provider/adapter subject（`grant_id IS NULL` 即未绑定不适用）；(b) 该 `stream_id` 不得已被**另一 provider 绑定的 effect** 用过（跨 session 检查，经 `session_events` 已有 chunk 行 + `v_grant_subject` 判定；未绑定 effect 无 provider 声明、不参与）。第 (4) 项保持 effect 级 dispatch marker（沿用 G3/G4，不改 attempt 级）。**G10 已消解**：`stream_registry` 表替换近似（首次合法绑定 chunk 同事务注册 stream→owner subject，外来 provider 复用拒 `CHUNK_ATTRIBUTION_INVALID`，见 A66） | 「provider 的 stream 按 provider 分配」无 registry 可查，以 DB 可判定的外来流复用冲突近似；完整 stream registry → G10 已交付 |
| A61 | G9a | `slices`/`grants` 的表级约束取「规格最小字段 + 类型/NOT NULL 实现方补齐」：`slices` 增复合 `UNIQUE(workspace_id, slice_id)`（承载 grants 租户一致性复合 FK）、`grants` 增 `CHECK(expires_at > not_before)`；`spec`/`constraints` 不可变用 BEFORE UPDATE 触发器（规格原文只说「不提供 UPDATE 路径」）。**G10 已消解**：完整 §2.1 模型以 stub 同列面迁入 `v8/grant/v8_grant.sql`（只增不改不删——G13 依赖 stub 稳定面），原列面/约束原样保留并成为完整模型的基座 | 摘要 §1.1/§1.2 明示「规格仅部分给出类型与 NOT NULL」「无 DDL 级触发器，规格未给」；触发器为防御纵深 |
| A62 | G10 | `constraints` 参数级求值 schema 自定（计划裁定「A1 最大实现自裁空间，随 G10 评审确认」）：`v_constraints_ok` 按 `paths`（LIKE 前缀）／`commands`（精确白名单）／`targets`（成员）／`max_bytes`／`max_rows` 五维对参数对象求值，**在场约束 × 缺失参数 fail-closed 拒绝**；`ttl_seconds` 与 `[not_before, expires_at)` 的关系取「可用窗 = [not_before, min(expires_at, not_before + ttl))」（`v_grant_lock_judge` 内求值，因需 grant 行）；slice-membership 以 `spec->'resources'` × `params->'target'` 双在场时强制（`v_slice_member`） | 规格只冻结六维枚举与合取语义，未冻结键名/参数对象形状/缺参语义 |
| A63 | G10 | subject 链解析规则自定（计划裁定「subject 链解析规则自定」）：`session` kind = 调用 session 或声明 caller subject；`driver` = session 行 driver、声明 caller driver 或 caller subject；`step` = active_step_id 或 caller subject；`plugin_identity` = 声明 caller subject（chunk 路径由 (6)(iii) 锚定 effect 绑定 grant 的 subject）或 session driver；声明 caller driver/epoch 与 session 行不符 fail-closed（`CALLER_DRIVER_MISMATCH`）。判定 reason 集合（GRANT_REVOKED/SLICE_REVOKED/OUT_OF_VALIDITY_WINDOW/CAPABILITY_MISMATCH/TENANT_MISMATCH/SUBJECT_MISMATCH/CONSTRAINT_VIOLATED/SLICE_MEMBERSHIP_DENIED/CALLER_DRIVER_MISMATCH）为 stage-local 命名（同 A20 族：规格冻结 `GRANT_DENIED` 语义、未冻结细分 reason 名）——`v_grant_find_valid` 返回 NULL 即对外 `GRANT_DENIED` | 规格「经 grant/driver 链解析」无逐字判定表 |
| A64 | G10 | `sessions` 增列 `workspace_id`（可空、默认 NULL；经 `v8_grant.sql` ALTER、未动 G2 冻结 DDL——A8「at least contains」先例）：承载 §2.1 合取项 2 的「调用 session」租户腿；NULL = legacy 未划定租户（既有全部 gate 会话不受影响，judge 对 NULL 会话不施加 workspace 过滤），G10 测试以绑定会话验跨租户拒绝 | 规格 session 控制行 15 列清单未含 workspace 列、合取项 2 又含「调用 session」一致性——增列为最小落地面 |
| A65 | G10 | session/heartbeat `event_append` 授权门的**触发条件 = 四调用方身份腿任一在场**（grant-model 路径）；四腿全缺省的 legacy 路径按 A57 同族注记降级放行（G10 保留、G12 摘除）。授权前置拒绝以 receipt 命名空间外的返回 + `authz_denial_audits` 独立审计行落地（键 = 调用安全上下文 + 目标命令标识、幂等合并一行；`occurrences` 为本表自有计数列、非 deferred 的指纹 occurrences 子表）；precheck 在 `v_append_events` 内先于自身 adjudicate 执行，前置 session 行锁后取 grant/slice 锁（八位主锁序） | 计划「偏差 A57 降级本 gate 保留」的具体化；测试层先经 `v_command_gate`（execute 分支零写入）不违反「先于 receipt 幂等查重」（拒绝路径仍零 receipt/binding） |
| A66 | G10 | `stream_registry` 语义自定：首次合法绑定 chunk **成功落盘同事务**注册 `stream_id → owner_subject`（workspace/effect 随行）；校验阶段查注册表（外来 owner 拒 `CHUNK_ATTRIBUTION_INVALID`）、注册后同事务复查 owner、竞态外来注册 fail-closed 整事务 RAISE 回滚（零事件零注册）；A60 近似（跨 session 扫 chunk 行）摘除 | 规格「stream_id 可归属 effect 绑定 provider/adapter」无注册时机/载体冻结 |
| A67 | G10 | fork 切点稳定性判定以切点内事件 + 当前 effect 状态近似：不稳 = 切点内 provisional `turn/end {outcome:unknown}` 且切点内无已知 end（closer 以切点内已知 `turn/end` 事件识别——`turn_end_closers` 表不在 grant 阶段加载序内）∨ 前缀 turn 存在非终态 effect（按当前状态）；Conformance 11 (a) 三断言实现在 `v8_grant.sql`、验收随 G13（其加载序含 repair/closer 表） | grant 库无 closer 表/repair 函数；计划明定验收归 G13 |
| A68 | G10 | pre-dispatch 双表原子同步共享子操作 `v_predispatch_cancel_sync` 落于 `v8_grant.sql`（G10 交付，WORKSPACE_LOST drain 已消费；G11 改 `v_request_cancel` 侧调用、G12 回归覆盖）——过渡期 cancel 内联块与共享函数两份实现并存为计划明定的合并序中间态 | 计划「函数体晚绑定，G11 稍后改 cancel 侧调用」。G11 已落地 `v_request_cancel` 改调共享子操作——其实现为 G11 侧六参变体 `v_pre_dispatch_cancel_sync`（非同源，见 A73）：合并后两定义暂并存、G12 单一化 |
| A69 | G10 | RLS 实现面自定（计划裁定「RLS 角色名/policy 谓词自定（测试以 SET LOCAL ROLE 行使）」）：`slices`/`grants`/`workspace_handles` ENABLE+FORCE RLS + `v8.workspace_id` GUC policy；非超级用户角色 `v8_worker`（grant setup_db 建号，LOGIN NOBYPASSRLS、仅 SELECT/EXECUTE、无业务表 DML）；SECURITY DEFINER 能力函数以引导超级用户为 definer（超级用户天然绕过 RLS——受控通道语义由函数参数 + 逐函数审计承载）；worker 直连以 socket URI 构造（pgembed trust 本地认证） | 超级用户绕过 RLS 为 PG 语义；「禁 SET ROLE」以 worker 直连会话断言（SET LOCAL 会话的角色集源自超级用户 session user、不构成有效向量） |
| A70 | G10 | `workspace_handles` checkpoint digest 覆盖物以 `sha256(manifest '|' repl '|' op_seq '|' generation)` 拼接实现（规格只冻结覆盖范围——worktree manifest/REPL 变量引用/最新已完成 op_seq/generation，字节编码属 §3.3 LATER）；materialize 的「文件缺失」以 `p_files_present` 布尔入参为可判定输入；「checkpoint 未覆盖最新已完成执行态」判据 = 存在未收束 `in_flight` mutation 行；controlled mutation 记录含 `invalidated`（随接管失效）态、其 reconcile 路径 LATER | 同 A14/A59 族：内部 digest 非跨语言断言对象；G13 真实 I/O 层不可达 |
| A71 | G10 | 计划文件清单外新增 `v8/grant/fixtures.py`（grant-seeded chunk fixture/seeders 共享 helper——计划明定「迁移时抽出……供 G12 复用」）与 `v8/grant/client.py`（operator 通道受控 wrapper——计划明定「operator 受控函数需要时加 client.py」）；`test_grant_stub` 整块自 `test_stream.py` 迁入 `test_grant.py` 后 **`test_stream.py` 断言计数重立：67 → 51 PASS**（迁出 stub 块 17 条、留薄烟测 1 条；A48 先例），G10 gate 143 PASS、G9b 72 PASS 不变 | 计划 G10 所有权段的抽取要求落为两个新文件；计数变化按准出判据 2 记台账 |
| A72 | G10 | seam 清单各同事务 grant 检查点（recall/fold/env_read/env_write/tool_resolve/authorize_effect）未实现：G10 交付了它们共用的「锁定+判定」子操作（`v_grant_lock_judge`）与 append 侧消费者（chunk 双 grant 合取、heartbeat 门），但 seam 检查点本体随 §3.3 assemble 面七步与 manifest hash 绑定 → assemble 里程碑；Conformance 14 首句的 dispatch 半边照测（G12 两道门）、seam 半边随 assemble（台账 L82 seam 段改记本偏差不划线） | 计划「明确不做」段裁定；seam 检查点依赖 assemble manifest 上下文 |
| A73 | G11 | 共享 pre-dispatch 双表原子同步子操作 `v_pre_dispatch_cancel_sync(p_session_id, p_command_id, p_audit_key_kind, p_audit_key_value, p_reason, p_scope_generation_id)` 在 G11 分支上定义于 `v8/plugin/v8_plugin.sql`（CREATE OR REPLACE、函数体自 `v_request_cancel` 内联块逐字抽取 + 计数重算；`v_request_cancel` 改为四参调用、行为不变）；其最终归宿为 G10 交付的 `grant/v8_grant.sql`（本分支 G10 未合并、函数名按计划给定的 `v_pre_dispatch_cancel_sync`），新增的 `p_reason`/`p_scope_generation_id` 两参带缺省供 generation drain 复用 | 三取消路径单实现（计划 G10/G11 行明示）。**wave1 合并落账（2026-09-16）**：G10 侧 `v_predispatch_cancel_sync`（四参、无计数重算，`grant/v8_grant.sql`）与本侧 `v_pre_dispatch_cancel_sync`（六参含 `p_reason`/`p_scope_generation_id` 缺省 + 计数重算）**非同源**，「相同体 OR REPLACE 共存」前提不成立——合并提交保留两定义并存（WORKSPACE_LOST drain 消费前者；`v_request_cancel` 与 generation drain 消费后者；晚绑定、无加载序风险），单一化收敛归 G12 回归统一到富实现并覆盖三路径 |
| A74 | G11 | steps 的「实现绑定列」取单列 `implementation_digest text`（可空，种子代/未接线行为 NULL）；规格 §4 行 745 要求 step 固定 catalog_generation + plugin_version/implementation_digest + contract version，本 stage 只落 catalog_generation（DEFAULT 种子代、ALTER 于 v8_plugin.sql、历史回填不可区分——A8 先例）+ 该单列 | 完整绑定（plugin_version/contract version 逐列）随 seal 门接线（G12）与 assemble 里程碑；G11 gate 断言列存在 + DEFAULT 种子代语义 |
| A75 | G11 | generation digest 的规范化表示自定（规格只冻结「成员全集的规范化表示按 §1.3 canonical profile 计算、两代成员不同即 digest 不同」）：profile `v8:generation-members@v1`，成员记录 {contract_version, driver, identity, implementation_digest, locus, plugin_version} 排键紧凑 JSON、按解析稳定序排列、排除代理键 implementation_id；构建期内容 digest 校验同款 profile `v8:implementation-content@v1`；Python 镜像在 `v8/plugin/client.py`，gate 以 ≥3 组独立 shell 手算 golden 向量锁 SQL==Python 字节级（含换序敏感与转义/UTF-8 向量） | 同 A14/A23 先例（未冻结字节公式处实现自选 + 跨语言 golden 锁定） |
| A76 | G11 | catalog active 指针载体 = `catalog_config` 单行表 + `catalog_pointer_history` 版本化处置历史（activate/rollback/clear/revoke/bootstrap 各行记 operator/时间/target）；指针↔状态不变量：激活自动 retire 前代 active、rollback 将 retired 目标重激活（「回退到上一可用 generation」）；`sessions.active_catalog_generation` 由 assemble 守卫 `v_assemble_bind_generation` 绑定当前指针（成功绑定、指针置空稳定拒 `NO_ACTIVE_GENERATION` 零控制态） | 规格只要求「版本化配置持久化（记录操作者、时间与目标 generation，可审计）」不冻结载体；operator 生命周期命令（revoke/activate/retire/dispose/GC）为非 receipt 受控操作（A13/A34 先例），入口拒绝以异常承载 |
| A77 | G11 | readiness 无 DB 表：按计划 G11 行「apply(ctx,config) 进程内幂等纯登记」以 `v8/plugin/client.py` 的 `PluginHandlerRegistry` + `NativeCtx`（tools.register/systemPrompt.section/on/get 四面）承载；publish 流程「worker 预加载并上报 readiness」= client 侧 build→apply→activate 三段，预加载失败经 `v_fail_generation_building('READINESS_FAILED')` 落 building→failed（active 指针不动） | 规格 §4 的 readiness 本就是进程内合同（「handler registry 只是非权威缓存」）；DB 侧仅 building→failed 终态 |
| A78 | G11 | 发布 manifest 形状自定：candidates 携带 {identity, contract_version, plugin_version, driver, locus, entry, content, implementation_digest, priority, requires[{identity, version, optional}], provides[{service, version}], spec{…}}；requires 按 identity+版本范围（文法：`*`/空 = any，否则 `,` 合取的 `>=/>/<=/</=/` dotted-numeric 比较符）、provides 按 service、重复 provide 判定按同 driver 域 | 规格只冻结排序口径与三类拒绝，不冻结 manifest 文法；gate 覆盖文法与三类整代拒绝 |
| A79 | G11 | 下线事务「内部回滚整体重试」以 PL/pgSQL exception block（隐式 savepoint）实现（函数内不能 EXECUTE SAVEPOINT）；重试上限 8 次（超限异常 `GENERATION_REVOCATION_RETRY_EXHAUSTED`，条件性收敛注记）；确定性重测以 GUC seam `v8.revoke_test_hide_session`（首遍预锁隐藏一个已绑定 session，G9b `v8.stream_window_exhausted` 先例） | §4 第 7 条 (d) 的回滚重试语义不变（释放全部锁、按新绑定集重新全序预锁、不倒序补锁）；seam 仅测试可观测 |
| A80 | G11 | `plugin_implementations.first_published_generation_id` 不设硬 FK：该列「仅是溯源记录、MUST NOT 表达归属或成员关系、不参与任何调度/目录判定」（§4 原文），而无引用 GC 会删除被共享实现行（仍被在世 generation 引用、MUST NOT 删）所溯源的世代行，硬 FK 将阻塞 GC | 冻结的绑定 FK 只有 (identity, contract_version)→plugin_specs（已落）；溯源列以软引用承载 |
| A81 | G11 | drain 审计载体：共享 sync 写 `ABORTED_BEFORE_DISPATCH` 行（与 request_cancel 路径逐字段同形），下线事务另写 `GENERATION_REVOKED` 上下文行（携带 G2 预留的 `internal_op_kind='generation_revocation_drain'` + `parent_command_id`=revoke 操作 id + 递增 `internal_op_ordinal`；未密封 plan 作废行以全 NULL 归属 + plan_hash 指纹承载）；交付给 G12 串联的共享 generation 检查子操作为 `v_step_generation_status(step_id)`（generation 行 FOR SHARE 共享读、无绑定返回 NULL——G12 视为非 failed） | Conformance 8 (v)「effect 层复用 ABORTED_BEFORE_DISPATCH 并以 audit 记 GENERATION_REVOKED 上下文」的最小双行读法；共享读锁位符合统一锁序 generation 位 |
| A82 | G13 | `compat_unmapped_audit` 的调用方绑定（「该 session 绑定的受权 compat adapter」）以 `compat_adapter_manifests.driver = session.driver` + envelope adapter identity 精确匹配落地（manifest 行不存在/身份不符 → stage-local code `ADAPTER_IDENTITY_MISMATCH`，rejected_mismatch 零副作用）；audit 事件键 `v_nonstream_event_key(session,'compat/unmapped',command_id,0,payload_hash)`（occurrence identity `(command_id, 0)`） | 经 grant/driver 链的完整 subject 解析 P1（A56 延续）；s31b §3.7(b) 只冻结绑定域不冻结字节公式（同 A14/A23 模式）；stage-local code 名同 A20 先例 |
| A83 | G13 | `v_compat_begin_switch`（Conformance 11 (c) 强制负向）：unsupported 声明 → 冻结 code `UNSUPPORTED`（变更前拒绝、mode/fence/switch intent 不变、receipt 幂等重放）；supported 声明 → stage-local code `SWITCH_PROTOCOL_NOT_IMPLEMENTED`（正向机制「明确不做」另行排期，仍零变更）；无 manifest 声明的 driver → stage-local `DRIVER_MANIFEST_MISSING` | 计划「明确不做」节：driver 切换正向机制（begin_switch/quiescing/finish_switch）排期为用户决策项，G13 仅交付 UNSUPPORTED 强制负向 |
| A84 | G13 | fork 切点三断言（Conformance 11 (a)）经 G13 验收层孪生承载：`v_compat_fork_cutoff_stable`（冻结 guard 判定：切点内未闭合 turn / 未解决 unknown 不借切点后 closer / pending 非终态 effect）+ `v_compat_fork`（非 receipt 受控函数：稳定切点 → 子会话创建 + 事件前缀继承零控制态；不稳定 → `FORK_CUTOFF_UNSTABLE` 父态不动）。fork 命令本体属 G10 `v8_grant.sql`（本分支未合并、任务禁止引用） | 合并阶段 MUST 核对孪生与 G10 实现对本 gate 全部判定向量一致（fork 不在统一命令清单内、receipt 面由 G10 冻结，孪生不触碰 receipt 命名空间以避免第二命令面）；G13 gate 在合并后复跑仍绿 |
| A85 | G13 | pinned host 外部 manifest 工件（`v8/compat/pinned_host_manifest.json`）：五件套（DSH package/Node/profile digest/adapter/canonicalizer）+ §5.2 前置验证项全部 null+note（未解析）；存在未解析项 → compat contract 不得宣称已过（报告器强制）；seed 行 `driver_switch_capability='unsupported'` 为保守声明（pinned host 未确认前无法声明 supported）、`dispatch_interception` NULL+note | pinned DSH Node host 身份为用户决策项（计划开放问题）；「未解析项不得宣称已过」为 §5.2/P0C 冻结要求 |
| A86 | G13 | GUC seam `v8.compat_dispatch_interception` / `v8.compat_driver_switch`（仿 G9b `v8.stream_window_exhausted` 先例）仅驱动矩阵逻辑测试：`v_compat_participation` 读 manifest 值（NULL → 有效值 none，未知即 none）+ 事务本地 GUC 覆盖（非枚举值忽略）；函数只输出 blocked 集/强制负向集，结构性无「记绿」通道 | 计划 P0C 环境边界：测试注入 ≠ capability 实证——`dispatch_interception` 真实取值只能来自 §5.2 前置验证（blocked），禁止以 GUC 伪造 sync_before_io 通过真实 I/O 层行 |
| A87 | G13 | 真实 provider（DeepSeek）DB 协议层子用例凭据环境变量门控（`DEEPSEEK_API_KEY`/`OPENAI_API_KEY`、`OPENAI_API_URI`/`OPENAI_MODEL`，v6 同名约定）：缺省永不触发真实调用（`DeepSeekLLM` 仅显式构造、默认 loop 与既有 gate 不变——AGENTS.md「FakeLLM/FakeTool、不调真实 provider」约定保持，显式凭据属 capability 来源 blocked）；缺失时 skip 计入 blocked 清单、退出码仍 0；真实调用在数据库事务外（连接 idle 前后断言）；fake suite 权威性断言保留 | 与 AGENTS.md 约定的关系：该约定约束默认测试面，Conformance 12「真实 provider 只用于协议、幂等能力和 uncertain window 认证」为合同显式许可的例外通道；本 gate 实跑时凭据在场、三子用例真实调绿（deepseek-flash） |
| A88 | G13 | P0C 报告器（`v8/compat/p0c_report.py`）交付 host 无关半边：fixture/subcase × 测试边界（数据库层/真实 I/O 层分列）× 两维组合、覆盖 Conformance 全部 16 条（4 并入 3 沿矩阵注）；blocked 仅允许来自两维矩阵（SQL 单源）或显式外部 allowlist（计划 P0C 环境边界七项 capability 来源）；六类不通过（非矩阵 blocked/条目缺失/Native 绿冒充 portable/真实 I/O 降级记绿/数据库层替代真实 I/O 层/负向因 blocked 免除）逐一拒收并有负向测试；真实 I/O 层行挂 blocked 待实证、P0C 终签 blocked | P0C 验收段「报告 MUST 按测试边界分列 + 存在非矩阵来源 blocked/条目缺失/以 Native 绿冒充 portable 均为不通过」；blocked 清单①–⑦即计划 P0C 环境边界 |
| A89 | G13 | §5.1 十六行映射数据库层以 compat driver（`dsh-compat`）走既有共享命令面（公开 append/completion facade/seal/dispatch/cancel/repair）实现；X03 直插 grants 行（stub 兼容面）+ `setup_db.py` seed 固定命名 grants（五 capability，driver 腿 subject；Wave1 恒过下无害，G12 门变真后走 seal/dispatch 路径的用例凭 seed 通过——计划 setup_db 所有权约定）；attempt/heartbeat 内部路径与 compact 控制平面未实现（矩阵 🟡 缺口如实记载，不冒充全覆盖） | 计划 G13 行「X03 用直插 grants 行（stub 兼容面）实现」「走 seal/dispatch 路径的用例一律携带 setup seed 的固定命名 grants」 |
| A90 | G12 | seal 冻结授权上下文载体：`effect_requests` 增列 `authz_params jsonb`（A8 先例 stage ALTER，G2 冻结 DDL 不动）+ `v_gate_params(command,target,path,bytes)`——target = `'effect:'||effect_kind`（初始 LLM slot）/ `'tool:'||tool_name`（工具 effect）、path = session 作用域资源路径、bytes = 密封 payload 尺寸；`effect_requests.grant_id`（G2 列）承载冻结的 `effect_submit` grant（chunk 归属第 (3) 项 provider/adapter subject 来源），`authorize_effect` grant id 存于 `authz_params` | 计划 G12 行「effect_requests.grant_id seal 冻结绑定（后续门按冻结 grant_id 复验）」 |
| A91 | G12 | 第一道门（seal 授权阶段）落于两条 seal 路径（`v_prepare_step` 初始 decision seal 与 `v_seal_batch` tools seal）：在任何 batch/effect/attempt 创建之前按具体参数**逐成员**实时授权（有效 `authorize_effect` ∧ 有效 `effect_submit` 各一）；任一成员失败 → 整个 seal **零副作用**拒 `GRANT_DENIED`（仅 binding + 拒绝 receipt[+audit]）；同 command_id 重放幂等返回原拒绝，授权恢复后需新 command_id 重发方可 seal | 计划 G12 行第一道门 |
| A92 | G12 | tools seal 的授权阶段置于 step 行锁**之后**（G6 重放/CAS 分支必须在该锁下判定），故此处 grant/slice 锁跟随 step 锁取得——与规范主锁序（grant/slice 位先于 step 位）相反；对侧取 grant 锁者（operator 撤销、其他 seal/dispatch、cohort 门）从不在持 grant 锁时取 step 锁，故无可构造死锁环（`v8/tools/v8_tools.sql` L382 注记） | 计划共享热点段「锁序耦合」风险项；spec §2.1 主锁序 |
| A93 | G12 | 第二道门（dispatch 派发门）：`ready → dispatch_started` 状态 UPDATE 前对**冻结的**两 grant（`grant_id` 与 `authz_params.authorize_grant_id`）以**冻结参数**复验，manifest 快照不豁免；拒绝只读零控制态（effect 保持 ready）；generation 只读不加锁（drain 所有权在下线事务）；两门独立性三序列（MUST NOT 互替、任一通过不豁免另一门、dispatch 通过后撤销不追溯取消已派发 attempt） | 计划 G12 行第二道门 |
| A94 | G12 | 冻结参数稳定性裁定：dispatch/cohort 的 constraints 复评以**冻结参数**为准（撤销、有效性窗口与 TTL 为**实时**输入）——不在派发时用新参数重算约束；非 NULL `grant_id` 伴 NULL `authz_params` 的 pre-G12 遗留行复评时 authorize 半边跳过（legacy binding）；NULL `grant_id` 恒判拒（Conformance 14） | `v8/effect/v8_effect.sql` L52–56 注记；计划裁定「dispatch 侧授权前置拒绝不入 G12」 |
| A95 | G12 | cohort 授权/generation 恒过 TODO 替换为真实现：步骤 (3) 逐成员复验 seal 冻结 grant（调用方预锁同一 grant/slice 集）；步骤 (4) step 绑定的 catalog generation 共享读检查（调用方已 FOR SHARE 预锁）；普通入口拒 → receipt `GRANT_DENIED`（session 保持 claimed、lease 不隐式释放、fence 不递增）；recovery 入口拒 → receipt 整体 accepted + step 子结果 `allocation_denied:GRANT_DENIED`（接管与旧 attempt 结算不回滚）；generation 拒经共享收束子操作落 `GENERATION_REVOKED` + `RETRY_STOPPED_BY_CLOSURE` | 计划 G12 行 cohort 恒过替换；G7a 恒过注记退役 |
| A96 | G12 | takeover 预锁集补全八位主锁序（G7b 可达子集 → 全集）：session → grant/slice → generation → step → effect → attempt，逐层按排序键加锁；grant 集显式覆盖 session 全部 effect 的冻结 grant（含 eligible `failed_retryable` sibling，非仅 in-flight 接管目标）——步骤 (3) 在同一事务已持有的锁下复评 | 计划 G12 行「takeover 预锁集补全八位」 |
| A97 | G12 | `ISOLATION_UNSUPPORTED` 三入口闭合：下线事务（G11 已交付）、WORKSPACE_LOST 终态 drain、recovery 接管——均非 READ COMMITTED 稳定拒（Conformance 8 (x)） | 计划 G12 行；Conformance 8 (x) 三入口补全 |
| A98 | G12 | A39 收口：WORKSPACE_LOST/INFRA failure-drain 后的会话可被 recovery claim **仅用于收束**（completed/cancelled 与非 drain 失败仍拒 `SESSION_TERMINAL`）；drain 后迟到 completion 正常结算 in-flight effect 且从不覆写终态 session；重复 drain 收敛（`drain_step_id` 清空、事件恰一条、fence 稳定）；终态 session 的 repair 走既有 closer 槽位链 | 计划 G12 行 A39 收口；台账 B 节 A39 |
| A99 | G12 | 偏差 A57 消解：`v_append_events` 的 opt-in legacy 降级**摘除**——未绑定 effect 且四调用方腿全缺省的 chunk 追加不再放行（第 (3) 项失配 → `CHUNK_ATTRIBUTION_INVALID` 零落盘）；三处既有 chunk fixture 改为 grant-seeded（经 G10 交付的 `v8/grant/fixtures.py::bind_chunk_provider` 共享 helper），`session/heartbeat` 授权门同步由「四腿任一在场触发」改为恒强制 | A57 本条（消解）；计划 G12 行「摘除 v8_append.sql A57 legacy 降级」 |
| A100 | G12 | 九处 stage `setup_db.py`（effect/tools/cancel/repair/retry/stream/loop/plugin/compat）seed 可通过 grant（门变真后正向路径零回归）；`stream` 的 STAGE_THROUGH 由合并期 10 更正为 **11**——`test_observation.py` 调 `v_recovery_takeover`，原「test_stream 无 takeover 依赖」的假设只对该文件成立 | 计划 G12 行各 stage seed；计划「SQL 加载序」段 stream 语义注记 |
| A101 | G14 | **槽位锁取得集规格字面 vs 实测差异**（计划「需裁定 ①」由探针实测回答）：生产源码中被判定取 `turn_end_slots` 行锁的函数集实测 = {`v_complete_effect`, `v_recovery_takeover`, `v_repair`, `v_turn_end_slot_update`}；规格 L356/L395 字面列 `complete_effect`/`request_cancel`/`FORCE_JOB_TAKEOVER`，其中 **`request_cancel` 实测不取槽位行锁**（走既有 `IF NOT EXISTS`+INSERT 建槽路径）、**`FORCE_JOB_TAKEOVER` 尚未实现**（G19 deliverable）。MUST NOT 静默通过：差异由 G14 gate 的实测取得集报告断言并在此落账；槽位/锁序的最终字面合规裁定待 G19 的 FORCE_JOB_TAKEOVER 落地后复评 | 计划「需用户/计划裁定」①；spec L356/L395；矩阵 #15 |
| A102 | G14 | D1-P1/D2-A 竞态向量的 holder 取锁方式：D2-A 的 holder 以 `FOR NO KEY UPDATE` 钉住 generation 行、并以受保护 lifecycle GUC（`v8.generation_lifecycle='on'`，revocation 函数自身使用的同一 seam）翻 `status='failed'`——**直接 `FOR UPDATE` 会与绑定步时 FK 检查的 `FOR KEY SHARE` 互斥而死锁测试主事务**（`steps.catalog_generation` FK 已实测存在）；`FOR NO KEY UPDATE` 与分配侧 `FOR SHARE` 互斥（竞态成立）且不阻 FK（绑定可继续）。D1-P1 仍用**真实** `v_revoke_generation`（该处无绑定步，无 FK 冲突） | G14 D1/D2 实现注记；plan G14 行「holder 持 generation FOR UPDATE（或真实下线线程）」 |
| A103 | G14 | D1 tools seal 半的拒绝码：预锁串行化把下线排在 seal 之前，seal 解除 session 行锁阻塞时下线已推进 `session_fence` 并失败化 generation，故 tools seal 被**先到的 guard** 稳定拒——实测 `SESSION_FENCE_STALE`（而非初始 decision seal 半的 `GENERATION_REVOKED`）。契约点为「稳定拒绝 + 零新增控制态」（`counts` 不变已断言），具体 code 由 guard 顺序决定，gate 断言接受二者之一并记录实测 | G14 D1 tools 半；spec 主锁序 |
| A104 | G14 | **G4 断言计数重立（A48/A71 先例）**：D5 向 `v8/effect/test_effect.py::test_outcome_mismatch` 增补声明 authority vectors (ii)(iii)（证据无终局回执→`unknown_outcome`；provider 证实成功 vs 声明取消→`succeeded`；各恰一行 `RESULT_OUTCOME_MISMATCH`），G4 gate 计数（`effect/test_effect.py`）**266 → 276**（G6 `tools/test_tools.py` 333 不变）；矩阵表头与 README 计数同步重立 | G14 D5/D6；A48/A71 先例 |
| A105 | G14 | D2 recovery 入口与 unknown sibling 向量的 generation 形态：以**直插 `status='failed'` 的 generation 行**（G12 gate 既有 insert 形状）绑定步，而非活体下线——活体下线必然 drain/收束该批次（terminal 或 blocked），使 takeover 无从处理该步；直插形态精确隔离「cohort generation 门」这一个被测面，且与 G12 `test_cohort_generation` 的既有先例同构 | G14 D2；G12 `test_cohort_generation` 先例 |
| A106 | G15 | 共享确定性 drain 核心落 **grant 段（位置 3）** 而非 drain 段：`v_workspace_fail_closed` 重构为参数化 `v_failure_drain_core(p_session_id, p_failure_code, p_reason, p_audit_key_value, p_audit_action)`（三分支 step 收束 + session terminal + 审计，code 参数化），INFRA 闭包 `v_infra_closure_effect` 同段落位；`v8/drain/v8_drain.sql`（位置 14）只承载受控入口 `v_fail_session`。理由：`v8/effect/v8_effect.sql`（位置 7）的四个 `DECISION_PLAN_INVALID` 拒绝点 MUST 同事务收束，而 effect 段库不含 drain 文件；函数体晚绑定使低段引用高段对象安全，反向不安全 | 计划 G15 所有权段「`v8/grant/v8_grant.sql`（`v_workspace_fail_closed` 重构为参数化共享核心）」；计划「SQL 加载序」段（drain 在 cancel 后）|

| A107 | G15 | `v_retry_stop_reason_cas` 的调用以 `IF to_regprocedure('v_retry_stop_reason_cas(uuid)') IS NOT NULL` 守卫（A51 先例）：retry 段（位置 10）晚于 effect 段（位置 7），effect 段库无该函数——故 `effect.retry_stop_reason` 的 §3.2.2 派生只在含 retry 文件的 stage 库落地（drain gate 断言之，effect gate 断言其余契约） | 计划 G15-D4「effect.`retry_stop_reason` 按 §3.2.2 有序分类派生」；A51 `to_regclass` 守卫先例 |

| A108 | G15 | `grant_ops_audit.action` 闭合集扩入 `'infra_drain'`（原集止于 `fork_session`）：INFRA 收束与 `ws_lost_drain` 同形、不同 code，需独立 action 以区分 | 计划 G15-D1/D2（收束审计）；G2 冻结 DDL 不动，该表为 G10 于 `v8_grant.sql` 创建、G15 拥有该文件 |

| A109 | G15 | 闭包 audit 行取 `effect_audit` 的**内部三元组全填**：`internal_op_kind='infra_closure'`（G2 预留值）+ `parent_command_id`（effect 拒绝点为 `p_command_id`；CAS 路径为 `'v_retry_stop_reason_cas'`）+ `internal_op_ordinal=0`——G2 冻结的 `effect_audit_internal_triple_all_or_none` 要求三者齐备或全 NULL | G2 冻结约束；计划「跨里程碑裁定」段「G15 只写 `effect_audit`（`internal_op_kind='infra_closure'`）」|

| A110 | G15 | `v_retry_stop_reason_cas` 的受控返回以**文本哨兵** `'INFRA_PROTOCOL_VIOLATION'` 承载（函数返回类型为 `text`，无法同时返回 outcome/code 对）；列保原值、两侧值经一条 `effect_audit` 行（reason=INFRA_PROTOCOL_VIOLATION、internal_op_kind=infra_closure）留存，重放无第二行 | 计划 G15-D7「返回值 = `INFRA_PROTOCOL_VIOLATION` 收束 outcome/code」——以实现可承载的形式落地 |

| A111 | G15 | **计数重立（A48/A71 先例）**：G4 由 276 增至 **297**（`DECISION_PLAN_INVALID` 两个断言块改写 + 四组合块扩展 + 重放幂等 + plan-reject 三形态各独立 fixture），G7a 由 255 增至 **258**（CAS 受控返回契约三断言）。G4 断言块原有「`dispatch_started` 零控制态」与「拒绝后无 mismatch audit 行」两条被新契约（`failed_terminal` + 一条闭包行）取代；plan-reject 块原在同一 effect 上连做三次拒绝，现改为每子用例独立 fixture（首次即终态化） | 计划 G15-D4/D7「G4/G7a 断言同步更新」（本 gate 交付项）|

| A112 | G15 | 共享核心的「重复/已终态」判据放宽为 `state IN ('completed','failed','cancelled')`（原 WORKSPACE_LOST 版为 `state='failed' AND failure_code='WORKSPACE_LOST'`）：满足 D5 父层 cause 优先级（已以 WORKSPACE_LOST 失败的 session 在收到非法标记 decision completion 时 MUST 保持原 failure_code）。对既有 WL 路径行为等价——其全部测试路径均落在 failed/WORKSPACE_LOST | 计划 G15-D5（父层 code 优先级）|

## B. 范围缺席（[LATER]，G6–G9 或后续里程碑消除）

- ~~seal 授权阶段与 generation 检查（`GRANT_DENIED`/`GENERATION_REVOKED` 两道门）→ **G12**~~（已交付：两条 seal 路径第一道门 + dispatch 第二道门 + cohort (3)/(4) 真实现，见 A91/A93/A95；G12 gate 165 PASS，全量 17 gate 零回归）。G9a 已落**最小** slice/grant stub（`slices`/`grants` + `v_grant_valid`，见 A56/A61），但 seal/dispatch 两道实时门的完整接入仍为 P1。
- ~~tools seal 第二路径与 tool effect 批次 → **G6**~~（已交付：`v8/tools/`，G6 gate 333 PASS）。
- ~~known_failure 结算（原拒 `KNOWN_FAILURE_LATER`）与 retry/retry_cohort_allocation/superseded 写入 → **G7a**~~（已交付：`v8/retry/`，G7a gate 255 PASS；`known_cancellation` 结算仍拒 `CANCEL_LATER` → G8）。
- ~~recovery 接管（六步原子流程、job lease guard、`dispatch_started -> ready` 边、recovery 入径 cohort 分配）与两入口对照联测 → **G7b**~~（已交付：`v8/retry/v8_takeover.sql` + `test_takeover.py`，G7b gate 127 PASS；`FORCE_JOB_TAKEOVER` 四 outcome、完整八位预锁/generation 死锁序 → 依赖 grant/§4，见 A34–A38）。
- ~~repair 命令与 closers/槽位受保护更新、canonicalizer 消费 closer、`blocked_unknown_effect` 恢复出边、混合 sibling 验收 → **G7c**~~（已交付：`v8/repair/`，G7c gate 214 PASS；接管处 known_success/known_cancellation 经 repair 升级闭环（A36 消解）；余量：槽位 create-when-absent（A41）、完整规则 3（A42 → G8）、terminal-session repair（A39 → drain 里程碑）、tools seal 消费 repair 持久化 decision identity 的端到端联测（repair 侧同构持久化已落地，LATER 注记））。
- ~~request_cancel 命令（粘性 latch、无活跃工作分支三窗口、pre-dispatch 双表原子同步、turn/end 派生、与 finish/dispatch 两序竞态）→ **G8a**~~（已交付：`v8/cancel/`，G8a gate 全绿）。~~取消收束族余量（`shared_cancel_closure` 五出口与 AG01 触发源收口 (α)(β)(γ)(δ)、cancel-wins 矩阵、`(β)` 已知结果处置、`known_cancellation` 结算、`v_aggregate_step` 规则 3、G7b 接管处 CANCEL_STICKY 结构 gate / G7c 最小规则 3 臂的替换）→ **G8b**~~（已交付：`v8/cancel/v8_closure.sql` + `cancel/test_closure.py`（G8b gate 222 PASS）+ 规则 3 落地于 `v8/retry/v8_retry.sql` 与四触发源接线；见 A48–A54）。**余量**：pre-dispatch 同步的另两触发源（WORKSPACE_LOST failure-drain / §4 generation drain）与 quiescing 下的受控边收束（driver 切换里程碑）；repair 的 `reconcile` 入口（未实现）。
- ~~流式 grammar 字段矩阵/计数 ABI~~ → **G9a**（已交付：`v8/stream/v8_stream.sql` + `stream/test_stream.py`（G9a gate 67 PASS）；字段矩阵五形态、计数边界、非 known_success 豁免、模式自报不改判定落地，见 A55–A61）。
- ~~`stream_progress` 五款状态门与 observation receipt 完整合同、等值断言时序（pending/窗口耗尽 `STREAM_INCOMPLETE`）、额外 index 冲突事实（F4）、`assistant/partial` 合成 → **G9b**~~（已交付：`v8/stream/v8_stream.sql` §5–§9 + `v8/stream/observation.py` + `stream/test_observation.py`（72 PASS）。落地五款门（`SESSION_TERMINAL`/superseded/`STREAM_CLOSED`/`REPAIR_REQUIRED`/接受）+ 两交叉 + quiescing 三向（含新增终局 `DRIVER_QUIESCING` gate，W01）、observation receipt 三元组 `(attempt_no, ordinal, payload_digest)`（Q04）与重发幂等/`IDEMPOTENCY_CONFLICT`、控制态零修改（AF01）、收齐集合精确相等（不物化 {0..N}）、等值生命周期 (a)/(b)/(d) 拆分 + 迟到 chunk 等值补齐（append 钩子）、F4 额外 index 三形态 + 空 payload、三表示对照、`assistant/partial`（unknown + sticky cancelled 有/无前缀）、S04 观测 payload 分层、双层 portable 验收器、窗口耗尽 `STREAM_INCOMPLETE`（接管）。**余量**：`reconcile` 入口接线（`OBSERVATION_WRONG_ENTRY` 结构注记）、跨 attempt 残留清理、真实 lease/超时窗口 sweep（本 stage 以 GUC `v8.stream_window_exhausted` 作可判定输入））。
- DECISION_PLAN_INVALID 的同事务 INFRA 收束（现为纯 receipt 拒绝）→ 随 failure-drain 里程碑。
- ~~chunk 六项归属校验第 (3)(6) 项（grant/身份链）、第 (2) 项已结算分支 → **G9a + 最小 grant stub**~~（已交付：第 (2) 已结算 attempt 臂、第 (3) stream_id 归属、第 (6) 三合取 + `v_grant_valid` 有效 grant 谓词含 slice 撤销传播，见 A56/A57/A60）。~~余量：§2.1 完整 grant/slice 模型（授权线性化点、seam 清单、RLS、operator 通道、`constraints` 参数级求值）→ P1~~。**G10 已划线四项**：授权线性化点（机制 (a) `v_grant_lock_judge` 固定锁序 + 两提交序裁定断言）、RLS（A69）、operator 通道（受控签发/撤销 + p_reason + 审计）、`constraints` 参数级求值（A62）已交付（`v8/grant/`，G10 gate 143 PASS；A56/A60/A61 消解、A62–A71 新记）。**未划线改记新偏差 A72**：seam 清单各同事务 grant 检查点（recall/fold/env_read/env_write/tool_resolve/authorize_effect）→ assemble 里程碑（Conformance 14 首句的 dispatch 半边照测、seam 半边随 assemble）。
- transport/payload 拒绝路径 (a)(b)(c) 与三键指纹（`rejection_fingerprint@v1`/`transport_rejection_key@v1`/`malformed_binding_fingerprint@v2`）、occurrences 子表 → audit 指纹里程碑。
- ~~canonicalizer 输入契约以事件 dict 携带控制信号（effect_status/decision_only/sticky_cancel/unsealed_tools_plan）~~ → **G7c 起由持久化控制态直接驱动**（G7c 测试层 `trace_events` 的 effect_status 信号一律自 `effect_requests` 持久化状态读回构造，不再手写；输入 dict 携带形式本身仍保留，后续里程碑收敛为查询层投影）。
- P0C（dsh-compat）真实 I/O 半边 → **依赖 pinned DSH Node host 身份（用户决策）**。G13 已交付 host 无关合同面（`v8/compat/`：`compat_unmapped_audit` 统一命令、adapter manifest 落库、P0C passed/failed/blocked 报告器、§5.1 十六行映射数据库层、DeepSeek 凭据门控协议层，G13 gate 153 PASS，见 A62–A69）；blocked 清单：§5.2 前置验证本体、全部真实 I/O 层子用例、最小 turn 双运行时同 observable trace、pinned 五件套实证、compat-only T0–T4 参加面实测、P0C 终签、真实 provider 凭据缺失环境（解锁链见 `v8/compat/pinned_host_manifest.json`）。

## C. 审查修复记录（已消解）

- P1×5：canonical float 域收窄（→A6）；driver/epoch guard NULL-unsafe（8 处改 `IS DISTINCT FROM`）；final_tools 分支 plan_hash 全量+plan 未持久化（→`steps.plan_canonical` + envelope `p_plan_canonical`）；heartbeat 卡死 open_turn_info（改用最后一个非 NULL turn_id 事件）；known_cancellation 分类缺失（判定表行 2 已实现，结算拒 `CANCEL_LATER` 属 B）。
- P2×7：`pg_temp` 批表限定；唯一索引转义按 (4)/(5) 重读分类；unknown 已结算 attempt 迟到 completion → `repair_required/REPAIR_REQUIRED`；`RESULT_OUTCOME_MISMATCH` 审计移至结算事务；`v_evidence_attempt_bound` 证据绑定；EffectResult 补 `step_id`（结构层比对）；claim 增 `p_expected_session_fence` CAS。
