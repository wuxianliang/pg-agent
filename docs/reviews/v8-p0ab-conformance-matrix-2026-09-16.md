# V8 Conformance 覆盖矩阵（P0A核心+P0B 基线）· 2026-09-16

对照 `docs/designs/v8-dev.md` §6 Conformance 最小集合 1–16。状态：✅ 已覆盖 / 🟡 部分 / ❌ 未开始 / ⛔ blocked（外部依赖）。依据 = 各 gate 测试报告（G1 191 / G2 86 / G3 130 / G4 266 / G5 250 / G6 333 / G7a 255 / G7b 133 / G7c 214 / G8a 93 / G8b 222 / G9a 51（test_grant_stub 迁出后重立基线，A71）/ G9b 72 / **G10 143 / G11 207** PASS）。**每完成一个后续 stage 更新此表。**

| # | 条目 | 状态 | 已覆盖内容（测试落点） | 缺口（去向） |
|---|---|---|---|---|
| 1 | append/create/complete 幂等、seq 无洞、首 attempt、发布即 ready、无持久化 planned | 🟡 | append 批次幂等/归并/冲突/expected seq 特例/双幂等并发/比较域/批内归并（G3）；seal 幂等、首 attempt=1、dispatch 不补建、无 planned effect/step（G4/G5）；白名单与 `EVENT_TYPE_RESTRICTED`、semantic ordinal 值域四 vectors、公开输入归属矩阵负向、semantic 必填负向（G3）；声明 hash/占用/键类型化 canonical 路径（G3/G4）；`prepare_step`/`seal_batch` seal receipt 重放（G6：初始 seal 同 command 重试、tools seal 同 command 重试 + 换 command_id 引用重放返回原 receipt、不一致 `SEAL_MISMATCH` 零副作用、sealed 成员/ordinal 不可变）；occurrence identity 场景 (4) seal slot 级（G6：同批同工具同参数两 slot → 两条独立 `tool/call` 事件、互异 event_key/seq）；tools seal 首 attempt 同事务/发布即 ready/CAS 拒绝零副作用（G6）；`retry_effect` receipt 幂等（G7a：同 command 重试重放、换 payload `IDEMPOTENCY_CONFLICT`、分配后重复 retry `EFFECT_NOT_RETRYABLE` 零副作用） | `repair` closer 幂等（G7c）；heartbeat 双拆分 (ii)–(vi)（attempt/heartbeat 未实现，LATER）；(a)(b)(c) 拒绝路径与三键（audit 里程碑）；`compat_unmapped_audit`（P0C） |
| 2 | dispatch snapshot/fence/接管/superseded | 🟡 | `STALE_JOB_FENCE` 零控制态、dispatch 绑定既有 attempt、envelope 四源读取注入（G4）；后继 attempt 分配经 `retry_cohort_allocation` 单入口（G7a：普通入口；recovery 入口 G7b 补两入口对照）；fence 验收前置于 attempt 状态处置——旧时代 completion 恒 `STALE_JOB_FENCE`、携带当前 fence 的被取代 attempt `ATTEMPT_SUPERSEDED` 仅 audit（G7a）；superseded 写后状态保留/write-once、分配事务 kill 回滚重跑收敛（UNIQUE(effect_id,dispatch_job_fence)）（G7a）；双表一致（parent 快照 == 当前最大 attempt 权威行）（G7a）；recovery 接管六步（G7b：`recovery_takeover` receipt 命令、预锁集可达子集 session→step→effect→attempt、job lease guard——session lease 过期 × job lease 有效跳过不动且后续合法 completion 照常采信、到期重扫接管、CAS 推进 `current_job_fence`+撤旧 lease+结算/分配一次提交）；接管后无新 attempt 分支 fence 拆分断言（current_job_fence 单调推进、attempt 冻结快照不变、旧 completion stale-reject 零控制态、两表快照域一致——控制 fence 不在快照域）；两入口对照联测（G7b：纯 eligible 批次普通 complete→retry_effect vs 接管出示同等绑定失败证据，分配数/三层终态/code 完全一致；单 effect 接管分配后 ready+waiting_effect 非 ready）；unknown/pending sibling 经 recovery 入口仅结算不分配；接管事务 kill 重跑收敛（分配支与 unknown 支，UNIQUE(effect_id,dispatch_job_fence)）；接管 defer 的 known_success/known_cancellation 经 repair 升级收束（G7c：fence 推进不改变待 repair 执行身份——接管 unknown 后绑定当前 attempt 的成功/取消/失败证据经 repair 正常采信落三终态，旧 attempt 证据冒充 `REPAIR_EVIDENCE_REQUIRED`）；接管 sticky 分支（G8b δ 触发源）：粘性 latch 下不再整命令拒绝 `CANCEL_STICKY`，接管事务内按共享取消收束落 `cancelled_after_dispatch`+`RETRY_SUPPRESSED_BY_CANCEL`、step/session `cancelled`/`CANCELLED_BY_REQUEST`、共享派生 turn/end，且与 (α) completion 入口三层结果/code 全等；quiescing 腿仍结构拒 `DRIVER_QUIESCING`）；**G10 grant 位 + G11 generation 位**：八位主锁序的 grant/slice 位以 `v_grant_lock_judge` 子操作交付（固定复合锁序 + 锁等待交错提交序断言；G12 全部门——seal/dispatch/cohort/接管八位预锁——消费同一实现）；generation 侧由 G11 交付——下线事务 session 全序预锁（→generation 排他→锁内重扫、发现新关联整体回滚重试、MUST NOT 倒序补锁）与共享 generation 检查子操作 `v_step_generation_status`（generation 行 FOR SHARE 共享读锁位）；grant 位先于 generation 位取得的全序接线归 G12 | `FORCE_JOB_TAKEOVER` 四 outcome（未实现，合同另行排期——grant/§4 模型均已就绪）、完整八位预锁锁序与 generation 死锁序断言（G12 接线后）、判定源独立性注入（parent 快照偏离/四源读取注入的 recovery 臂补测） |
| 3 | wait 与 completion 并发不丢唤醒、NOTIFY 乱序一致 | ❌ | —（P0B 无队列/NOTIFY；DB 扫描恢复已由 chaos 覆盖等价语义） | NOTIFY/队列唤醒层（后续） |
| 4 | （并入 3 的编号沿用规格原文） | — | — | — |
| 5 | known/unknown 正交、声明权威、混合 sibling | 🟡 | `known_success`/unknown 兜底分类、`RESULT_OUTCOME_MISMATCH` 不拒绝（G4）；unknown 唯一 provisional end、blocked（G4/G5）；正常「工具→再决策→无工具回答」中间态 `ready`（G6：非空 plan 成功 decision → step `ready,stage=decision`/session `ready` 不终态化；`final_tools` 批全成功 → `succeeded`/`closed` 后 session 仍 `ready` 非 `completed`，coordinator 创建下一 decision step，`decision_only` 关 turn 后才 `finish_session`→`completed`；`TURN_ALREADY_CLOSED` 负向）；known_failure 双要件判定表四用例（G7a：幂等无回执→unknown、非幂等+回执+无副作用证据+eligible→known failure 安全重试、仅有无副作用证据→unknown 兑底、非幂等无证据→unknown）与双要件证据持久化绑定 attempt；声明 succeeded × 失败证据 vector (i)（G7a）；聚合规则 4/5 + `retry_stop_reason` 有序分类全表（G7a：first_attempt_failure/budget_exhausted/not_retry_eligible、纯 eligible 批 step `failed_retryable`/session `ready` MUST NOT fail_session、规则 4 批含纯预算耗尽 session `failed` code 按 reason 集唯一派生、混合 sibling 残留收束 `RETRY_STOPPED_BY_CLOSURE`+`not_retry_eligible`）；纯 eligible 批次普通入口全链（G7a：cohort 全体 attempt_no+1/identity 复用/superseded 标记/ready 无窗口/重试后 waiting_effect/恢复成功终态）；repair 三终态与 `REPAIR_EVIDENCE_REQUIRED` 两负向（(iv) 仅无副作用证据、(v) 失败回执副作用不明）、旧 attempt 冒充负向、目标非 unknown 负向（G7c）；fence/取代区分（G7c：接管推进 fence 后同执行证据经 repair 采信成功收束；被取代 attempt 证据冒充不获采信）；预算耗尽 repair 派生 `FAILED_RETRY_BUDGET_EXHAUSTED`（G7c：attempt 2 unknown + max_attempts=2）；混合 sibling 验收（G7c：同批 unknown+failed_retryable → `blocked_unknown_effect` → repair 证实 unknown succeeded → 规则 5 收束 step `failed_retryable`/`FAILED_RETRYABLE`、session `ready`、MUST NOT 为已 repair effect 建新 attempt → `retry_effect` 恢复 → 终态与 outcome_code 按矩阵唯一）；repair→retry 恢复闭环（G7c）；取消族接线（G8a：`request_cancel` 无活跃工作分支三窗口收束 `cancelled`/`CANCELLED_BY_REQUEST`（含窗口 (iii) 既有 terminal step 终态与 `outcome_code` 不变）、sticky latch 首取消 0→1 后续幂等不重复递增、terminal session 返原终态不改 epoch；pre-dispatch 双表原子同步——`ready` effect 与其当前 attempt 同事务 `cancelled_before_dispatch`、迟到 completion 零控制态拒 `ATTEMPT_ALREADY_SETTLED`）；取消收束族（G8b）：`v_shared_cancel_closure` 五出口逐条（exit 1 retryable→`cancelled_after_dispatch`+`RETRY_SUPPRESSED_BY_CANCEL`；exit 2 terminal failure 原样保持；exit 3 success→`COMPLETED_AFTER_CANCEL`；exit 4 unknown 保持 `blocked_unknown_effect`（rule 1 优先）；exit 5 `known_cancellation` 保持 §4.3 code、不写两类 closure audit）、AG01 四触发源 (α)(β)(γ)(δ) 逐一 + 两入口一致性（同证据经 completion 与 takeover 三层结果与 code 全等）、cancel-wins 矩阵四行（sticky×success、sticky×failed_terminal→`FAILED_TERMINAL_CANCELLED`、provider→`CANCELLED_BY_PROVIDER`、provider×残留 retryable→`FAILED_TERMINAL_PROVIDER_CANCELLED`+`RETRY_STOPPED_BY_CLOSURE`）、`known_cancellation` 经 completion 结算（provider/request 两码 + 同 command_id 重放） | EffectResult 声明 vectors (ii)(iii)（G9 随流式/声明矩阵补） |
| 6 | cancel/compact 并发、锁语义、终态 abort | 🟡 | request_cancel × dispatch 两提交序（G8a：两命令均先锁 session 行、线性化点 = 提交序——cancel 先提交 → 后到 dispatch 稳定拒 `CANCEL_STICKY`、attempt 从未进 `dispatch_started`；dispatch 先提交 → attempt 保持 `dispatch_started`、后到 cancel 不回溯取消、step `cancel_requested`/session `waiting_effect`）；request_cancel × finish_session 两提交序（G8a：cancel 先 → session `cancelled`、后到 finish 零状态变化；finish 先 → session `completed`、后到 cancel 返原终态且不改 epoch）；**G8b 本 stage 可达部分**：cancel×completion 两序（completion 先→规则 5 批次，后到 cancel 经规则 3 sticky 收束 exit 1；cancel 先→规则 2 sticky `cancel_requested`，后到 completion 经 (α) 出口收束）、cancel×retry 两序（retry 先→新 `ready` attempt 被 pre-dispatch 双表同步取消；cancel 先→`retry_effect` 稳定拒 `CANCEL_STICKY` 零控制态）、cancel×repair 两序（repair 先→终态 no-op 收敛唯一 end；cancel 先→repair 收束后 sticky 仍胜、provider effect code 保持不改写）、sticky 收束后重复取消幂等（terminal 返原终态、epoch 不变、无第二条 end）、closure 事务 kill 重跑收敛 | compact 三命令与 LLM enqueue 并发矩阵（`COMPACT_BUSY_EFFECTS`/`COMPACT_IN_PROGRESS`/`STALE_COMPACT_FENCE`、终态事务 compact 受控 abort、compactions 表重放）→ **LATER**（compact 三命令未实现，P1/后续）；槽位行锁序位「断言」（G8b 以两提交序的结果收敛断言覆盖：cancel×repair 两序各收敛为唯一 canonical `turn/end`；显式锁序位探针留待并发探针里程碑） |
| 7 | 并行工具反序 ordinal 一致 trace | ✅ | 同批两 tool effect 以相反顺序 dispatch/complete（G6）：聚合终态相同（`succeeded`/`closed`+`ready`）、semantic 序（`dispatch_ordinal`）不变、`normalize()` observable trace 两序逐一相等、seq 均连续无洞；失败后反序重试恢复批（G7a：A 失败 B 成功与 B 失败 A 成功两序同收敛） | — |
| 8 | generation 刷新/下线/drain | 🟡 | **G11（`plugin/test_plugin.py` 207 PASS）**：refresh 失败不改变 active generation（构建任一环节失败——扫描/digest 校验/依赖解析/预加载——building→failed 终态化且 active 指针与已绑定 step 不受影响）；新 step 用新 generation、旧 step 用冻结 generation（steps 增 `catalog_generation` DEFAULT 种子代、历史回填不可区分；激活原子切换前代 retired、rollback 后续行 step 绑回退后 active）；未 ready worker 不得 claim（进程内 readiness registry：同 digest 幂等/异 digest 冲突、目录 active ≠ handler ready 负向）；成员关系断言（refresh 同键同 digest 复用全局不可变行仅加成员行、无第二行无 first_published 改写、变更插件新行指向新代、同键异 digest 稳定拒绝、两代 digest 覆盖各自成员全集且不同、GC 无引用随成员行删且共享行不删）；下线七条——(i) 未派发 drain（双表 `cancelled_before_dispatch`+`ABORTED_BEFORE_DISPATCH` 审计+`GENERATION_REVOKED` 上下文、重扫至无残留、迟到 completion 拒 `ATTEMPT_ALREADY_SETTLED`、未密封 plan 作废留审计）；(ii) in-flight 持快照完成、session 不因下线立即失败；(iii) 指针两分支（operator 显式回退/置空版本化持久化 + 置空分支「claim→assemble 拒 `NO_ACTIVE_GENERATION` 零控制态→显式 yield 回 ready」）；(iv) 三合取 step 收束 `failed_terminal`/`GENERATION_REVOKED`、sticky cancel 优先不改写、混合成员（A drained + B unknown→repair budget_exhausted）规则 4 派生 `FAILED_RETRY_BUDGET_EXHAUSTED` 不被覆盖；(v) code 唯一（drain 复用 `ABORTED_BEFORE_DISPATCH`+审计记 `GENERATION_REVOKED`，step/session 层仅三合取分支用之）；(ix-a) 下线锁序安全（全序预锁→generation 排他→锁内重扫→发现新关联整体回滚重试、不倒序补锁，GUC seam 确定性重测）；(ix-b) 收敛（有限次重试后无残留）；(x) 下线事务入口 REPEATABLE READ 稳定拒 `ISOLATION_UNSUPPORTED` 零副作用 + READ COMMITTED 对照；retired 不受限负向；generation digest SQL==Python 字节级 ≥3 组手算 golden；依赖解析稳定全序（UTF-8 字节序不依赖 locale、dotted-numeric、环/重复 provide/版本不匹配整代拒绝） | (vi) 下线×seal 竞态、(viii) 下线×retry allocation 竞态与分配拒绝后最终聚合两段 → **G12**（seal/dispatch/cohort generation 门接线——G11 已交付共享检查子操作 `v_step_generation_status`）；(x) recovery 接管/`FORCE_JOB_TAKEOVER` 入口隔离拒 → G12/G10（`FORCE_JOB_TAKEOVER` 合同本身另行排期）；dispatch 派发门 `GENERATION_REVOKED` 只读拒 → G12 |
| 9 | authorization 失败封闭 | 🟡 | G9a 最小 grant stub：`slices`/`grants` DDL（UNIQUE(workspace_id,name)、kind 五值/subject_kind 四值/capability 十三值闭合、复合 FK 租户一致性、spec/constraints 不可变触发器、revoked_at 单调）、`v_grant_valid` 有效 grant 谓词全合取逐项（含 **slice 撤销传播**）、chunk 六项第 (6) 项三合取负向（缺 event_append / 持 stream_ingest 缺 event_append / 非 owner 调用方）与第 (3) 项外来 provider 流负向（`CHUNK_ATTRIBUTION_INVALID` 零落盘）（G9a，断言块 G10 迁移后由 G10 gate 复盖）。**G10 完整模型**：`constraints` 六维参数级求值各 ≥1 负向（path 前缀外/命令白名单外/目标外/TTL 超/max_bytes 超/max_rows 超 + 在场约束缺参 fail-closed）、合取项 3 三向量（目标资源不属于 slice.spec → GRANT_DENIED、四 subject kind 经 grant/driver 链完整解析（plugin_identity 经声明 caller subject、kind 感知负向、声明 driver/epoch 不符 fail-closed）、同租户异 slice 资源仍 GRANT_DENIED——RLS 不替代 slice-membership）、受控撤销（p_reason 必填 + 同事务 audit、不存在/被拒、撤销后重签 = 新 grant_id、slice 撤销等效检查级联）、授权线性化点机制 (a)（FOR UPDATE 固定锁序 (workspace_id,slice_id,grant_id) 升序、撤销先提交→其后检查 GRANT_DENIED、授权/dispatch 先提交→撤销不追溯取消已 dispatch_started attempt、锁等待交错构造提交序断言）、RLS（workspace_id 行级隔离 + worker 无业务表 DML 仅 capability function + SECURITY DEFINER 逐函数审计 + NOBYPASSRLS/worker 直连禁 SET ROLE）、operator 通道（受控签发/撤销 + 原因 + 审计）、session/heartbeat event_append 授权门（无有效 grant（含已撤销）/跨租户 → GRANT_DENIED：授权前置拒绝——receipt 命名空间外（零 receipt/binding 写入）、独立授权拒绝审计行、同上下文重发幂等合并）、capability API 边界数据库层半边（凭证不进 session_events payload）（G10） | seam 清单各同事务 grant 检查点（→ assemble 里程碑，偏差 A72；`#9` 本缺口保留不划）；两阶段 manifest 快照 → G12+；seal/dispatch/cohort 两道实时门接线 → G12 |
| 10 | chunk/canonicalizer/golden vectors/流完整性 | 🟡 | canonical golden vectors 大集（JCS/转义/边界/时间/键派生跨实现一致，G1）；chunk 四元组合并/乱序/同异内容/归属校验 (1)(2)(4)(5)/值域边界四点（G3）；normalize 唯一性/到达序无关/失败封闭（G3）；`closer_event_key@v1` SQL==Python 字节级交叉核对 ≥3 组独立手算期望（G7c）；**G9a：session_events 流列 CHECK 负向（chunk 缺列/非 chunk 带列/越界/ordinal 误用）、chunk 六项 (2)(3)(6) 补全、乱序 2,0,1＋值域边界四点、四元组派生 SQL==Python 字节级＋幂等/冲突、`stream_complete` 字段矩阵五形态各用例、计数边界（N=2^63-2×C=2^63-1 并存 / C=2^63-1 单 / N=2^63-1 拒 / C=2^63 拒）与矛盾/负值/非整数、非流式成功与流式 known_failure 两对照、模式自报不改判定（G9a）；**G9b：五款状态门五分支各一（`SESSION_TERMINAL`／superseded `rejected_stale`／attempt 四终态 `STREAM_CLOSED`／unknown 与窗口耗尽 `REPAIR_REQUIRED`／接受）+ 两交叉（terminal×窗口耗尽→gate 1 优先、attempt 终结×窗口到期→`STREAM_CLOSED` 优先）+ quiescing 三向（非终局观测接受、终局 `DRIVER_QUIESCING`、语义 append 负向）；observation receipt 三元组 `(attempt_no, ordinal, payload_digest)` + 同 command 重发幂等（同 ordinal、无第二事件）+ 换 payload `IDEMPOTENCY_CONFLICT` + 观测各归各 binding 无跨 command 覆盖 + 控制态零修改（effect/attempt/session 全保持）+ 中间后终局衔接；等值生命周期（(b) final 先到 pending→正确尾部补齐无冲突、矛盾尾部记冲突且业务终态不可改写、两序业务终态一致）+ final/chunk 等值失败结算记冲突 + F4 额外 index 三形态（超前／结束事实后／验证完成后）与空 payload 同判据 + 三表示对照（仅 N／仅 C／并存判定一致）+ 双层验收器（append 层冲突事实与等值失败事实各判 portable 失败、无事实通过）+ 归提前提（被拒 chunk 不入合并/冲突/流事实）；窗口耗尽 (α) 流式无终局接管→`unknown_outcome`+`STREAM_INCOMPLETE`+partial、(β) completion 先到尾部耗尽→终态不变/等值 pending/无冲突无 `STREAM_INCOMPLETE`；`assistant/partial`（unknown 与 sticky cancelled 有/无前缀对照、provider 既有）；observation payload 分层（`stream_progress` 不入 normalize 输出、count 字段非流集合事实）** | 跨 attempt 残留清理、`reconcile` 入口接线（`OBSERVATION_WRONG_ENTRY` 结构注记）、真实 lease/超时窗口 sweep（G9b）；指纹三键 golden（audit 里程碑） |
| 11 | fork 切点/切换正负向 | ❌ | — | 后续（fork/switch 里程碑） |
| 12 | deterministic fake LLM/fake clock/seeded | ✅ | FakeLLM 固定种子、固定 locale 时区由 PG 统一（G5）；FakeTool 确定性执行（G6：输出与回执 id 由 slot 身份派生，chaos 重跑字节级复现） | 真实 provider 认证用例（P0C+） |
| 13 | yield 换 worker/checkpoint/WORKSPACE_LOST | 🟡 | yield 释放/fence 递增/旧 fence 拒绝、fresh-process 收敛（G5） | workspace_handle/checkpoint/`WORKSPACE_LOST` fail-closed（后续） |
| 14 | grant 线性化/revoke×dispatch | 🟡 | **G10**：授权线性化点机制 (a) 交付——`v_grant_lock_judge`「锁定+判定」子操作（FOR UPDATE 固定锁序 (workspace_id,slice_id,grant_id) 升序、slice 行先于其 grant、嵌于 session→grant/slice→…主锁序、G12 全部门复用同一实现）；revoke×check 两提交序（撤销先提交→其后授权检查见 revoked 返 GRANT_DENIED）；check/dispatch×revoke 两提交序（授权+dispatch 同事务先提交→其后撤销仅翻 grant 行、已 dispatch_started attempt 不被追溯取消——收束只经 fence/lease 与 completion/repair）；交错并发按锁等待构造提交序断言（T2 revoke 阻塞于 T1 持有的 grant 行锁、T1 提交后 T2 落地、attempt 终态不回改）；post-revoke 新检查再拒（G10） | dispatch/seal/cohort 实时门本体（消费同一子操作）→ G12；manifest 快照不豁免 → G12；seam 检查点半边 → assemble（A72） |
| 15 | 取消/unknown fixture 字节级、reducer 资格 guard | 🟡 | reducer 单元级：reason 优先级、sticky>provider、资格 guard、before/after 二分、全 pre-dispatch（G3 canonicalizer）；「先 unknown 后 repair」只追加重算 fixture（G7c）；取消路径接线后的 fixture 字节级断言（G8a：request_cancel 无活跃工作分支三窗口各自收束——窗口 (i)/(ii) 无 turn 时不产 turn 事件、窗口 (ii) 已开启 turn 且无 work → 唯一 `turn/end {interrupted:true, reason:cancelled_by_request_before_dispatch}`、窗口 (iii) `final_tools` 亚类（存在曾进入 dispatch 的 effect）→ 唯一 `cancelled_by_request_after_dispatch`、turn 关闭亚类不改写既有 known end；slot 已知行 head = 事件键、`normalize()` 输出与之逐一一致；pre-dispatch 双表同步（effect+attempt 同事务 `cancelled_before_dispatch`、迟到 completion 零控制态拒 `ATTEMPT_ALREADY_SETTLED`、`ABORTED_BEFORE_DISPATCH` 可查）；终结资格 guard 负向（pending sibling → 无已知 end，G8a）；混合取消 turn fixture（G8b）：同一 turn 内 provider 证实取消 + sticky 收束 → canonical 仅含唯一 `turn/end {interrupted:true, reason:cancelled_by_request_after_dispatch}`（sticky 优先 provider）、MUST NOT 出现第二条 `turn/end` 或 `cancelled_by_provider` reason（persisted 事件亦无第三条）；provider-confirmed cancel-after-dispatch fixture（repair 与 completion 两入口同款唯一 `cancelled_by_provider`）；cancel-after-unknown fixture（唯一 provisional `turn/end {outcome:unknown, reason:unknown_after_dispatch}`、无已知取消语义；sticky latch 下 unknown 结算仍落 rule 1 blocked）；全 pre-dispatch 取消（唯一 `before_dispatch`）；终结资格 guard 两负向 (a) provider-cancel + in-flight sibling → 无任何已知 end、sibling 收束后 guard 重新求值，(b) 带 tool calls 的 decision 缺 final → `assistant/partial` 留存、无 `turn/end`；§4.3 code 映射与 closure 家族/摘要单元断言 | finish×尾部 chunk 竞争（并发探针里程碑）；槽位显式锁序位探针（并发探针里程碑） |
| 16 | compat blocked 矩阵/P0C 报告 | ⛔ | — | P0C（Node host 未确认） |

注：编号 3/4 按规格原文实际条目排布（原文第 3 条 wait/NOTIFY、第 4 条空缺合并）；矩阵以语义对照为准。
