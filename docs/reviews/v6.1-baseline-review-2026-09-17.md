# v6.1 PLAN ONLY — Iteration 1 baseline review

Date: 2026-09-17. Status: **baseline established; NOT approved; no fixes started**.

## Result / authority

Independent Oracle reviewed the entire unchanged 365-line plan plus the complete evidence packet and selected actual sources. Explicit verdict: **P0=0 / P1=12 (all OPEN)**; P2=3. This does not satisfy the parent goal of P0=0/P1=0. No issue has been closed this turn.

- Plan: `docs/plans/v6.1-code-file-workbench-plan-2026-09-17.md`
- SHA-256: `7e02eaafb46e9bc7fef29be280df124dec55dd2c2cb5f526052098a2eb17a5d5`
- Full independent response: `prompt-exports/oracle-chat-2026-09-17-213010-new-chat-dd6be3-4b2f.md`
- Oracle chat: `new-chat-DD6BE3`; mode `chat` (static full-plan review, not working-tree diff review).
- Traceable evidence/parent selection guide: `docs/reviews/v6.1-baseline-evidence-2026-09-17.md`
- Exact native commands, raw stdout/stderr and exits: `prompt-exports/v6.1-baseline-native-evidence-2026-09-17.md`
- Reproducible read-only capture script: `prompt-exports/v6.1-baseline-native-collector-2026-09-17.py`

Frozen six-part L4 rubric is reproduced in the evidence packet without modification. Oracle confirmed the full evidence packet was included. It reviewed local-upstream excerpts in that packet, not independent outside-root file reads or installed binaries. Coordinator directly spot-checked primary source. This is **L4 static design review + local static boundary checks**, NOT L1/L2 runtime verification. No stage/test/gate was run or claimed green.

## Complete stable P0/P1 ledger

**P0: none confirmed.** All rows below are **P1 / OPEN**. `P` means the target plan's canonical filesystem line numbers; `E` means the evidence packet above. Source paths without a root prefix are relative to `/Users/wxl/Projects/pg-agent`; `sitting_duck/...` and `tigerfs/...` are under `/Users/wxl/Projects/` and captured in E/raw evidence. Full scenarios and future positive/negative tests are preserved in the Oracle export. These concise acceptance criteria are plan-level closure requirements, not implementation accomplished this turn.

### V61-B001 — workspace/file identity and run authorization
- **Plan:** P223–226,237,251,265–273,321.
- **Evidence:** `v6/duck_tools/duck_tools.sql:7–34` only supplies current-run enqueue context; `tigerfs/docs/spec.md:1296–1337` gives UUID/parent/filename file-first identity rather than universal path PK.
- **Failure:** identical relative paths in different workspaces can collide; caller-supplied workspace is not proof of run authorization.
- **Acceptance:** freeze canonical workspace/object/path identity before schema, including rename/delete-recreate semantics; bind run read/write grants to authoritative identity. Require same-path two-workspace positive test and cross-run/workspace, alias/traversal negatives; mount mode additionally needs symlink/escape tests.

### V61-B002 — worker role prohibition contradicts required state transitions
- **Plan:** P63–68,230–233,243–244.
- **Evidence:** `v6/queue_bridge/duckdb_processor.py:131–191`; `v6/session_durability/duckdb_runtime.py:121–203,239–291`; `v6/queue_bridge/duck_queue.sql:357–458`; `v6/kernel_freeze/worker.py:411–477` all require control-state writes.
- **Failure:** copying these paths under a role forbidden from state writes fails; granting broad DML to make them work breaks the stated boundary. Merely using two connections is insufficient.
- **Acceptance:** name authorities and restricted command interfaces for claim/session/apply/resume/archive/DLQ/file mutation/revocation; define invoker/owner boundaries. Positive least-privileged full cycle; negative direct state DML, forged/cross-run claim and apply.

### V61-B003 — product write approval is not execution authorization
- **Plan:** P18,243,268–277,281–283,320–321.
- **Evidence:** `v6/queue_bridge/duck_queue.sql:35–36`; `v6/duck_tools/duck_tools.sql:44–73`; `v5/prompt_pipeline/prompt_pipeline.sql:53–118`: capability metadata/live prompt visibility do not specify an execution authorization check.
- **Failure:** approval to build the feature could be mistaken for permission for every run to write. Revocation while queued and whether render persists bytes are unspecified; D5 says run read-only while mandatory tools write.
- **Acceptance:** distinguish feature approval from scoped authority (principal/workspace/action/content scope/lifetime/revocation), default deny; classify every persistent entry including render. Gates: authorized writes succeed, unapproved/read-only/out-of-scope/revoked queued requests have zero side effects even via direct calls. D4 remains user-owned; no approval invented.

### V61-B004 — SELECT-only validation does not define extension read authority
- **Plan:** P125–129,230–233,252,272,192,281–283.
- **Evidence:** `v6/dialect_guardrails/duckdb_validation.py:8–16,29–80`; `v6/session_durability/duckdb_grammar.py:241–285`; `sitting_duck/src/sql_macros/duck_blocks.sql:304–321`; fork `src/main/config.cpp:924–960`.
- **Failure:** legal SELECT/native functions/macros can request file reads outside the intended artifact policy; a directory allowlist is not run authorization. This is a missing policy, **not a demonstrated sandbox exploit**.
- **Acceptance:** define allowed relations/functions/macros/dynamic relation resolution, separate trusted ingress from arbitrary SQL/block_query; fail closed on unvetted native/indirect reads. Positive artifact chain; negative direct/quoted/qualified/macro/dynamic unauthorized reads, including another workspace within allowed prefix. Selected release/extension behavior must be tested.

### V61-B005 — no expected-version CAS or authoritative byte/generation contract
- **Plan:** P27,159–166,174,225,268–270,307.
- **Evidence:** `tigerfs/docs/spec.md:749–760,3398–3413` states per-operation autocommit/last-write-wins; `v6/duck_tools/duck_tools.sql:7–34` has no file expected-version contract.
- **Failure:** two runs each read revision 7 then overwrite/patch; per-run op_seq and atomic UPDATE do not prevent silent lost updates. Mirror generation may lag mount writes.
- **Acceptance:** define exact hash bytes/codec, expected version/hash, create/overwrite/patch/remove semantics, atomic content+hash+generation, external mount-write participation, delete/recreate ABA and dependency checks. Positive one-version increment; negative concurrent runs/external writes/stale patch-remove/multiple patch matches cannot silently overwrite or partially mutate.

### V61-B006 — file side effects lack durable idempotency and stale-worker fencing
- **Plan:** P14,35,243–244,291.
- **Evidence:** `v6/queue_bridge/duckdb_processor.py:112–191,193–284` commits claim before execution and memoizes results in process; `v6/queue_bridge/duck_queue.sql:357–458` deduplicates metadata results; `v6/kernel_freeze/worker.py:411–477` applies/archives later.
- **Failure:** file commits, worker dies before apply, retry repeats patch or falsely reports failure. Expired old worker can resume after takeover without attempt fencing. CAS alone cannot identify prior committed success.
- **Acceptance:** stable request/digest binding; durable mutation receipt with specified atomic commit/recovery relationship; attempt/lease fencing at mutation and result acceptance; define DLQ/termination handling after committed file effects. Future crash gates before write, after file commit/before apply, after apply/before archive must yield one logical mutation and the recorded result; stale writers/results rejected; ordering retries not terminally archived.

### V61-B007 — replay does not pin immutable source/config/dependency revision
- **Plan:** P57,65,187,215,238,287–288.
- **Evidence:** `v6/session_durability/duckdb_runtime.py:239–291` re-snapshots source and checks SQL hash; `v6/queue_bridge/duckdb_processor.py:193–224` hashes source column description, not file bytes.
- **Failure:** a file changes after registration; restart silently re-parses latest bytes under the old artifact name and SQL hash. Same-input two-call determinism does not prove revision replay.
- **Acceptance:** bind source revision/hash, language/options, DuckDB/extension/schema manifest and dependency generations; retain snapshots/input or reject unavailable revisions; define invalidation vs snapshot semantics; hydration never re-executes file writes. Gates: exact-version restart success; changed/deleted input/config/dependency refuses false restoration with no write side effects.

### V61-B008 — mandatory 19-column gate uses wrong source helper
- **Plan:** P83,214,304.
- **Evidence:** `sitting_duck/src/parse_ast_function.cpp:22–69,176–189` calls dynamic schema; `src/unified_ast_backend.cpp:356–361` is static helper, whereas `:654–716` adds/omits fields by config.
- **Failure:** correct selected-version public API may fail the wrong fixed-19 oracle, or implementation may discard required downstream fields. No installed-binary column count is claimed.
- **Acceptance:** version+explicit-option schema manifest, necessary column order/types and downstream fields, bound into artifact/replay identity. Positive actual selected API description; negative missing/type/config drift rejects bootstrap/register rather than silently accepting.

### V61-B009 — AST→blocks convenience API reads paths, conflicting with default row-only path
- **Plan:** P32,85,159–166,271–272,288.
- **Evidence:** `sitting_duck/src/sql_macros/duck_blocks.sql:304–321` calls `read_ast`; `:69–95` provides artifact-table `_from` form; `:47–62` requires full peek for complete bodies; `parse_ast_function.cpp:32–36` defaults to smart peek of 120; list wrapper `duck_blocks.sql:333–352`.
- **Failure:** default deny-file-access plane invokes path wrapper; a naive artifact replacement can still render truncated long bodies.
- **Acceptance:** name full content/artifact-only call chain without enabling mount fallback, complete-source config, block schema/order/list aggregation/renderer contract. Selected-binary positive test under disabled external access with long Unicode/multiline bodies and byte-accurate render→write; negatives reject path access and incomplete-source artifacts. Renderer implementation itself remains unverified locally.

### V61-B010 — write/patch content transport and retention undefined
- **Plan:** P59,237,268–269,276.
- **Evidence:** `v6/duck_tools/duck_tools.sql:7–34` stores full p_payload in ledger and merges it into PGMQ.
- **Failure:** copying enqueue template includes content/old/new in queue and DLQ; omitting them leaves worker without a specified content source. S3's no-content rule directly covers registration, but no separate write transport contract resolves this.
- **Acceptance:** controlled immutable content reference or explicitly bounded approved transport; define storage/authorization/digest/length/retention and allowed queue/ledger/DLQ/log/result fields. Bind grant/CAS/receipt to identical content. Positive retry can fetch same authorized bytes; negatives changed/expired/cross-run/oversized refs rejected; inspect queue+DLQ for policy-compliant absence of raw content.

### V61-B011 — stage/load and independent-worker lifecycle gates are not closed
- **Plan:** P55,180–202,212–219,226,237–244,281–283.
- **Evidence:** `v6/load.py:20–42,45–74,96–109`; `v6/kernel_freeze/worker.py:25–38,207–220,286–320,411–477`; `v4/plugin_taxonomy/plugin_taxonomy.sql:80–166`.
- **Failure:** S2 replay depends on materialization not decided until S3; S3 registration precedes S4 queue; P1 required to freeze op set without listed deliverable; 5 added SQL files/cutoffs not identified. Missing retry handling can archive a waiting request. This does not mean a separate worker or fixture solution is impossible.
- **Acceptance:** enumerate new SQL paths/load/refresh/stage cutoffs, explicit early low-level fixtures/dependencies followed by true integration, frozen op manifest; define queue ownership/retry/terminal/apply/resume/archive and PG-backed capability publication without mixed DuckDB imports. Future empty-stage DB gates plus real cross-process FakeLLM chain; retry not apply/archive; DLQ terminates; old files/dual-env identity checked.

### V61-B012 — end-to-end byte/native-resource/error budgets unspecified
- **Plan:** P30,34,188,193,239,265–273,302.
- **Evidence:** `parse_ast_function.cpp:62–67,187–189` proves parameter parsing/registration only; `v6/session_durability/duckdb_runtime.py:56–69` has bounded query fetching/interrupt; `duckdb_grammar.py:241–285` sets DuckDB memory limit, not all Python/native/file allocations.
- **Failure:** psycopg2 fetches oversized content before parse cap checks; full peek, block aggregation or rendering grows before final result truncation; errors lack required retry/terminal classification.
- **Acceptance:** explicit units and service ceilings for fetch/parse/query/search/blocks/render/write and aggregate scope; bound before allocating content; outer native-execution stop strategy; offset/limit semantics, truncation flags, post-timeout state and redacted error Types/retryability/request IDs. Positive boundary Unicode/encoding cases; negative oversized fields/node explosion/native timeout/results and zero/negative/omitted caps cannot bypass limits or leak content.

## Unpassed prerequisites, user decisions, P2 — not mixed into P1 count

Future prerequisites (all NOT RUN): actual ABI LOAD classification; single released DuckDB+all required signed extensions compatibility/hash manifests; installed-binary/source correspondence; TigerFS mapping/codec and authorized mount lifecycle; actual budget/cancellation enforcement; all required plugins' content-only APIs and composition; separately authorized isolated time-boxed fork alternative if selected. A fail-closed future probe is not itself a confirmed defect.

User decisions D1–D6 remain pending: plane architecture, row/mount modes, one/two plane choice, destructive capability approval, workspace sharing/permissions, optional plugin scope. Do not remove mandatory writes to manufacture an all-green review. D4 decision is distinct from B003 execution authorization.

Oracle's P2 items:
1. P129: directory exception is looser than deny-all, not stricter.
2. P19: K1+P1+S1–S10 totals 12, not 11; §3/D4 sequencing vs §7/D4 approval numbering ambiguity.
3. P185,216 vs P301: mount lifecycle required at P1 versus risk table claiming mount only in S9. Row-only core gates and full product mount acceptance must be distinguished.

## Exact source context for parent selection

Select full plan (1–365), full evidence packet, and this ledger. For load-bearing actual source:
- `v6/load.py:1–113`
- `v6/duck_tools/duck_tools.sql:1–73`
- `v6/queue_bridge/duckdb_processor.py:112–284`
- `v6/queue_bridge/duck_queue.sql:1–40,169–232,357–462`
- `v6/session_durability/duckdb_runtime.py:56–69,121–203,239–291`
- `v6/session_durability/duckdb_grammar.py:19–21,151–189,241–285`
- `v6/kernel_freeze/worker.py:25–38,207–220,286–320,411–477,553–584`
- `v6/dialect_guardrails/duckdb_validation.py:1–80`
- `v6/dialect_guardrails/duck_prompt.sql:1–22`
- `v4/plugin_taxonomy/plugin_taxonomy.sql:80–166`
- `v5/prompt_pipeline/prompt_pipeline.sql:53–118`

Local upstream path spans and HEADs are in evidence packet. Its excerpts avoid loading entire external repositories. Raw capture C2–C6 provides exact printed primary-source lines.

## Commands, boundary verification, artifacts

Only read-only native Python source/hash collection and git `rev-parse`, `ls-files`, `status --short` were used; no project modules imported. Each interactive C1–C7 command exited 0. Exact script bodies, rerun commands and full raw outputs are retained in native evidence. Capture invocation (run initially and again after final review creation):

```sh
python3 prompt-exports/v6.1-baseline-native-collector-2026-09-17.py > prompt-exports/v6.1-baseline-native-evidence-2026-09-17.md
```

Terminal stdout/stderr: empty. Initial capture exit 0; final capture verification is reported in the task tool transcript. This is a read-only collector with output restricted to the allowed review-artifact path, NOT a gate script. Web calls returned no usable content; no internet-based claim used as evidence. Children did not provide complete command transcripts, so their unverified command claims are not counted as independently reproduced commands.

Artifacts created this turn only:
1. this review ledger;
2. `docs/reviews/v6.1-baseline-evidence-2026-09-17.md`;
3. native collector under prompt-exports;
4. native output under prompt-exports;
5. Oracle response export under prompt-exports.

No plan/product/root dependency/loop memory edit. Initial/final fingerprint comparisons cover plan, memory, root AGENTS, root pyproject/lock and all 163 tracked v1–v6 files. Raw C1 prints the exact hashes; their unchanged values are preserved in E. Git status should show only the two new untracked review Markdown files beyond the same five pre-existing untracked documents; ignored prompt-exports do not appear there. No commit/push.

## Delegation audit and next narrow objective

Completed and collected: v6 lifecycle/load probe, replacement authorization probe, replacement API signature probe. Earlier authorization probe cancelled, broad upstream probe failed 451; both terminal. All five child IDs and dispositions are in E. Rejected erroneous claims include 20-file count, mandatory old-worker edits, static SQL routing, unknown artifact→view replay, SECURITY INVOKER owner rights, CREATE SECRET bypass, missing parse caps, and invalid macro line 4971. They do not enter the baseline ledger.

**Next recommended objective (not started):** docs-only revision of the foundational contract for **B001–B003**: canonical workspace/object identity, run permissions, authority matrix/restricted state transitions, and default-deny destructive authorization. Preserve pending user choices explicitly and do not enable writes. These contracts provide inputs for later CAS/receipt/replay and stage revisions. Parent should carry all 12 IDs unchanged and use the same full-plan L4 rubric after revision, not a narrower passing criterion.

Stop here: baseline complete; no fixes, no next-stage implementation, no loop memory update.
