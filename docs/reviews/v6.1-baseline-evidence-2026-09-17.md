# v6.1 PLAN ONLY baseline evidence packet — 2026-09-17

## Scope and verification level

Target: `docs/plans/v6.1-code-file-workbench-plan-2026-09-17.md`, full 365 lines, SHA-256 `7e02eaafb46e9bc7fef29be280df124dec55dd2c2cb5f526052098a2eb17a5d5`.

Iteration 1 establishes a baseline only. NO plan/product/memory edits, dependency installation, DB execution/reset, provider invocation, mount/unmount, commit/push, or upstream mutation. Static reads are not L1/L2 verification. This packet is evidence and review orientation, not a replacement plan or a claim that future gates passed.

Frozen L4 check (verbatim from parent memory line 5):

> L4 独立 Oracle 审核完整最新版方案及可追溯证据，逐项评估 (1) v6 事实与零修改边界、独立环境/ABI 可行性，(2) workspace/run/角色隔离、SQL/文件读取安全、写能力批准，(3) 内容/hash/generation/并发 CAS、幂等、租约/崩溃重试、重放，(4) 阶段依赖、装载顺序与 gate 可执行性，(5) 上游 API/预算真实性、错误/预算合同，(6) 文档内一致性与验收覆盖。P0=可能导致灾难性数据损坏/权限突破的设计缺陷；P1=核心功能不可实现、重要安全/一致性漏洞或缺失阻塞性合同/gate。最终需显式 P0=0 P1=0 且既有问题逐项关闭有证据。不得降低评级、移出必需功能或把未验证猜测写成事实来通过。

A legitimate fail-closed future feasibility probe is not automatically a confirmed defect. The target is explicitly unimplemented. Do not classify absence of v6.1 implementation as a defect. Missing or contradictory contracts for mandatory functions can be defects independently of implementation status. User D1–D6 choices remain unapproved.

## Evidence quality / probe disposition

Three workstreams were delegated read-only. One authorization session was cancelled before a report; one broad upstream session failed with provider error 451. Replacement focused probes finished. All five children are terminal: completed `8EACF8F2-3DC0-429E-B48D-AB5EF578F830`, `99D640A2-D0F0-4F49-B71D-F2F096CFF8BE`, `F5327761-B6DD-4D03-ABEC-27980A9E9599`; cancelled `0A353652-E174-4FDE-A6C3-5B3157221A89`; failed `993F7192-C880-4BB8-A5EB-E9D08A66FBCD`.

Probe reports contained material factual errors; they are NOT accepted verdicts and their proposed P0/P1 counts are discarded. Coordinator checked load-bearing facts directly. Specific exclusions:

- v6 loader has **21**, not 20 SQL entries (last is `duck_prompt.sql`). Plan's 21-file baseline is correct.
- Plan explicitly specifies a new independent worker (lines 15, 143–155 and table line 68); fixed v6 Python polling does NOT prove v6 edits required.
- SQL `apply_queue_result` uses `plugin_bindings` dynamically, not a hardcoded Python-style router.
- `hydrate` filters pending rows to `kind == 'view'`; it does NOT treat every unknown kind as a view. New codefs tables/session manager are intended; no ALTER of v6 artifact-kind CHECK is required by the plan.
- `SECURITY INVOKER` uses caller authority, not function-owner authority. No runtime authorization test was executed.
- Existing validator rejects multiple statements and non-SELECT outer statements and includes `CREATE`, `DROP`, `SECRET`; claimed `CREATE SECRET` bypass is false.
- `parse_ast` does register all three named resource caps. Do NOT report their absence.
- `ast_to_blocks` macro is at local lines 304–321, not a child-reported line 4971 (file length is 353).

## Coordinator-verified v6 source anchors (parent/Oracle selection)

All paths below are relative to `/Users/wxl/Projects/pg-agent`.

| Topic | Exact source spans | Verified facts / review relevance |
|---|---|---|
| Inheritance and refresh | `v6/load.py:20–42,45–74,96–109` | 17 v3/v4/v5 + 4 v6 inputs = 21; per-file refresh in `REFRESH_AFTER`; stage cutoffs explicit. New v6.1 SQL ordering and cutoffs must be specified without touching this file. |
| Queue metadata | `v6/queue_bridge/duck_queue.sql:2–36,169–232,357–462` | Closed kind/capability arrays; COMMENT-bound handler discovery; apply locks operation/session, enforces op_seq, terminal-state replay shortcut, updates metadata, resumes. Apply has no source-file side-effect receipt or lease fencing token. |
| Generic apply | `v4/plugin_taxonomy/plugin_taxonomy.sql:80–166` | Resolves `queue_handler` from `plugin_bindings`, deduplicates `(queue_name,msg_id)`, executes handler dynamically. |
| Enqueue | `v6/duck_tools/duck_tools.sql:7–34,36–73` | Caller current run, op ledger and op_seq, payload MD5, copies full `p_payload` into queue message, then `pgmq.send`. SECURITY INVOKER. Parameter validation and artifact drop dependency check, no file authorization/CAS contract to inherit. |
| Claim / execution | `v6/queue_bridge/duckdb_processor.py:112–191,193–284` | PG writes during claim; 300s stale lease; run_schema reclaim vs temp LOST; claim commits before DuckDB work; process-local `_completed` memoization; results applied later. A file write after claim and before apply needs its own crash contract. |
| Worker routing / result transaction | `v6/kernel_freeze/worker.py:29–31,286–320,411–477,480–514,553–574` | Python queue routing is fixed; only duck queue gets special retry-body interception; result apply+archive is a later PG transaction. Existing LLM worker and new codefs worker need an explicit bridge without importing incompatible DuckDB runtimes. |
| Session/replay | `v6/session_durability/duckdb_runtime.py:121–203,239–291` | Manager directly writes session metadata; reopening run_schema re-reads sources via `snapshot_table`; view hash is hash of SQL text, not source bytes. This does not by itself provide immutable file-revision replay. |
| Runtime hardening | `v6/session_durability/duckdb_grammar.py:19–21,151–166,182–189,241–285` | Locked v6 identity, optional grammar capabilities, disable autoinstall/autoload, digest-checked load, disable external access, 512 MiB memory. New release-plane config must be independent. |
| SQL guard | `v6/dialect_guardrails/duckdb_validation.py:8–16,29–56,58–80` | SELECT-only plus token/function denylist; quoted tokens skipped. This is not a catalog-aware authorization policy for newly loaded native functions or SQL macros; do not infer a demonstrated exploit. |
| Prompt | `v6/dialect_guardrails/duck_prompt.sql:1–22`; `v5/prompt_pipeline/prompt_pipeline.sql:53–118` | v3 recipe describes artifact-only reads; live slots call registered retrievers. Prompt visibility is not an execution permission check. |

## Local primary upstream evidence

Native command collector and full raw outputs (including exact commands, stdout/stderr and exit):
- `prompt-exports/v6.1-baseline-native-collector-2026-09-17.py`
- `prompt-exports/v6.1-baseline-native-evidence-2026-09-17.md` (initial C1–C6 capture: 1186 lines; final rerun adds C7 canonical plan-line capture).

Collector invocation: `python3 prompt-exports/v6.1-baseline-native-collector-2026-09-17.py > prompt-exports/v6.1-baseline-native-evidence-2026-09-17.md`; exit 0, no terminal stdout/stderr. C1–C6 reproduce earlier interactive read-only Python heredocs, all exit 0; earlier C4 displayed truncated output, captured rerun is complete. Script bodies and exact rerun commands are preserved, not runtime gates. Child reports did not provide complete native command transcripts; only coordinator-captured commands count as auditable command evidence.

### Local identities (HEAD, not a claim of clean checkout or installed binary identity)

| Checkout | HEAD |
|---|---|
| `/Users/wxl/Projects/duckdb-pgagent` | `a1f0ab191185c0852b162adc6feb206822dc9daa` |
| `/Users/wxl/Projects/sitting_duck` | `9d11f16d351ec64964b50f2f579b8001a64a8712` |
| `/Users/wxl/Projects/tigerfs` | `6580f5bb68dfbca34fb1e086fa583ea77a90d738` |
| `/Users/wxl/Projects/pgembed` | `ffc32ff4a6ac84ba8c263f4bb45b863a2820725c` |
| `/Users/wxl/Projects/duckdb-web` | `cd638b3402bea801087c34a2cb7c170d9c38f6e7` |
| `/Users/wxl/Projects/duckdb_webbed` | `3190271fd628391aba88bdb2314d943eb6d2fb62` |

`/Users/wxl/Projects/duck_block_utils` is absent. No attempt was made to install or clone it. Local HEAD/source is not proof of what a future signed community binary contains.

### ABI / file sandbox

`/Users/wxl/Projects/duckdb-pgagent/src/main/extension.cpp:51–58`: CPP and C_STRUCT_UNSTABLE metadata compare exact DuckDB version directory name. `src/main/extension/extension_install.cpp:36–44`: release version vs dev SourceID. This supports source-based ABI concern, not a performed LOAD failure.

`src/main/settings/custom_settings.cpp:270–280`: allowed directories cannot be changed with external access disabled or before filesystem exists. `src/main/config.cpp:924–960`: with external access disabled, allowed paths/directories may still permit file access. Enabling an allowed prefix is a relaxation of deny-all, NOT stricter than deny-all as plan line 129 says. These are fork-source observations; selected release-plane and third-party extension behavior need their own fail-closed verification.

`/Users/wxl/Projects/pgembed/pgbuild/Makefile:687–696` downloads TigerFS release archive, checks pinned SHA, installs binary. No build or CLI execution occurred here.

### sitting_duck content budgets are real registration, but not release/runtime proof

`src/parse_ast_function.cpp:22–69,176–189`:

```cpp
// Bind (lines 32–36)
string context_str = "native";
string source_str = "lines";
string structure_str = "full";
string peek_str = "smart";
int32_t peek_size = 120;
// lines 62–67
ParseResourceCapParameters(input.named_parameters, config);
return_types = UnifiedASTBackend::GetFlatDynamicTableSchema(config);
names = UnifiedASTBackend::GetFlatDynamicTableColumnNames(config);
// registration lines 187–189
parse_ast_func.named_parameters["max_source_bytes"] = LogicalType::BIGINT;
parse_ast_func.named_parameters["parse_timeout_ms"] = LogicalType::BIGINT;
parse_ast_func.named_parameters["max_parse_nodes"] = LogicalType::BIGINT;
```

`src/read_ast_streaming_function.cpp:624–653` also registers those caps plus path-only controls such as `batch_size`, `max_depth`, `prune`, `ignore_errors`.

Budget configuration parsing is not proof of end-to-end enforcement, positive minimums, hard cancellation, fetch-before-allocation bounds, or community binary support. Those remain explicit future prerequisites.

### The claimed mandatory 19-column schema points at the wrong helper

Plan lines 83 and 214 require comparing real `parse_ast`/`read_ast` schema to static helper's 19 columns. The public content binder instead calls **dynamic** schema helpers above.

`src/unified_ast_backend.cpp:356–361` is the static 19-name helper. `:654–716` is the dynamic helper actually bound: native context adds `signature_type`, `parameters`, `modifiers`, `annotations` (`:672–676`); line source omits columns while full source adds them (`:680–693`); structural/peek columns depend on config (`:696–714`). No runtime column count is claimed here. A selected-version/options schema manifest is required; hard-coding 19 as source authority is contradicted by inspected binding.

### AST-to-blocks: public convenience wrapper reads files

`/Users/wxl/Projects/sitting_duck/src/sql_macros/duck_blocks.sql:47–62,69–95,298–353`:

```sql
-- lines 304–321: ast_to_blocks(source, language := NULL, ...)
WITH __ast_blocks_src AS (
    SELECT * FROM read_ast(source, language, peek := 'full')
)
SELECT * FROM ast_to_blocks_from('__ast_blocks_src', ...);
-- lines 333–352: ast_to_blocks_list wraps ast_to_blocks and aggregates
list(block ORDER BY element_order) AS blocks
```

`ast_to_blocks_from(source, ...)` instead uses `query_table(source)` at line 95. Its comments at 47–62 explicitly require full peek for complete bodies; default smart peek can truncate. These are candidate integration constraints for plan lines 85 and 287–291 under D2-A (no filesystem access). Do not silently turn D2-B on to claim D2-A works. `db_render_blocks` implementation is not locally verified; the sibling's SQL comment illustrates a list input, not authoritative duck_block_utils runtime proof.

### TigerFS identity and concurrency

`/Users/wxl/Projects/tigerfs/docs/spec.md:1296–1337` describes file-first backing table: UUID `id`, `parent_id`, leaf `filename`, `filetype`, `title`, `author`, `headers`, `body`, `encoding`, timestamps, composite uniqueness; synthesized `md`/`txt` view metadata selects rendering. It is not a universal `(path PK, content)` table. This supports keeping file-first/data-first/byte codec as an unresolved explicit P1-stage decision, and requiring hash/CAS to bind to the authoritative byte representation selected.

`:749–760` describes operation-level autocommit, row-as-file atomic single UPDATE, multi-column directory writes non-atomic. `:3398–3413` explicitly states no additional locking and last-write-wins per column. Atomic single UPDATE does NOT imply expected-version CAS or crash-safe exactly-once logical mutation.

## Questions for independent full-plan review (not assigned verdicts)

- Do path-only mirror PK and pending D5 actually define a run/workspace authorization boundary and canonical identity? What must be frozen before S1/S2 rather than deferred to S7?
- How can the same proposed worker lack state-table write privileges while copying claim/session/apply code that writes state? Which narrow authority owns claim, apply, resume/archive, and revocation?
- Is D4 a one-time product approval or per-operation authority? What gates reject unapproved writes, including render if it persists bytes?
- Where are expected content hash/generation, cross-run shared-file concurrency, durable request receipt, lease fencing, and crash-after-write-before-apply recovery specified? Distinguish metadata apply idempotency from physical mutation idempotency.
- Can replay re-read changing files yet maintain content identity? Which authoritative bytes/config/version are retained or verified, and can replay ever issue writes?
- Does arbitrary user SELECT/block_query permit unvetted path/native functions/macros? A future negative gate must cover the selected extension set, not just v6 blacklist.
- Do stage gates depend on facilities only added later (S2 replay vs S3 materialization, S3 op processing vs S4 queue), and are new SQL paths/cutoffs explicit? Distinguish fixable sequencing gaps from mere missing implementation.
- Are file content transport, byte limits, errors, budgets and positive control paths fully defined before granting writes? The v6 enqueue template copies the entire payload to PGMQ.
- Do public schema/macro claims match the source path actually invoked? Is each required extension/API/budget selected-version evidence or an explicit unpassed prerequisite?

## Immutable scope fingerprints and initial Git state

Initial branch `main` tracking `origin/main`; no tracked changes; exactly five existing untracked files (target plan plus unrelated v11 and three v8 plans). Preserve all. Fingerprints before and after source capture matched:

- loop memory: `6df81666538fb5e6fd3fada3ca531510e6711cb51e98ed5e30ed17881176f1e0`
- root AGENTS: `45f5d74bdaa19dc9cfbdca39acd65a4056aa610e53d493e612a37ead054ceb1a`
- root pyproject: `b3751495ce0a1474715b8c50c80751310c7035b0b055e2ab049bda7a96ebd59f`
- root uv.lock: `3163d60f86a8831759b057932681e6f56ebb33fe1efc98634b0acaf85b6f643e`
- all 163 tracked files under v1–v6, sorted path NUL + per-file SHA-256 binary digest, composite: `91c20842d9de4039774277bd9752e5fbe025bf30b80ecbec020bdca943115bd9`

A web lookup of official DuckDB extension distribution docs was attempted but returned no usable tool content. No web-derived claim or citation is used as proof. All external technical facts above are scoped to the captured local primary source. Availability of current remote community artifacts remains unverified.
