"""v8 grant stage package (G10).

v8_grant.sql — the complete section 2.1/2.2 model: the migrated G9a stub
DDL replaced by the full slice/grant model, parameter-level constraint
evaluation, the lock+judge linearization suboperation, the operator
channel, RLS, the stream registry, workspace_handles with the
WORKSPACE_LOST fail-closed drain, the shared pre-dispatch dual-table sync
suboperation and the minimal fork.
"""
